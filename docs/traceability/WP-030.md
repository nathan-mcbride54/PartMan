<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-030 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| MODEL-001 | `apps/desktop/src/format-bytes.ts`, `apps/desktop/src/strings.ts`, `apps/desktop/src/preview.ts`, `packages/ui/src/Inspector.tsx`, `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `cargo xtask desktop` | One BigInt formatter derives IEC display sizes and grouped exact bytes from the same value. Device/node sizes and every byte-valued inspector fact stay typed as BigInt until that locale-aware rendering boundary, where both forms are shown. Component-owned English labels—including semantic-role labels—count grammar, formatter locale, themes, and health presentation are externalized in the desktop catalogue. |
| PLAN-004 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| PLAN-004 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| PLAN-004 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| PLAN-004 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| PLAN-004 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| SAFE-002 | `apps/desktop/src-tauri/src/lib.rs`, `apps/desktop/src-tauri/tauri.conf.json`, `apps/desktop/src-tauri/capabilities/main-ui.json`, `apps/desktop/src/security-boundary.test.ts`, `cargo xtask desktop` | The default shell is unprivileged: the native host registers no command or plugin, the only window capability has an empty permission list, and the production gate requests no elevation or device access. |
| SAFE-009 | `crates/tokens/Cargo.toml`, `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/src/lib.rs`, `cargo xtask ci`, `cargo xtask desktop` | The token crate and native shell host inherit the workspace lint policy, including denied unsafe code. The Tier-1 and desktop gates compile and lint these unprivileged boundaries; the host contains no unsafe exception. |
| Section 11.7 | `docs/work-packages/WP-030.md`, `cargo xtask traceability` | WP-030 traceability is generated from validated sources, with an explicit zero-loss disposition for every row and narrative section in the former hand-maintained document. |
| Section 12 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| Section 4.1 | `apps/desktop/src-tauri/Cargo.toml`, `apps/desktop/src-tauri/tauri.conf.json`, `apps/desktop/package.json`, `apps/desktop/tsconfig.json`, `apps/desktop/src/App.tsx`, `cargo xtask desktop` | The shell uses the required Tauri 2 native host with a React and TypeScript front end, and the gate builds their packaged assets into a native release executable. |
| UI-001 | `a_missing_file_is_refused_rather_than_treated_as_an_empty_token_set` | A missing token source is a read error rather than a default palette or a vacuously clean audit |
| UI-001 | `apps/desktop/src/App.tsx`, `apps/desktop/src/strings.ts`, `packages/design-tokens/src/generated.css`, `apps/desktop/src/shell.test.tsx`, `cargo xtask desktop` | The shell starts in the canonical dark theme and exposes system, light, and high-contrast choices whose styles are generated from the one token source. The desktop gate proves the consumed generated stylesheet is current. |
| UI-001 | `colour_lookup_resolves_through_theme_and_role` | Theme-and-role lookup resolves only declared values and returns no invented fallback for unknown themes or semantic roles |
| UI-001 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| UI-001 | `every_theme_is_audited_not_just_the_default` | The harness evaluates every supported theme, including high contrast, instead of proving only that the default dark palette passes |
| UI-001 | `loading_the_repository_token_file_succeeds_and_carries_its_versions` | The shipped token file loads with its pinned specification and token-set versions and includes the default dark theme |
| UI-001 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| UI-001 | `the_repository_token_file_is_where_the_crate_expects_it` | The crate resolves the one repository token source from its manifest directory rather than from a caller-controlled working directory |
| UI-001 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| UI-001 | `the_required_themes_include_the_default_and_the_accessible_one` | Independent policy requires the default dark theme, accessible high-contrast theme, and system light theme |
| UI-002 | `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `packages/ui/src/PartManShell.tsx`, `packages/ui/src/TopologyMap.tsx`, `packages/design-tokens/src/generated.ts`, `docs/quality/accessibility.md`, `cargo xtask desktop` | The read-only synthetic shell renders every UI-002 region and all eight UI-003 entity roles. Native controls and ARIA state accompany visible selection text, while all eight entity roles combine text with generated icons and generated shapes. The typed pairing boundary prevents UI-only role colours from painting normal text; semantic and interaction tests cover selection, theme changes, keyboard order, and the mounted collapsible drawer. This and the recorded rendered-state inspection are bounded increment-2 UI-008 evidence; the document explicitly preserves the complete responsive keyboard/focus, screen-reader, zoom, reduced-motion, and computed-style work that remains. |
| UI-003 | `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `packages/ui/src/PartManShell.tsx`, `packages/ui/src/TopologyMap.tsx`, `packages/design-tokens/src/generated.ts`, `docs/quality/accessibility.md`, `cargo xtask desktop` | The read-only synthetic shell renders every UI-002 region and all eight UI-003 entity roles. Native controls and ARIA state accompany visible selection text, while all eight entity roles combine text with generated icons and generated shapes. The typed pairing boundary prevents UI-only role colours from painting normal text; semantic and interaction tests cover selection, theme changes, keyboard order, and the mounted collapsible drawer. This and the recorded rendered-state inspection are bounded increment-2 UI-008 evidence; the document explicitly preserves the complete responsive keyboard/focus, screen-reader, zoom, reduced-motion, and computed-style work that remains. |
| UI-003 | `colour_lookup_resolves_through_theme_and_role` | Theme-and-role lookup resolves only declared values and returns no invented fallback for unknown themes or semantic roles |
| UI-003 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| UI-003 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| UI-003 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| UI-003 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-003 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| UI-007 | `a_file_missing_a_required_section_is_refused_not_defaulted` | Omitting a required policy section is rejected instead of defaulted, so absent non-colour channels cannot pass the accessibility gate |
| UI-007 | `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `packages/ui/src/PartManShell.tsx`, `packages/ui/src/TopologyMap.tsx`, `packages/design-tokens/src/generated.ts`, `docs/quality/accessibility.md`, `cargo xtask desktop` | The read-only synthetic shell renders every UI-002 region and all eight UI-003 entity roles. Native controls and ARIA state accompany visible selection text, while all eight entity roles combine text with generated icons and generated shapes. The typed pairing boundary prevents UI-only role colours from painting normal text; semantic and interaction tests cover selection, theme changes, keyboard order, and the mounted collapsible drawer. This and the recorded rendered-state inspection are bounded increment-2 UI-008 evidence; the document explicitly preserves the complete responsive keyboard/focus, screen-reader, zoom, reduced-motion, and computed-style work that remains. |
| UI-007 | `delta_e_is_zero_for_identity_and_symmetric` | CIE76 colour difference is zero for identity and symmetric for reversed inputs |
| UI-007 | `delta_e_separates_black_from_white_by_the_full_lightness_range` | CIE76 reproduces the externally fixed full lightness range of approximately 100 between black and white |
| UI-007 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| UI-007 | `red_and_green_converge_for_protanopia_and_deuteranopia` | Protanopia and deuteranopia simulations exhibit their defining red-green convergence instead of merely returning plausible numbers |
| UI-007 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| UI-007 | `simulation_leaves_greys_untouched` | Every colour-vision simulation preserves the achromatic axis closely enough to catch a mistyped matrix row |
| UI-007 | `surfaces_and_text_do_not_carry_meaning_under_ui_007` | Surfaces, text, borders, and focus decoration remain outside the meaning-bearing roster so redundant-channel rules target identity and state rather than visual primitives |
| UI-007 | `the_colour_separation_floor_is_recorded_as_a_project_decision` | The colour-separation smell-test floor is an explicit project decision above the collapse that prompted it, not a value the palette can lower |
| UI-007 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| UI-007 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-007 | `the_summary_reports_the_tightest_pairing_it_saw` | A passing audit reports its tightest contrast and colour-vision margins instead of hiding proximity to either policy floor |
| UI-007 | `tritanopia_collapses_blue_without_collapsing_red_against_green` | Tritanopia collapses the blue-yellow axis without being an accidental copy of a red-green deficiency matrix |
| UI-008 | `a_file_missing_a_required_section_is_refused_not_defaulted` | Omitting a required policy section is rejected instead of defaulted, so absent non-colour channels cannot pass the accessibility gate |
| UI-008 | `a_malformed_colour_is_refused_at_load_rather_than_at_first_use` | A malformed colour in any declared role is rejected while loading the whole token set rather than deferred until first use |
| UI-008 | `a_missing_file_is_refused_rather_than_treated_as_an_empty_token_set` | A missing token source is a read error rather than a default palette or a vacuously clean audit |
| UI-008 | `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `packages/ui/src/PartManShell.tsx`, `packages/ui/src/TopologyMap.tsx`, `packages/design-tokens/src/generated.ts`, `docs/quality/accessibility.md`, `cargo xtask desktop` | The read-only synthetic shell renders every UI-002 region and all eight UI-003 entity roles. Native controls and ARIA state accompany visible selection text, while all eight entity roles combine text with generated icons and generated shapes. The typed pairing boundary prevents UI-only role colours from painting normal text; semantic and interaction tests cover selection, theme changes, keyboard order, and the mounted collapsible drawer. This and the recorded rendered-state inspection are bounded increment-2 UI-008 evidence; the document explicitly preserves the complete responsive keyboard/focus, screen-reader, zoom, reduced-motion, and computed-style work that remains. |
| UI-008 | `black_on_white_is_the_published_maximum_of_twenty_one_to_one` | The contrast implementation reproduces WCAG's published maximum of 21 to 1 for black on white |
| UI-008 | `both_versions_are_pinned_outside_the_token_file` | Both the specification vocabulary version and token-set version are pinned outside the file being audited and cannot be conflated |
| UI-008 | `contrast_is_symmetric_and_bottoms_out_at_one` | Contrast is order-independent and every identical foreground/background pair bottoms out at one to one |
| UI-008 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| UI-008 | `every_theme_is_audited_not_just_the_default` | The harness evaluates every supported theme, including high contrast, instead of proving only that the default dark palette passes |
| UI-008 | `loading_the_repository_token_file_succeeds_and_carries_its_versions` | The shipped token file loads with its pinned specification and token-set versions and includes the default dark theme |
| UI-008 | `luminance_is_ordered_by_lightness` | Relative luminance remains strictly ordered from the shipped dark surface through light text, catching an inverted transfer function |
| UI-008 | `malformed_json_is_refused_with_the_path_in_the_message` | Malformed JSON fails closed with source-path context instead of becoming an empty or partial accessibility policy |
| UI-008 | `parsing_accepts_six_digit_hexadecimal_in_either_case` | The token reader accepts exactly six-digit sRGB hexadecimal in either letter case |
| UI-008 | `parsing_refuses_every_other_spelling` | Plausible alternate colour spellings are rejected rather than silently interpreted into a value nobody declared |
| UI-008 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| UI-008 | `the_caveats_are_carried_into_the_output` | Audit output names the keyboard, screen-reader, zoom, and other rendered behavior that a static token harness does not establish |
| UI-008 | `the_green_channel_dominates_luminance` | Relative luminance preserves the published channel-weight ordering with green above red above blue |
| UI-008 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| UI-008 | `the_summary_reports_the_tightest_pairing_it_saw` | A passing audit reports its tightest contrast and colour-vision margins instead of hiding proximity to either policy floor |
| UI-008 | `the_wcag_floors_are_the_published_values` | The independent policy pins normal-text and non-text contrast to the published WCAG 2.2 AA floors rather than values supplied by the palette under audit |
| UI-008 | `threshold_names_map_to_the_criterion_they_mean_and_nothing_else` | Pairing kinds map only to their exact external contrast criteria and unknown spellings fail closed instead of selecting a default |
| UI-011 | `every_check_rejects_a_mutation_that_defeats_it` | Twenty-seven named hostile mutations prove the static policy checks can fail for theme, vocabulary, channel, contrast, version, and progress-state regressions |
| UI-011 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `cargo xtask tokens` | One token source is checked against independent theme, vocabulary, contrast, redundant-channel, and colour-separation policy. This establishes the static design contract only; rendered and interactive accessibility remain explicitly outside the claim. |
| UI-011 | `the_repository_token_set_satisfies_every_rule` | The shipped token set passes a non-vacuous audit over the required themes, semantic roles, redundant channels, contrast pairings, and risk vocabulary |
| UI-011 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-011 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| UI-013 | `apps/desktop/src/format-bytes.ts`, `apps/desktop/src/strings.ts`, `apps/desktop/src/preview.ts`, `packages/ui/src/Inspector.tsx`, `apps/desktop/src/shell.test.tsx`, `apps/desktop/src/interactions.test.tsx`, `cargo xtask desktop` | One BigInt formatter derives IEC display sizes and grouped exact bytes from the same value. Device/node sizes and every byte-valued inspector fact stay typed as BigInt until that locale-aware rendering boundary, where both forms are shown. Component-owned English labels—including semantic-role labels—count grammar, formatter locale, themes, and health presentation are externalized in the desktop catalogue. |

14 requirement(s), 31 annotation(s), 8 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
