# WP-030 traceability

- Spec version: 4.0.0
- Work package: WP-030, increment 1
- Requirements affected: UI-001, UI-007, UI-008
- Requirements referenced but **not** established: UI-002, UI-003, UI-011,
  ACC-011
- Safety constraints preserved: SAFE-001, SAFE-002, SAFE-009

| Evidence | Requirement or contract |
| --- | --- |
| `schemas/design-tokens.json` | Single source of truth for the visual language; read by the harness and, when it exists, by the front end |
| `themes_are_present`, `every_theme_is_audited_not_just_the_default` | UI-001 dark default, accessible high contrast, and system light theme all exist and define the same roles |
| `check_contrast` over every theme and pairing | UI-008 WCAG 2.2 AA: 4.5:1 text (SC 1.4.3), 3:1 interface components (SC 1.4.11) |
| `check_non_color_channels` | UI-007 colour is never the only carrier: icon, label and shape declared for every role that means something, and no two roles share an icon *and* a label |
| `check_color_vision` | UI-007 supporting smell test: risk-bearing pairs stay apart under simulated protanopia, deuteranopia and tritanopia |
| `every_check_rejects_a_mutation_that_defeats_it` (12 mutations) | Section 12: every check is proved capable of failing, and named by the requirement that catches it |
| `black_on_white_is_the_published_maximum_of_twenty_one_to_one`, `delta_e_separates_black_from_white_by_the_full_lightness_range` | Colour maths anchored to externally published values, not to numbers this code emitted |
| `red_and_green_converge_for_protanopia_and_deuteranopia`, `tritanopia_collapses_blue_without_collapsing_red_against_green`, `simulation_leaves_greys_untouched` | Colour-vision matrices checked by their defining property rather than by trusting transcribed digits |
| `parsing_refuses_every_other_spelling`, `a_malformed_colour_is_refused_at_load_rather_than_at_first_use` | Strict reader: a colour is refused rather than interpreted, and the whole file is validated at load |
| `cargo xtask tokens`, run inside `cargo xtask ci` | The harness gates CI on all three operating systems rather than being available to run |

## Verification

The local evidence command is `cargo xtask tokens`, and it also runs inside
`cargo xtask ci`. It reads one JSON file and computes numbers from it. It
contains no privileged or destructive behaviour, enumerates no device, opens no
block device, and launches no process.

The harness prints the tightest contrast pairing and the closest colour-vision
pair it measured, so a pass reports its own margin rather than only its verdict.
At the time of writing those are 3.57:1 against a 3.0 floor, and delta-E 21.9
against a 12.0 floor.

## Known gaps carried into later increments

- **UI-008 is only partially covered.** Contrast is computable from a token
  file; keyboard-only operation, screen-reader semantics, 200% zoom and reduced
  motion are not, and none of them are inspected. They belong to increments 2
  and 3, and **M0's "accessibility harness runs" criterion should be read as
  partially met, not met.**
- **Only declared pairings are checked.** A front end is free to combine two
  roles this file does not pair, and nothing would notice. Increment 2 must add
  a check that the shell uses no combination absent from
  `contrastRules.pairings`; until then the harness's coverage is bounded by what
  someone remembered to declare.
- **Non-colour channels are declared but not rendered.** Nothing verifies that a
  component actually draws the icon and label the file promises. That check
  needs a shell.
- **UI-003 and UI-011 are referenced, not established.** The token file carries a
  role for every entity type UI-003 names and every progress state UI-011 names,
  so the vocabulary is in place — but "distinguish visually and textually" is a
  claim about rendered output, and nothing renders yet.
- **The colour-vision floor of delta-E 12 is a judgement, not a standard.** WCAG
  sets no such threshold. It was chosen to be comfortably above the delta-E 10.1
  collapse this increment found and comfortably below the 21.9 the corrected
  palette achieves. A future increment with real user evidence should revisit
  it; it is recorded as chosen rather than derived.
