<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-030 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| PLAN-004 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| PLAN-004 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| PLAN-004 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| PLAN-004 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| PLAN-004 | `label_ids_are_stable_literal_contract_values` | Every semantic role maps to its exact stable meaning label and the complete 25-label roster includes concrete themes plus system selection |
| PLAN-004 | `required_distinct_pairs_are_exact` | Risk and progress role pairs whose confusion would mislead remain an exact independent roster |
| PLAN-004 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| PLAN-004 | `the_full_visual_roster_is_exactly_thirty_one_roles` | The full visual roster is exactly ten foundational roles followed by the 21 independently pinned semantic roles, with no duplicate |
| PLAN-004 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| PLAN-004 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| PLAN-004 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| SAFE-009 | `crates/tokens/Cargo.toml`, `apps/desktop/Cargo.toml`, `cargo xtask ci` | The token and desktop crates inherit the workspace lint policy, including denied unsafe code, and the Tier-1 gate compiles and lints both unprivileged boundaries. |
| Section 11.7 | `docs/work-packages/WP-030.md`, `cargo xtask traceability` | WP-030 traceability is generated from validated sources, with an explicit zero-loss disposition for every row and narrative section in the former hand-maintained document. |
| Section 12 | `bounded_displays_cut_only_between_complete_tokens` | Every bounded representation honors its character limit, preserves complete escape tokens, and marks omission with a scalar absent from full output |
| Section 12 | `escape_lookalikes_cannot_collide_with_encoded_units` | Literal backslashes and text that resembles an escape remain distinct from escaped authority units in both representation domains |
| Section 12 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| Section 12 | `every_single_authority_unit_round_trips_without_collision` | Every byte and every WTF-16 code unit has a unique domain-separated full display that an independent decoder returns to the exact authority value |
| Section 12 | `every_static_catalogue_entry_is_unique_and_nonblank` | Every closed static shell, accessibility, empty-state, and error-copy ID has one stable key and a nonblank English value |
| Section 12 | `invalid_display_limits_fail_closed` | A display limit too small for a domain prefix and unambiguous marker is rejected rather than silently exceeded |
| Section 12 | `parameterized_messages_apply_the_english_catalogue_grammar` | Count grammar and composed accessible exact-byte labels are centralized in typed catalogue messages rather than component-owned English |
| Section 12 | `registry_membership_is_order_independent_despite_a_presentation_collision` | Registry membership resolves an explicitly supplied opaque ID independently of item order even when unrelated raw identifiers share presentation-only truncated text; correct item-to-ID association remains a future view-model obligation |
| Section 12 | `selection_registry_rejects_duplicate_invalid_and_unknown_ids` | Duplicate IDs, malformed callback values, and canonical but unknown IDs fail closed at the Rust-owned selection registry |
| Section 12 | `selection_wire_encoding_is_exact_and_strict` | A caller-supplied nonzero selection value crosses the future renderer boundary through one fixed uppercase-hex string without numeric loss or alternate spellings; allocation provenance remains a future view-model obligation |
| Section 12 | `the_catalogue_and_repository_token_label_rosters_are_equal` | The independent English catalogue resolves exactly every canonical v2 theme and semantic label ID without a missing or invented entry |
| Section 12 | `the_hostile_identifier_corpus_round_trips_exactly` | Invalid UTF-8, ill-formed WTF-16, controls, bidi, combining text, non-Western text, literal backslashes, and escape lookalikes all retain their exact original units |
| Section 12 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| Section 12 | `token_label_resolution_is_total_only_for_the_declared_roster` | Token IDs are unique, their English values are nonblank, and an unknown generated ID fails closed instead of rendering a placeholder |
| Section 12 | `token_loader_rejects_duplicate_keys_in_every_map_namespace` | Every map-shaped token namespace rejects duplicate JSON member names instead of silently choosing one declaration last-wins |
| Section 12 | `very_long_identifiers_have_strictly_bounded_visual_output` | Bounded presentation handles adversarially long input while returning only the configured small output rather than materializing the full escaped display |
| UI-001 | `a_missing_file_is_refused_rather_than_treated_as_an_empty_token_set` | A missing token source is a read error rather than a default palette or a vacuously clean audit |
| UI-001 | `colour_lookup_resolves_through_theme_and_role` | Theme-and-role lookup resolves only declared values and returns no invented fallback for unknown themes or semantic roles |
| UI-001 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-001 | `every_theme_is_audited_not_just_the_default` | The harness evaluates every supported theme, including high contrast, instead of proving only that the default dark palette passes |
| UI-001 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-001 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-001 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-001 | `loading_the_repository_token_file_succeeds_and_carries_its_versions` | The shipped token file loads with its pinned specification and token-set versions and includes the default dark theme |
| UI-001 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| UI-001 | `the_catalogue_and_repository_token_label_rosters_are_equal` | The independent English catalogue resolves exactly every canonical v2 theme and semantic label ID without a missing or invented entry |
| UI-001 | `the_caveats_are_carried_into_the_output` | Audit output names the operating-system, rendering, non-colour selection, absent health-state vocabulary, catalogue, and wider accessibility behavior that static tokens do not establish |
| UI-001 | `the_repository_token_file_is_where_the_crate_expects_it` | The crate resolves the one repository token source from its manifest directory rather than from a caller-controlled working directory |
| UI-001 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-001 | `theme_roster_labels_and_signals_are_exact` | The independent policy pins the dark default, separate high-contrast theme, system selection label, and exact system-colour-scheme mapping |
| UI-001 | `token_loader_rejects_duplicate_keys_in_every_map_namespace` | Every map-shaped token namespace rejects duplicate JSON member names instead of silently choosing one declaration last-wins |
| UI-001 | `token_loader_rejects_each_missing_renderer_neutral_contract_section` | Every renderer-neutral measurement, theme, typography, layout, and cursor section is mandatory rather than defaulted to an unaudited value |
| UI-001 | `token_loader_rejects_unaudited_theme_requirement_metadata` | Theme requirement traceability is independent policy rather than unaudited prose that the canonical token file can contradict |
| UI-003 | `colour_lookup_resolves_through_theme_and_role` | Theme-and-role lookup resolves only declared values and returns no invented fallback for unknown themes or semantic roles |
| UI-003 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-003 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-003 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-003 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-003 | `label_ids_are_stable_literal_contract_values` | Every semantic role maps to its exact stable meaning label and the complete 25-label roster includes concrete themes plus system selection |
| UI-003 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| UI-003 | `the_catalogue_and_repository_token_label_rosters_are_equal` | The independent English catalogue resolves exactly every canonical v2 theme and semantic label ID without a missing or invented entry |
| UI-003 | `the_full_visual_roster_is_exactly_thirty_one_roles` | The full visual roster is exactly ten foundational roles followed by the 21 independently pinned semantic roles, with no duplicate |
| UI-003 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-003 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-003 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| UI-007 | `a_file_missing_a_required_section_is_refused_not_defaulted` | Omitting a required policy section is rejected instead of defaulted, so absent non-colour channels cannot pass the accessibility gate |
| UI-007 | `delta_e_is_zero_for_identity_and_symmetric` | CIE76 colour difference is zero for identity and symmetric for reversed inputs |
| UI-007 | `delta_e_separates_black_from_white_by_the_full_lightness_range` | CIE76 reproduces the externally fixed full lightness range of approximately 100 between black and white |
| UI-007 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-007 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-007 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-007 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-007 | `label_ids_are_stable_literal_contract_values` | Every semantic role maps to its exact stable meaning label and the complete 25-label roster includes concrete themes plus system selection |
| UI-007 | `red_and_green_converge_for_protanopia_and_deuteranopia` | Protanopia and deuteranopia simulations exhibit their defining red-green convergence instead of merely returning plausible numbers |
| UI-007 | `required_distinct_pairs_are_exact` | Risk and progress role pairs whose confusion would mislead remain an exact independent roster |
| UI-007 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| UI-007 | `simulation_leaves_greys_untouched` | Every colour-vision simulation preserves the achromatic axis closely enough to catch a mistyped matrix row |
| UI-007 | `surfaces_and_text_do_not_carry_meaning_under_ui_007` | Surfaces, text, borders, and focus decoration remain outside the meaning-bearing roster so redundant-channel rules target identity and state rather than visual primitives |
| UI-007 | `the_caveats_are_carried_into_the_output` | Audit output names the operating-system, rendering, non-colour selection, absent health-state vocabulary, catalogue, and wider accessibility behavior that static tokens do not establish |
| UI-007 | `the_colour_separation_floor_is_recorded_as_a_project_decision` | The colour-separation smell-test floor is an explicit project decision above the collapse that prompted it, not a value the palette can lower |
| UI-007 | `the_full_visual_roster_is_exactly_thirty_one_roles` | The full visual roster is exactly ten foundational roles followed by the 21 independently pinned semantic roles, with no duplicate |
| UI-007 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-007 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-007 | `the_summary_reports_the_tightest_pairing_it_saw` | A passing audit reports its tightest contrast and colour-vision margins instead of hiding proximity to either policy floor |
| UI-007 | `tritanopia_collapses_blue_without_collapsing_red_against_green` | Tritanopia collapses the blue-yellow axis without being an accidental copy of a red-green deficiency matrix |
| UI-008 | `a_file_missing_a_required_section_is_refused_not_defaulted` | Omitting a required policy section is rejected instead of defaulted, so absent non-colour channels cannot pass the accessibility gate |
| UI-008 | `a_malformed_colour_is_refused_at_load_rather_than_at_first_use` | A malformed colour in any declared role is rejected while loading the whole token set rather than deferred until first use |
| UI-008 | `a_missing_file_is_refused_rather_than_treated_as_an_empty_token_set` | A missing token source is a read error rather than a default palette or a vacuously clean audit |
| UI-008 | `black_on_white_is_the_published_maximum_of_twenty_one_to_one` | The contrast implementation reproduces WCAG's published maximum of 21 to 1 for black on white |
| UI-008 | `both_versions_are_pinned_outside_the_token_file` | Both the specification vocabulary version and breaking token-contract version are pinned literally outside the file being audited |
| UI-008 | `bounded_displays_cut_only_between_complete_tokens` | Every bounded representation honors its character limit, preserves complete escape tokens, and marks omission with a scalar absent from full output |
| UI-008 | `committed_slint_contract_has_no_generation_drift` | The committed Slint token boundary is exactly the deterministic output of the audited repository schema |
| UI-008 | `contrast_is_symmetric_and_bottoms_out_at_one` | Contrast is order-independent and every identical foreground/background pair bottoms out at one to one |
| UI-008 | `contrast_pairing_policy_is_exact` | Every current oriented foreground/background use and its text-or-UI contrast class is pinned independently of the token file |
| UI-008 | `cursor_policy_is_exact` | Text-caret width and all four semantic cursor mappings are pinned to their exact closed spellings |
| UI-008 | `escape_lookalikes_cannot_collide_with_encoded_units` | Literal backslashes and text that resembles an escape remain distinct from escaped authority units in both representation domains |
| UI-008 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-008 | `every_single_authority_unit_round_trips_without_collision` | Every byte and every WTF-16 code unit has a unique domain-separated full display that an independent decoder returns to the exact authority value |
| UI-008 | `every_theme_is_audited_not_just_the_default` | The harness evaluates every supported theme, including high contrast, instead of proving only that the default dark palette passes |
| UI-008 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-008 | `generated_function_api_is_explicitly_public_and_pure` | Every generated lookup intended for another global or a future wrapper is explicitly public and pure because Slint 1.17.1 functions default to private |
| UI-008 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-008 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-008 | `identifier_encoding_is_reversible_and_collision_safe` | Data-derived Slint identifiers round-trip every UTF-8 byte and remain distinct under Slint 1.17.1 normalization and Rust enum case conversion |
| UI-008 | `integer_measurements_have_exact_slint_literals` | Integer token measurements render without binary floating-point conversion or host-dependent formatting |
| UI-008 | `invalid_display_limits_fail_closed` | A display limit too small for a domain prefix and unambiguous marker is rejected rather than silently exceeded |
| UI-008 | `layout_policy_is_exact` | Spacing, radius, stroke, focus-offset, padding, layout-spacing, and minimum-target values are exact independent policy declarations |
| UI-008 | `loading_the_repository_token_file_succeeds_and_carries_its_versions` | The shipped token file loads with its pinned specification and token-set versions and includes the default dark theme |
| UI-008 | `luminance_is_ordered_by_lightness` | Relative luminance remains strictly ordered from the shipped dark surface through light text, catching an inverted transfer function |
| UI-008 | `malformed_json_is_refused_with_the_path_in_the_message` | Malformed JSON fails closed with source-path context instead of becoming an empty or partial accessibility policy |
| UI-008 | `measurement_unit_policy_is_exact` | Logical-pixel and permille suffixes have exact renderer-neutral meanings outside the canonical file they qualify |
| UI-008 | `pair_identifier_encoding_preserves_orientation` | Oriented pair identifiers retain both source roles without depending on a delimiter that either encoded atom can contain |
| UI-008 | `parameterized_messages_apply_the_english_catalogue_grammar` | Count grammar and composed accessible exact-byte labels are centralized in typed catalogue messages rather than component-owned English |
| UI-008 | `parsing_accepts_six_digit_hexadecimal_in_either_case` | The token reader accepts exactly six-digit sRGB hexadecimal in either letter case |
| UI-008 | `parsing_refuses_every_other_spelling` | Plausible alternate colour spellings are rejected rather than silently interpreted into a value nobody declared |
| UI-008 | `registry_membership_is_order_independent_despite_a_presentation_collision` | Registry membership resolves an explicitly supplied opaque ID independently of item order even when unrelated raw identifiers share presentation-only truncated text; correct item-to-ID association remains a future view-model obligation |
| UI-008 | `rendering_is_byte_deterministic_and_platform_neutral` | The generated source is deterministic UTF-8 text with LF-only line endings, no BOM, and one terminal newline |
| UI-008 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| UI-008 | `selection_registry_rejects_duplicate_invalid_and_unknown_ids` | Duplicate IDs, malformed callback values, and canonical but unknown IDs fail closed at the Rust-owned selection registry |
| UI-008 | `selection_wire_encoding_is_exact_and_strict` | A caller-supplied nonzero selection value crosses the future renderer boundary through one fixed uppercase-hex string without numeric loss or alternate spellings; allocation provenance remains a future view-model obligation |
| UI-008 | `text_and_ui_contrast_boundaries_are_statically_distinct` | Normal-text and UI-only pair rosters have different Slint ID, declaration, result, and resolver types rather than a lower-threshold runtime kind flag |
| UI-008 | `text_flow_and_input_policy_are_exact` | Text-flow enum spellings, text-input style and flow, and selected-text contrast roles are pinned independently and literally |
| UI-008 | `the_caveats_are_carried_into_the_output` | Audit output names the operating-system, rendering, non-colour selection, absent health-state vocabulary, catalogue, and wider accessibility behavior that static tokens do not establish |
| UI-008 | `the_full_visual_roster_is_exactly_thirty_one_roles` | The full visual roster is exactly ten foundational roles followed by the 21 independently pinned semantic roles, with no duplicate |
| UI-008 | `the_green_channel_dominates_luminance` | Relative luminance preserves the published channel-weight ordering with green above red above blue |
| UI-008 | `the_hostile_identifier_corpus_round_trips_exactly` | Invalid UTF-8, ill-formed WTF-16, controls, bidi, combining text, non-Western text, literal backslashes, and escape lookalikes all retain their exact original units |
| UI-008 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-008 | `the_summary_reports_the_tightest_pairing_it_saw` | A passing audit reports its tightest contrast and colour-vision margins instead of hiding proximity to either policy floor |
| UI-008 | `the_wcag_floors_are_the_published_values` | The independent policy pins normal-text and non-text contrast to the published WCAG 2.2 AA floors rather than values supplied by the palette under audit |
| UI-008 | `threshold_names_map_to_the_criterion_they_mean_and_nothing_else` | Pairing kinds map only to their exact external contrast criteria and unknown spellings fail closed instead of selecting a default |
| UI-008 | `token_loader_enforces_integer_units_and_their_signedness` | Every numeric token field uses its declared bounded integer representation, including signed negative letter spacing, instead of accepting fractional or out-of-range values |
| UI-008 | `token_loader_rejects_duplicate_keys_in_every_map_namespace` | Every map-shaped token namespace rejects duplicate JSON member names instead of silently choosing one declaration last-wins |
| UI-008 | `token_loader_rejects_each_missing_renderer_neutral_contract_section` | Every renderer-neutral measurement, theme, typography, layout, and cursor section is mandatory rather than defaulted to an unaudited value |
| UI-008 | `token_loader_rejects_unknown_closed_cursor_enum_value` | Closed renderer-neutral vocabularies reject cursor values that the audited contract does not define |
| UI-008 | `token_loader_rejects_unknown_nested_typography_field` | Unknown nested typography fields fail closed so a misspelled or renderer-private override cannot silently escape the shared contract |
| UI-008 | `typography_policy_is_exact` | The platform font strategy and all seven typography styles are exact independent policy values rather than schema-owned configuration |
| UI-008 | `very_long_identifiers_have_strictly_bounded_visual_output` | Bounded presentation handles adversarially long input while returning only the configured small output rather than materializing the full escaped display |
| UI-011 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-011 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-011 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-011 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-011 | `label_ids_are_stable_literal_contract_values` | Every semantic role maps to its exact stable meaning label and the complete 25-label roster includes concrete themes plus system selection |
| UI-011 | `required_distinct_pairs_are_exact` | Risk and progress role pairs whose confusion would mislead remain an exact independent roster |
| UI-011 | `schemas/design-tokens.json`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `cargo xtask tokens` | One token source is checked against independent theme-signal, complete colour-roster, semantic-vocabulary, measurement-unit, typography, layout, cursor, selection-pairing, exact oriented contrast-pairing, redundant-channel, and exact colour-separation-roster policy. The same audited source deterministically generates the committed typed Slint ABI, and dirty regeneration fails the command. This establishes the static design/generation contract only; Slint compiler acceptance, operating-system signals, and rendered or interactive accessibility remain outside the claim. |
| UI-011 | `the_catalogue_and_repository_token_label_rosters_are_equal` | The independent English catalogue resolves exactly every canonical v2 theme and semantic label ID without a missing or invented entry |
| UI-011 | `the_full_visual_roster_is_exactly_thirty_one_roles` | The full visual roster is exactly ten foundational roles followed by the 21 independently pinned semantic roles, with no duplicate |
| UI-011 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-011 | `the_roster_has_no_duplicates_and_every_entry_carries_meaning` | The required semantic roster contains no duplicate and agrees with the predicate that demands redundant non-colour channels |
| UI-011 | `the_vocabulary_matches_the_specification_it_claims_to_come_from` | The independent vocabulary pins all eight storage entities, all five risk severities, and all eight progress states to their owning requirements |
| UI-013 | `committed_slint_contract_has_no_generation_drift` | The committed Slint token boundary is exactly the deterministic output of the audited repository schema |
| UI-013 | `every_policy_family_rejects_a_hostile_mutation` | Named hostile mutations prove every independently pinned v2 policy family can fail while the evidence remains independent of the table's changing row count |
| UI-013 | `every_static_catalogue_entry_is_unique_and_nonblank` | Every closed static shell, accessibility, empty-state, and error-copy ID has one stable key and a nonblank English value |
| UI-013 | `generated_contract_is_complete_but_renderer_and_catalogue_neutral` | The generated contract carries every version-2 token family but no widget palette, style metric, asset, translation, or English display-label channel |
| UI-013 | `generated_identifier_vocabulary_matches_the_audited_schema` | Every generated enum inventory exactly matches its audited source roster, including labels, renderer metrics, cursors, and oriented text-versus-UI contrast IDs |
| UI-013 | `generation_refuses_a_token_set_that_fails_policy` | Rendering starts only after the independent token audit accepts the complete versioned contract |
| UI-013 | `identifier_encoding_is_reversible_and_collision_safe` | Data-derived Slint identifiers round-trip every UTF-8 byte and remain distinct under Slint 1.17.1 normalization and Rust enum case conversion |
| UI-013 | `label_ids_are_stable_literal_contract_values` | Every semantic role maps to its exact stable meaning label and the complete 25-label roster includes concrete themes plus system selection |
| UI-013 | `parameterized_messages_apply_the_english_catalogue_grammar` | Count grammar and composed accessible exact-byte labels are centralized in typed catalogue messages rather than component-owned English |
| UI-013 | `rendering_is_byte_deterministic_and_platform_neutral` | The generated source is deterministic UTF-8 text with LF-only line endings, no BOM, and one terminal newline |
| UI-013 | `schemas/design-tokens.json`, `crates/tokens/src/tokens.rs`, `crates/tokens/src/policy.rs`, `crates/tokens/src/audit.rs`, `crates/tokens/src/slint.rs`, `packages/design-tokens/generated/partman-tokens.slint`, `apps/desktop/src/catalogue.rs`, `apps/desktop/src/catalogue/tests.rs`, `cargo xtask tokens` | Canonical theme and semantic entries carry stable label IDs rather than English display strings, the generated ABI carries typed IDs rather than labels, and the independent closed English catalogue resolves exactly all 25 canonical IDs. This establishes the static externalized-string boundary; component wiring, exact-byte inspector presentation, and rendered labels remain future application evidence. |
| UI-013 | `the_catalogue_and_repository_token_label_rosters_are_equal` | The independent English catalogue resolves exactly every canonical v2 theme and semantic label ID without a missing or invented entry |
| UI-013 | `the_caveats_are_carried_into_the_output` | Audit output names the operating-system, rendering, non-colour selection, absent health-state vocabulary, catalogue, and wider accessibility behavior that static tokens do not establish |
| UI-013 | `the_repository_token_set_satisfies_every_rule` | The shipped v2 token contract passes a non-vacuous audit over every independently pinned policy family without coupling evidence to a check count |
| UI-013 | `theme_roster_labels_and_signals_are_exact` | The independent policy pins the dark default, separate high-contrast theme, system selection label, and exact system-colour-scheme mapping |
| UI-013 | `token_label_resolution_is_total_only_for_the_declared_roster` | Token IDs are unique, their English values are nonblank, and an unknown generated ID fails closed instead of rendering a placeholder |
| UI-013 | `token_loader_rejects_duplicate_keys_in_every_map_namespace` | Every map-shaped token namespace rejects duplicate JSON member names instead of silently choosing one declaration last-wins |
| UI-013 | `token_loader_rejects_legacy_theme_and_semantic_channel_label_fields` | Theme and semantic-channel labels are localization identifiers, and legacy embedded display-label fields are rejected both as replacements and as extra aliases |

10 requirement(s), 70 annotation(s), 4 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
