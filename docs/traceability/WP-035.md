<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-035 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| CAP-004 | `an_absent_tool_reports_where_partman_looked` | An absent tool's report carries every candidate path checked in both renderings and can never render as found — a missing dependency drawn as a pass would be a plausible fake success, and a mutation doing exactly that survived the suite until these pins existed |
| CAP-004 | `the_doctor_reports_presence_version_and_range_as_facts` | The doctor reports presence, version, and tested-range membership as facts with provenance — the answering path and the sanitized banner — and a probe failure is a typed state, never a guessed version |
| CAP-004 | `version_banners_parse_or_stay_unrecognized_never_guessed` | A version banner parses to major.minor or stays unrecognized with its raw line preserved — never guessed — and the recorded util-linux banner shape parses to the recorded family |
| CLI-002 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| CLI-002 | `the_bundle_command_states_agree_with_dispatch_behavior` | The bundle's command-surface states agree with dispatch behavior — a command is reported answering iff it exits 0 and refusing iff it exits with the refusal code — so the diagnostics cannot claim an unimplemented surface answers |
| CLI-005 | `a_non_unicode_argument_is_refused_not_a_panic` | A non-Unicode argument is a typed usage refusal at the documented code, not a panic: std::env::args() would abort with an undocumented 101, so the binary's entry seam owns the conversion |
| CLI-005 | `exit_codes_match_the_contract_the_help_text_documents` | The documented exit codes are pinned as literals — 0 ok, 2 usage refusal, 3 typed refusal — so renumbering a constant is a visible test edit, and the help text interpolates the same constants so a renumbering cannot desynchronize the two |
| CLI-008 | `every_json_emission_is_one_well_formed_schema_versioned_envelope` | Every JSON emission, including usage refusals, is one well-formed envelope carrying the schema version; domain payloads are absent rather than emitted unversioned |
| CLI-008 | `hostile_strings_cannot_break_the_envelope` | Quotes, backslashes, and control bytes in any rendered string are escaped, so no value can break the envelope's structure or smuggle a control byte into --json output |
| CLI-008 | `no_output_in_any_mode_contains_an_ansi_sequence` | No output in any mode contains an ANSI escape byte, so NO_COLOR is honored by construction and --json output is ANSI-free |
| FS-007 | `facts_are_technology_properties_without_status_vocabulary` | Every shipped fact names a technology, an operation, a limit, and a checkable basis, and neither rendering contains CAP-003 status vocabulary — the facts are inputs to blocked reasons, and the blocked-reason surface is WP-050's |
| INV-007 | `absence_and_unavailability_are_distinct_outcomes` | A probe beyond the object's end is a positively observed absence — knowledge derived from the handle's own length — while a read failure is unavailability, and the two are distinct outcome states because treating could-not-look as looked-and-found-nothing is the fail-closed violation |
| INV-007 | `apps/cli/src/lib.rs` | Deny-by-default is the diagnostics builder's type rather than a filter: a closed field enum is the only route into the bundle, every variant renders compile-time data, and no API accepts a caller-supplied key or value. What a type cannot prove — that no variant's renderer reads the environment — is refused by the source-guard test and the empty shipped closure, with the glob-import bypass refused by the workspace's pedantic clippy gate. |
| INV-007 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| INV-007 | `missing_discovery_evidence_is_a_typed_refusal_not_an_omission` | The missing discovery evidence is an in-band typed refusal naming its increment — never an omission a reader could mistake for a clean bill of health |
| INV-007 | `replay_over_a_deterministic_fixture_image_is_attributed_and_reproducible` | Every replay observation carries its attribution — adapter name, version, method — and replay over one of WP-020's deterministic images is byte-reproducible, so the record is raw discovery evidence a reader can re-derive, not narrative |
| MODEL-003 | `every_json_emission_is_one_well_formed_schema_versioned_envelope` | Every JSON emission, including usage refusals, is one well-formed envelope carrying the schema version; domain payloads are absent rather than emitted unversioned |
| MODEL-003 | `version_reports_through_the_envelope` | The version surface reports the workspace package version through the same envelope discipline as every other surface |
| MODEL-004 | `replay_over_a_deterministic_fixture_image_is_attributed_and_reproducible` | Every replay observation carries its attribution — adapter name, version, method — and replay over one of WP-020's deterministic images is byte-reproducible, so the record is raw discovery evidence a reader can re-derive, not narrative |
| SAFE-004 | `the_doctor_probes_only_compiled_absolute_paths` | The doctor probes only the roster's compiled absolute candidate paths, in order, and nothing else — there is no PATH search to influence, and an empty roster is a typed statement rather than a silent pass |
| SAFE-004 | `the_launcher_clears_the_child_environment` | The launcher's child environment is cleared and gains exactly one written variable, LC_ALL=C — pinned as a source-text assertion because no behavioral proof fits the tier's process set, with the pin's reach stated: direct spellings, with smuggling routes closed by the empty closure and the wildcard-import lint, and the residue held by review |
| SAFE-004 | `the_real_launcher_answers_bounded_with_provenance` | The real launcher answers with bounded output, completing before the deadline fires, for a tool that exists, launched by absolute path with a structured argument array; the deadline's kill path is exercised by review and manual probe only, as doctor.rs states, and the probe subject is the compile-time-selected cargo, already in the tier's process set |
| SAFE-005 | `a_non_unicode_argument_is_refused_not_a_panic` | A non-Unicode argument is a typed usage refusal at the documented code, not a panic: std::env::args() would abort with an undocumented 101, so the binary's entry seam owns the conversion |
| SAFE-005 | `absence_and_unavailability_are_distinct_outcomes` | A probe beyond the object's end is a positively observed absence — knowledge derived from the handle's own length — while a read failure is unavailability, and the two are distinct outcome states because treating could-not-look as looked-and-found-nothing is the fail-closed violation |
| SAFE-005 | `inspect_answers_with_typed_statements_not_a_fake_topology` | Bare inspect answers with a typed no-adapter statement naming the platform package and the standing gated list carrying its register issues — never a plausible empty machine, and never silence about what the inspector will not say |
| SAFE-005 | `inspect_reports_bytes_never_classifications` | Inspect output reports bytes and access facts and never classifications: the vocabulary of interpretation — table formats, signatures, identity strength — is mechanically refused in both renderings, because classification is exactly what the register holds open |
| SAFE-005 | `replay_flag_misuse_is_refused_structurally` | The --replay flag's malformed shapes are structured usage refusals with exact spellings: a missing value, a second occurrence, and attachment to a command that is not inspect are each refused rather than guessed at |
| SAFE-005 | `replay_refuses_non_regular_objects_with_a_typed_value` | A replayed object that the opened handle reports as not a regular file is refused with a typed value before its first byte is read, and an unopenable path is a typed refusal, not a panic — so a device named by path is refused unread and the no-device-opens sentence survives |
| SAFE-005 | `unknown_tokens_are_refused_with_their_exact_spelling` | A token the parser does not recognize is refused with its exact spelling rather than guessed at, in both output modes; a second command word is refused carrying the second token's spelling, never a canonical name the user did not type |
| SAFE-006 | `apps/cli/src/lib.rs` | Deny-by-default is the diagnostics builder's type rather than a filter: a closed field enum is the only route into the bundle, every variant renders compile-time data, and no API accepts a caller-supplied key or value. What a type cannot prove — that no variant's renderer reads the environment — is refused by the source-guard test and the empty shipped closure, with the glob-import bypass refused by the workspace's pedantic clippy gate. |
| SAFE-006 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| SAFE-006 | `no_output_in_any_mode_carries_an_environment_value` | No output in any mode carries the host's username, home path, or computer name, nor any other environment value the host actually sets that is six bytes or longer and not byte-equal to a rendered compile-time constant — a tripwire whose reach stops at variables the test host sets; the source guard is what refuses an environment read regardless of host state |
| SAFE-006 | `the_human_bundle_is_pinned_byte_for_byte` | The human diagnostics rendering is pinned byte-for-byte against a literal template, so an extra human-only disclosure fails the tier exactly like a smuggled JSON key |
| SAFE-006 | `the_shipped_sources_read_no_environment_variable` | The shipped sources contain no environment read — env::var, env::vars, and var_os are absent from every shipped source file the test enumerates: lib.rs, main.rs, doctor.rs, and facts.rs — so an environment value cannot reach output regardless of which variables the host sets; compile-time env::consts and env! are the allowed forms |
| SAFE-009 | `apps/cli/Cargo.toml`, `cargo xtask ci` | The CLI crate inherits the workspace lint policy, including denied unsafe code, and the Tier-1 gate compiles and lints this unprivileged boundary. |
| SEC-007 | `apps/cli/src/lib.rs`, `apps/cli/src/doctor.rs` | The shipped binary opens no socket and reads no environment variable; offline is the construction, not a mode. Increments 3 and 4 grew the I/O reach and this statement with them: the doctor performs existence checks and version probes of roster tools at compiled absolute paths under SAFE-004's discipline — structured argument arrays, no PATH search, bounded output, a time limit, and a cleared child environment — and inspect --replay reads one caller-named object, refused through the opened handle unless it is a regular file, so a block device named by path is refused unread. Nothing else reads a file or launches a process. Tier-1 tests inject a scripted launcher and replay only temporary files they create and remove themselves, so the only executables any test launches remain git and the compile-time-selected cargo. |
| SEC-007 | `export_diagnostics_is_byte_identical_across_invocations` | The bundle is byte-identical across invocations within one environment — stability, not environment-independence, which the source guard and the empty shipped closure establish |
| SEC-007 | `the_shipped_sources_read_no_environment_variable` | The shipped sources contain no environment read — env::var, env::vars, and var_os are absent from every shipped source file the test enumerates: lib.rs, main.rs, doctor.rs, and facts.rs — so an environment value cannot reach output regardless of which variables the host sets; compile-time env::consts and env! are the allowed forms |
| Section 12 | `an_absent_tool_reports_where_partman_looked` | An absent tool's report carries every candidate path checked in both renderings and can never render as found — a missing dependency drawn as a pass would be a plausible fake success, and a mutation doing exactly that survived the suite until these pins existed |
| Section 12 | `an_empty_roster_renders_as_a_typed_statement` | An empty roster renders as a typed not-implemented statement naming the platform's adapter package — never as a clean bill that would read as all dependencies satisfied |
| Section 12 | `inspect_answers_with_typed_statements_not_a_fake_topology` | Bare inspect answers with a typed no-adapter statement naming the platform package and the standing gated list carrying its register issues — never a plausible empty machine, and never silence about what the inspector will not say |
| Section 12 | `inspect_reports_bytes_never_classifications` | Inspect output reports bytes and access facts and never classifications: the vocabulary of interpretation — table formats, signatures, identity strength — is mechanically refused in both renderings, because classification is exactly what the register holds open |
| Section 12 | `missing_discovery_evidence_is_a_typed_refusal_not_an_omission` | The missing discovery evidence is an in-band typed refusal naming its increment — never an omission a reader could mistake for a clean bill of health |
| Section 12 | `the_bundle_command_states_agree_with_dispatch_behavior` | The bundle's command-surface states agree with dispatch behavior — a command is reported answering iff it exits 0 and refusing iff it exits with the refusal code — so the diagnostics cannot claim an unimplemented surface answers |
| Section 14 | `the_shipped_dependency_closure_is_empty` | No normal or build dependency exists, so no hash or plan implementation can arrive from outside the crate; std's own hashers are held off the output type by its compile-fail non-Hash proof, and past that the boundary is a named review obligation |
| Section 16 | `the_doctor_probes_only_compiled_absolute_paths` | The doctor probes only the roster's compiled absolute candidate paths, in order, and nothing else — there is no PATH search to influence, and an empty roster is a typed statement rather than a silent pass |
| Section 16 | `version_banners_parse_or_stay_unrecognized_never_guessed` | A version banner parses to major.minor or stays unrecognized with its raw line preserved — never guessed — and the recorded util-linux banner shape parses to the recorded family |

16 requirement(s), 29 annotation(s), 3 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
