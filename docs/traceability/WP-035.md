<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-035 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| CAP-004 | `an_absent_tool_reports_where_partman_looked` | An absent tool's report carries every candidate path checked in both renderings and can never render as found — a missing dependency drawn as a pass would be a plausible fake success, and a mutation doing exactly that survived the suite until these pins existed |
| CAP-004 | `the_doctor_reports_presence_version_and_range_as_facts` | The doctor reports presence, version, and tested-range membership as facts with provenance — the answering path and the sanitized banner — and a probe failure is a typed state, never a guessed version |
| CAP-004 | `the_real_launcher_preserves_a_nonzero_exit_as_failure` | The real bounded launcher distinguishes an unsuccessful process exit from a successful answer, preserving only a bounded, terminal-safe first line as provenance; an invalid structured Git option supplies a deterministic nonzero exit without adding a Tier-1 executable class |
| CAP-004 | `version_banners_parse_or_stay_unrecognized_never_guessed` | A version banner parses to major.minor or stays unrecognized with its raw line preserved — never guessed — and the recorded util-linux banner shape parses to the recorded family |
| CLI-002 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| CLI-002 | `the_bundle_command_states_agree_with_dispatch_behavior` | The bundle's command-surface states agree with dispatch behavior — a command is reported answering iff it exits 0 and refusing iff it exits with the refusal code — so the diagnostics cannot claim an unimplemented surface answers |
| CLI-005 | `a_non_unicode_argument_is_refused_not_a_panic` | A non-Unicode argument is a typed usage refusal at the documented code, not a panic: std::env::args() would abort with an undocumented 101, so the binary's entry seam owns the conversion |
| CLI-005 | `exit_codes_match_the_contract_the_help_text_documents` | The documented exit codes are pinned as literals — 0 ok, 2 usage refusal, 3 typed refusal — so renumbering a constant is a visible test edit, and the help text interpolates the same constants so a renumbering cannot desynchronize the two |
| CLI-008 | `every_json_emission_is_one_well_formed_schema_versioned_envelope` | Every JSON emission, including usage refusals, is one well-formed envelope carrying the schema version; domain payloads are absent rather than emitted unversioned |
| CLI-008 | `hostile_arguments_cannot_inject_terminal_controls` | Caller-controlled command, flag, second-command, replay-value, and non-Unicode refusal text cannot inject C0, DEL, or C1 controls into human output; when a refusal emits a caller token, JSON preserves that exact value only as escapes, while replay refusals deliberately do not echo paths |
| CLI-008 | `hostile_strings_cannot_break_the_envelope` | Quotes, backslashes, and control bytes in any rendered string are escaped, so no value can break the envelope's structure or smuggle a control byte into --json output |
| CLI-008 | `human_terminal_encoding_is_injective_for_controls_and_backslashes` | Human rendering is an injective visible encoding: controls cannot execute, and neither a literal escape spelling nor U+FFFD can alias a control input |
| CLI-008 | `no_output_in_any_mode_contains_an_ansi_sequence` | No output in any mode contains an ANSI escape byte, so NO_COLOR is honored by construction and --json output is ANSI-free |
| FS-007 | `facts_are_technology_properties_without_status_vocabulary` | Every shipped fact names a technology, an operation, a limit, and a checkable basis, and neither rendering contains CAP-003 status vocabulary — the facts are inputs to blocked reasons, and the blocked-reason surface is WP-050's |
| INV-006 | `discovery_cannot_auto_mount_or_run_repair_tools` | The current shipped source graph has no auto-mount or repair execution route: every production source outside doctor is pinned free of direct process-command construction, the module declaration set is closed, and the production doctor launcher accepts no caller arguments and invokes every compiled roster path with fixed informational --version; alternate spellings remain a named review obligation, bounded by denied unsafe code and the empty shipped dependency closure |
| INV-007 | `absence_and_unavailability_are_distinct_outcomes` | Absence is a value and renders in the observed family with what established it; a probe straddling the object's end never claims absence of bytes that exist; and a read failure renders as failed — never as unavailability and never as absence — with the three states pinned in both output modes |
| INV-007 | `apps/cli/src/lib.rs` | Deny-by-default is the diagnostics builder's type rather than a filter: a closed field enum is the only route into the bundle, every variant renders compile-time data, and no API accepts a caller-supplied key or value. What a type cannot prove — that no variant's renderer reads the environment — is refused by the source-guard test and the empty shipped closure, with the glob-import bypass refused by the workspace's pedantic clippy gate. |
| INV-007 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| INV-007 | `missing_discovery_evidence_is_a_typed_refusal_not_an_omission` | The missing discovery evidence is an in-band typed refusal naming its increment — never an omission a reader could mistake for a clean bill of health |
| INV-007 | `replay_over_a_deterministic_fixture_image_is_attributed_and_reproducible` | Every replay observation carries its attribution — adapter name, version, method — and replay over one of WP-020's deterministic images is byte-reproducible, so the record is raw discovery evidence a reader can re-derive, not narrative; neither rendering echoes the replayed object's name |
| MODEL-003 | `domain_requests_refuse_with_exact_typed_gates_and_no_payload` | Reserved inventory, topology, and capability requests are recognized and refuse on stdout at exit 3 with exact typed gates in both modes; their JSON envelopes contain no domain payload key, so absence never masquerades as an empty machine, snapshot, or verdict set |
| MODEL-003 | `every_json_emission_is_one_well_formed_schema_versioned_envelope` | Every JSON emission, including usage refusals, is one well-formed envelope carrying the schema version; domain payloads are absent rather than emitted unversioned |
| MODEL-003 | `version_reports_through_the_envelope` | The version surface reports the workspace package version through the same envelope discipline as every other surface |
| MODEL-004 | `replay_over_a_deterministic_fixture_image_is_attributed_and_reproducible` | Every replay observation carries its attribution — adapter name, version, method — and replay over one of WP-020's deterministic images is byte-reproducible, so the record is raw discovery evidence a reader can re-derive, not narrative; neither rendering echoes the replayed object's name |
| SAFE-004 | `output_capture_limits_are_per_stream_and_fail_closed` | Each child-output stream is independently bounded at 4096 bytes and the aggregate retained capture is therefore at most 8192 bytes. |
| SAFE-004 | `the_doctor_probes_only_compiled_absolute_paths` | The doctor probes only the roster's compiled absolute candidate paths, in order, and nothing else — there is no PATH search to influence, and an empty roster is a typed statement rather than a silent pass |
| SAFE-004 | `the_launcher_clears_the_child_environment` | The launcher's child environment is cleared and gains exactly one written variable, LC_ALL=C — pinned as a source-text assertion because no behavioral proof fits WP-035's test process set, with the pin's reach stated: direct spellings, with smuggling routes closed by the empty closure and the wildcard-import lint, and the residue held by review |
| SAFE-004 | `the_real_launcher_answers_bounded_with_provenance` | The real launcher captures a successful exit with bounded output before the deadline by probing Git at one reviewed absolute path; the adjacent doctor-module test supplies Git's intentional nonzero arm, and neither test launches a roster tool or shell |
| SAFE-004 | `the_real_launcher_preserves_a_nonzero_exit_as_failure` | The real bounded launcher distinguishes an unsuccessful process exit from a successful answer, preserving only a bounded, terminal-safe first line as provenance; an invalid structured Git option supplies a deterministic nonzero exit without adding a Tier-1 executable class |
| SAFE-005 | `a_non_unicode_argument_is_refused_not_a_panic` | A non-Unicode argument is a typed usage refusal at the documented code, not a panic: std::env::args() would abort with an undocumented 101, so the binary's entry seam owns the conversion |
| SAFE-005 | `absence_and_unavailability_are_distinct_outcomes` | Absence is a value and renders in the observed family with what established it; a probe straddling the object's end never claims absence of bytes that exist; and a read failure renders as failed — never as unavailability and never as absence — with the three states pinned in both output modes |
| SAFE-005 | `compiled_schedule_matches_the_preregistered_shape` | The compiled schedule is the preregistered one: negative controls first, alternating order-balanced trials, one closing healthy control. |
| SAFE-005 | `coverage_gate_names_entries_that_retained_no_property` | A successfully captured entry retaining no property is a no-entry state, reported so the decisive pair goes inconclusive rather than letting two empty projections compare equal and read as a negative. |
| SAFE-005 | `devlinks_canonicalization_sorts_tokens_and_preserves_content` | The one declared canonicalization sorts DEVLINKS tokens and preserves every token and every other line, so two captures differing only in udevadm's symlink-set rendering order compare stable, and a genuinely different token set still fails. |
| SAFE-005 | `domain_requests_refuse_with_exact_typed_gates_and_no_payload` | Reserved inventory, topology, and capability requests are recognized and refuse on stdout at exit 3 with exact typed gates in both modes; their JSON envelopes contain no domain payload key, so absence never masquerades as an empty machine, snapshot, or verdict set |
| SAFE-005 | `inspect_answers_with_typed_statements_not_a_fake_topology` | Bare inspect answers with a typed no-adapter statement naming the platform package and the standing gated list carrying its register issues — never a plausible empty machine, and never silence about what the inspector will not say |
| SAFE-005 | `inspect_reports_bytes_never_classifications` | Inspect observations report bytes and access facts, never classifications: a named list of interpretation words is mechanically refused in both renderings' observation sections — the list's reach is exactly those words, with the residue held by review — while the gated section legitimately names what it gates |
| SAFE-005 | `projection_differences_name_the_differing_keys` | Projection differences name retained keys from either side. |
| SAFE-005 | `raw_record_hex_round_trips_or_refuses` | Hex round-trips exactly and malformed hex refuses. |
| SAFE-005 | `replay_flag_misuse_is_refused_structurally` | The --replay flag's malformed shapes are structured usage refusals with exact spellings — a missing value, a following flag mistaken for a value, a second occurrence, and attachment to a command that is not inspect — because a token read two contradictory ways in one invocation is a guess, and this parser refuses rather than guesses |
| SAFE-005 | `replay_open_flags_match_the_supported_target_abi` | Replay opens are non-blocking on every supported Unix target using that target's reviewed ABI values; Linux itself has generic, MIPS, and SPARC families, and Darwin differs again, so a shared literal is mechanically rejected; a source-use guard keeps that constant wired into the actual open call |
| SAFE-005 | `replay_refuses_non_regular_objects_with_a_typed_value` | A replayed object that is not a regular file is refused with a typed value before any byte is read — the pre-open look refuses the common case before any open — an unopenable path is a typed refusal rather than a panic, neither refusal echoes the object's name, and the gated list travels with refusal answers too |
| SAFE-005 | `stability_projection_ignores_symlink_block_order_but_not_properties` | Stability compares the projection — the E: property sequence with the declared DEVLINKS canonicalization — so udevadm's S:-block set-order nondeterminism cannot fail it, while a genuinely changed property or reordered property sequence still does. |
| SAFE-005 | `the_handle_gate_refuses_a_directory_handle_on_windows` | The handle-level gate refuses a non-regular object on Windows too: a directory handle opened with backup semantics — a handle no path-based Tier-1 test can otherwise produce there — is refused by fstat through the handle, so the platform whose device names most concern the boundary has direct evidence of the gate |
| SAFE-005 | `unavailability_and_failure_are_distinct_rendered_states` | Unavailability and failure are distinct rendered states in both modes, per ADR-C4, and neither masquerades as a value or an absence; exercised at the rendering seam with constructed observations, because no portable Tier-1 setup forces a mid-file read error — stated so this claim is not read as behavioral I/O-failure coverage |
| SAFE-005 | `unknown_tokens_are_refused_with_their_exact_spelling` | A token the parser does not recognize is refused with its exact logical spelling rather than guessed at: JSON round-trips the exact scalar string and human output uses a lossless injective visible encoding; a second command word carries the second token, never a canonical name the user did not type |
| SAFE-006 | `apps/cli/src/lib.rs` | Deny-by-default is the diagnostics builder's type rather than a filter: a closed field enum is the only route into the bundle, every variant renders compile-time data, and no API accepts a caller-supplied key or value. What a type cannot prove — that no variant's renderer reads the environment — is refused by the source-guard test and the empty shipped closure, with the glob-import bypass refused by the workspace's pedantic clippy gate. |
| SAFE-006 | `coverage_gate_names_entries_that_retained_no_property` | A successfully captured entry retaining no property is a no-entry state, reported so the decisive pair goes inconclusive rather than letting two empty projections compare equal and read as a negative. |
| SAFE-006 | `devlinks_canonicalization_sorts_tokens_and_preserves_content` | The one declared canonicalization sorts DEVLINKS tokens and preserves every token and every other line, so two captures differing only in udevadm's symlink-set rendering order compare stable, and a genuinely different token set still fails. |
| SAFE-006 | `export_diagnostics_admits_exactly_the_allowlisted_fields` | The bundle's JSON key set equals the pinned allowlist exactly, with the expected keys as literals so widening the allowlist is a visible reviewed edit; that deny-by-default is the builder's type rather than a filter is a property of lib.rs recorded as structured evidence there, not something a key-set assertion can distinguish |
| SAFE-006 | `no_output_in_any_mode_carries_an_environment_value` | No output in any mode carries the host's username, home path, or computer name, nor any other environment value the host actually sets that is six bytes or longer and not byte-equal to a rendered compile-time constant — a tripwire whose reach stops at variables the test host sets; the source guard is what refuses an environment read regardless of host state |
| SAFE-006 | `normalizer_drops_exactly_the_declared_keys` | The frozen normalizer drops exactly the six predeclared plumbing keys and retains every other property in order. |
| SAFE-006 | `stability_projection_ignores_symlink_block_order_but_not_properties` | Stability compares the projection — the E: property sequence with the declared DEVLINKS canonicalization — so udevadm's S:-block set-order nondeterminism cannot fail it, while a genuinely changed property or reordered property sequence still does. |
| SAFE-006 | `the_human_bundle_is_pinned_byte_for_byte` | The human diagnostics rendering is pinned byte-for-byte against a literal template, so an extra human-only disclosure fails the tier exactly like a smuggled JSON key |
| SAFE-006 | `the_shipped_sources_read_no_environment_variable` | The shipped sources contain no environment read — env::var, env::vars, and var_os are absent from every shipped source file the test enumerates: lib.rs, main.rs, doctor.rs, facts.rs, and inspect.rs — so an environment value cannot reach output regardless of which variables the host sets; compile-time env::consts and env! are the allowed forms |
| SAFE-009 | `apps/cli/Cargo.toml`, `cargo xtask ci` | The CLI crate inherits the workspace lint policy, including denied unsafe code, and the Tier-1 gate compiles and lints this unprivileged boundary. |
| SEC-007 | `apps/cli/src/lib.rs`, `apps/cli/src/doctor.rs`, `apps/cli/src/inspect.rs` | The shipped binary opens no socket and reads no environment variable; offline is the construction, not a mode. Increments 3 and 4 grew the I/O reach and this statement with them: the doctor performs existence checks and version probes of roster tools at compiled absolute paths under the SAFE-004-derived controls implemented here — structured argument arrays, no PATH search, bounded output, a time limit, and a cleared child environment. Executable identity beyond the absolute path is not verified, and capability mapping remains WP-050's work. `inspect --replay` reads one caller-named object, refused through the opened handle unless it is a regular file, so a block device named by path is refused unread. Nothing else reads a file or launches a process. Most WP-035 dispatch tests inject a scripted launcher and replay only temporary files they create and remove themselves. The two real-launch tests exercise Git at reviewed absolute paths; the direct subprocess classes in WP-035 tests are Git and the compile-time-selected Cargo. |
| SEC-007 | `export_diagnostics_is_byte_identical_across_invocations` | The bundle is byte-identical across invocations within one environment — stability, not environment-independence, which the source guard and the empty shipped closure establish |
| SEC-007 | `output_capture_limits_are_per_stream_and_fail_closed` | Each child-output stream is independently bounded at 4096 bytes and the aggregate retained capture is therefore at most 8192 bytes. |
| SEC-007 | `the_shipped_sources_read_no_environment_variable` | The shipped sources contain no environment read — env::var, env::vars, and var_os are absent from every shipped source file the test enumerates: lib.rs, main.rs, doctor.rs, facts.rs, and inspect.rs — so an environment value cannot reach output regardless of which variables the host sets; compile-time env::consts and env! are the allowed forms |
| Section 12 | `an_absent_tool_reports_where_partman_looked` | An absent tool's report carries every candidate path checked in both renderings and can never render as found — a missing dependency drawn as a pass would be a plausible fake success, and a mutation doing exactly that survived the suite until these pins existed |
| Section 12 | `an_empty_roster_renders_as_a_typed_statement` | An empty roster renders as a typed not-implemented statement naming the platform's adapter package — never as a clean bill that would read as all dependencies satisfied |
| Section 12 | `domain_requests_refuse_with_exact_typed_gates_and_no_payload` | Reserved inventory, topology, and capability requests are recognized and refuse on stdout at exit 3 with exact typed gates in both modes; their JSON envelopes contain no domain payload key, so absence never masquerades as an empty machine, snapshot, or verdict set |
| Section 12 | `inspect_answers_with_typed_statements_not_a_fake_topology` | Bare inspect answers with a typed no-adapter statement naming the platform package and the standing gated list carrying its register issues — never a plausible empty machine, and never silence about what the inspector will not say |
| Section 12 | `inspect_reports_bytes_never_classifications` | Inspect observations report bytes and access facts, never classifications: a named list of interpretation words is mechanically refused in both renderings' observation sections — the list's reach is exactly those words, with the residue held by review — while the gated section legitimately names what it gates |
| Section 12 | `missing_discovery_evidence_is_a_typed_refusal_not_an_omission` | The missing discovery evidence is an in-band typed refusal naming its increment — never an omission a reader could mistake for a clean bill of health |
| Section 12 | `the_bundle_command_states_agree_with_dispatch_behavior` | The bundle's command-surface states agree with dispatch behavior — a command is reported answering iff it exits 0 and refusing iff it exits with the refusal code — so the diagnostics cannot claim an unimplemented surface answers |
| Section 14 | `the_output_type_does_not_implement_hash` | Outcome does not implement Hash: this regular unit-test target carries two otherwise-disjoint trait candidates, so adding Hash makes the marker selection ambiguous and fails Tier-1 compilation |
| Section 14 | `the_shipped_dependency_closure_is_empty` | No normal or build dependency exists, so no hash or plan implementation can arrive from outside the crate; std's own hashers are held off the output type by a Tier-1 compile-time ambiguity proof, and past that the boundary is a named review obligation |
| Section 16 | `the_doctor_probes_only_compiled_absolute_paths` | The doctor probes only the roster's compiled absolute candidate paths, in order, and nothing else — there is no PATH search to influence, and an empty roster is a typed statement rather than a silent pass |
| Section 16 | `version_banners_parse_or_stay_unrecognized_never_guessed` | A version banner parses to major.minor or stays unrecognized with its raw line preserved — never guessed — and the recorded util-linux banner shape parses to the recorded family |

17 requirement(s), 46 annotation(s), 3 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
