<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-000 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| SAFE-005 | `unavailable_destructive_tiers_fail_closed` | Tier 2 and Tier 3 refuse by default; an unavailable tier fails closed rather than running |
| SAFE-007 | `a_destructive_profile_is_an_argument_not_an_environment_variable` | the profile is a command-line argument, so it cannot be inherited by accident from a parent shell |
| SAFE-007 | `a_destructive_tier_refuses_even_with_the_profile_word` | no destructive suite exists, so the runner refuses rather than reporting a pass over an empty run |
| SAFE-007 | `tier_parser_accepts_explicit_tier_one`, `tier_parser_rejects_missing_proof_by_omission`, `tier_parser_rejects_out_of_range_and_malformed_tiers` | Test tiers are always explicit, and malformed or omitted tier selection cannot fall through to a runnable destructive suite. |
| SAFE-007 | `unavailable_destructive_tiers_fail_closed` | Tier 2 and Tier 3 refuse by default; an unavailable tier fails closed rather than running |
| SAFE-009 | `every_workspace_member_inherits_the_lint_policy` | `unsafe_code = "deny"` reaches a crate only if it opts into the workspace lints, so a member omitting the stanza is refused rather than silently exempt |
| SEC-005 | `LICENSE-MIT`, `LICENSE-APACHE`, `Cargo.toml`, `deny.toml` | The elected project licence has committed texts and workspace policy, so the release inventory has a subject and first-party crates are not exempt. |
| SEC-005 | `a_nested_json_licence_property_does_not_satisfy_the_gate` | a `license` key nested anywhere but the document root does not satisfy the inventory |
| SEC-005 | `cargo xtask verify-licenses`, `a_manifest_that_loses_its_licence_key_is_a_violation` | Every Cargo and npm manifest must keep the elected licence declaration; removing one is a measured gate failure. |
| SEC-005 | `every_repository_manifest_declares_the_project_licence` | every Cargo and npm manifest declares the project licence, checked semantically so the release inventory has a subject |
| SEC-010 | `.cargo/config.toml`, `the_xtask_alias_enforces_the_lockfile_at_the_gate_boundary` | The xtask alias uses --locked at the boundary that builds the gate, so a missing dependency cannot be silently restored before policy runs. |
| SEC-010 | `.github/workflows/ci.yml` | The committed workflow runs the required foundation gates on Windows, macOS, and Linux and pins executable action dependencies immutably. |
| SEC-010 | `a_dockerfile_action_is_followed_to_its_base_images` | a Docker action is followed into its Dockerfile, where every image the build pulls must be digest-pinned |
| SEC-010 | `a_local_reference_must_actually_be_runnable_and_inside_the_repository`, `recursion_through_local_references_terminates` | A local-action exemption must resolve to runnable metadata inside the repository, and recursive references cannot evade or hang discovery. |
| SEC-010 | `a_release_comment_cannot_be_borrowed_from_another_step`, `a_pinned_digest_still_needs_a_readable_release_tag` | Every action occurrence binds its own readable release comment to its digest; another step cannot lend it one. |
| SEC-010 | `container_images_are_executable_dependencies_too` | job containers, the container shorthand, service containers and a Docker action image are all pinned, not only `uses:` references |
| SEC-010 | `deny.toml`, `cargo xtask supply-chain` | Advisory, licence, source, and duplicate-version policy is executable through the repository's pinned supply-chain command. |
| SEC-010 | `digest_pins_are_accepted_and_mutable_references_are_not` | a full commit SHA is accepted and a tag or branch reference is refused, which is what "pinned by digest" has to mean |
| SEC-010 | `discovery_does_not_depend_on_how_the_key_is_spelled`, `a_workflow_that_cannot_be_parsed_is_a_violation_not_a_skip` | Workflow dependency discovery is structural across YAML spelling variants, and malformed workflow input fails closed. |
| SEC-010 | `repository_workflows_are_pinned_by_digest`, `action_pin_check_fails_closed_on_a_missing_workflow_directory`, `action_pin_check_fails_closed_on_an_empty_workflow_directory` | The action gate checks the real workflows and refuses missing or empty discovery roots instead of passing over no input. |
| SEC-010 | `rust-toolchain.toml`, `Cargo.lock`, `cargo xtask verify-toolchain` | The compiler, lockfile, and pre-gate toolchain check pin the Rust build inputs rather than resolving them silently during a gate. |
| SEC-010 | `the_npm_advisory_check_finds_every_package_not_one_named_directory` | npm advisory coverage follows discovery rather than a hard-coded path, so a new package cannot be born unaudited |
| SEC-010 | `the_repository_workflows_pass_the_real_gate` | this repository's own workflows pass the digest-pinning gate, so the check is exercised against real input rather than only against fixtures |
| SEC-010 | `the_three_bypasses_the_text_scanners_missed_are_refused` | action discovery is a structural YAML parse: the quoted-key, anchored-key and YAML-escape bypasses that defeated three text scanners are permanent regressions |
| SEC-010 | `tools/xtask/src/main.rs`, `.github/dependabot.yml`, `cargo xtask fuzz --seconds 1`, `cargo xtask supply-chain` | The excluded fuzz dependency graph is locked, audited, update-monitored, and preflighted before the nightly fuzz command resolves it. |
| Section 1.10 | `.github/CODEOWNERS` | GitHub requests review from the initial owner in addition to the local file-granular work-package enforcement. |
| Section 1.10 | `a_generated_lockfile_is_regenerated_by_whoever_changes_a_manifest`, `declaring_a_path_generated_is_not_claiming_it`, `a_derived_path_needs_a_derivation_this_tool_can_check` | A generated lockfile may travel only with a manifest it resolves; declaring an unknown derivation or calling a path generated does not grant ownership. |
| Section 1.10 | `a_rename_is_judged_at_both_ends`, `a_path_is_a_byte_string_not_a_trimmed_quoted_one` | Change ownership judges both sides of renames and uses NUL-delimited raw paths, preventing rename and path-normalization bypasses. |
| Section 1.10 | `a_reservation_that_has_started_matching_files_is_coverage` | Once a reserved path exists, inventory and change-ownership gates agree that the reserving package owns it. |
| Section 1.10 | `cargo xtask verify-change-ownership --base origin/main`, `a_change_must_declare_which_work_package_it_belongs_to`, `a_change_cannot_reach_outside_its_assignment_or_widen_it_to_fit`, `a_governance_change_may_move_assignments_but_carry_nothing_else` | A change is judged against its base-revision assignment, so self-widening and mixed governance/code changes are refused. |
| Section 1.10 | `cargo xtask verify-ownership`, `every_tracked_path_belongs_to_a_work_package`, `ownership_claim_patterns_are_understood_or_refused`, `claim_matching_is_prefix_exact_not_substring` | Every tracked path belongs to an understood assignment, and stale, ambiguous, or substring-only ownership claims fail closed. |
| Section 1.10 | `every_commit_declares_for_itself_and_git_decides_what_a_trailer_is`, `a_merge_commit_is_exempt_because_this_repository_cannot_trailer_one` | Git's trailer parser checks every non-merge commit independently, while merge commits are explicitly exempt for the documented GitHub workflow. |
| Section 1.10 | `the_requirement_vocabulary_comes_from_the_specification_not_from_this_tool` | the traceability vocabulary comes from real ID definitions, numbered headings, and actual numbered operating-contract items rather than from strings this tool invents |
| Section 11.7 | `a_platform_gated_test_is_not_reported_as_missing_evidence` | A platform-gated test may be absent from another operating system's binary without making identical source pass on one platform and fail on another. |
| Section 11.7 | `an_annotation_with_no_function_under_it_binds_to_nothing_and_is_refused`, `an_annotation_that_has_slid_onto_another_test_is_refused` | An annotation adrift from its test or silently attached to a neighbour is refused instead of crediting absent evidence. |
| Section 11.7 | `cargo xtask traceability` | CI validates generated traceability against specification-owned references and independently checked evidence sources. |
| Section 11.7 | `structured_evidence_blocks_are_structural_and_typed` | structured evidence is parsed from an exact fenced block, and malformed, duplicate, or unclosed sources are refused rather than skipped |
| Section 11.7 | `structured_evidence_is_validated_against_independent_sources` | a structured row must name known requirements and live, package-owned evidence; invalid commands and stale references each fail closed |
| Section 11.7 | `the_requirement_vocabulary_comes_from_the_specification_not_from_this_tool` | the traceability vocabulary comes from real ID definitions, numbered headings, and actual numbered operating-contract items rather than from strings this tool invents |
| Section 12 | `an_annotation_cannot_invent_a_requirement`, `an_annotation_without_a_claim_is_refused`, `finding_no_annotations_is_a_failure_not_a_clean_run`, `the_happy_path_reports_the_requirements_it_actually_covered` | The generator is proved capable of rejecting unknown requirements, missing claims, and empty discovery while retaining a passing control. |
| Section 12 | `docs/adr/0000-template.md` | Architectural deviations have a repository-owned ADR form carrying decision, alternatives, consequences, and evidence. |
| Section 12 | `structured_evidence_blocks_are_structural_and_typed` | structured evidence is parsed from an exact fenced block, and malformed, duplicate, or unclosed sources are refused rather than skipped |
| Section 12 | `structured_evidence_is_validated_against_independent_sources` | a structured row must name known requirements and live, package-owned evidence; invalid commands and stale references each fail closed |
| Section 13 | `.gitattributes`, `cargo xtask fmt-check` | LF normalization keeps the formatting gate reproducible on Windows, macOS, and Linux checkouts. |
| Section 13 | `.github/workflows/ci.yml` | The committed workflow runs the required foundation gates on Windows, macOS, and Linux and pins executable action dependencies immutably. |
| Section 13 | `parser_maps_every_documented_task`, `parser_rejects_unknown_tasks_and_stray_arguments` | Every documented repository task is reachable through the single xtask entry point and unknown commands or arguments are refused. |

9 requirement(s), 15 annotation(s), 26 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
