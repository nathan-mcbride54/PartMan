# WP-000 traceability

- Spec version: 4.0.0
- Work package: WP-000
- Requirements affected: SEC-005, SEC-010, Section 1.10
- Safety constraints preserved: SAFE-001, SAFE-002, SAFE-007, SAFE-009

| Evidence | Requirement or contract |
| --- | --- |
| `rust-toolchain.toml`, committed `Cargo.lock` | SEC-010 pinned toolchain and lockfile |
| `verify_toolchain` in `tools/xtask/src/main.rs` | SEC-010 pinned toolchain verified before every gate |
| `deny.toml`, `cargo xtask supply-chain` | SEC-010 advisory and license gates |
| `.github/workflows/ci.yml` | Three-OS CI and immutable action pin |
| `digest_pins_are_accepted_and_mutable_references_are_not`, `repository_workflows_are_pinned_by_digest`, `the_repository_workflows_pass_the_real_gate`, `action_pin_check_fails_closed_on_a_missing_workflow_directory`, `action_pin_check_fails_closed_on_an_empty_workflow_directory` | SEC-010 action digest pinning enforced, not merely documented |
| `.gitattributes` | LF normalization; without it `cargo fmt --check` fails on any Windows checkout |
| `parser_maps_every_documented_task`, `parser_rejects_unknown_tasks_and_stray_arguments` | Single runner: every documented task is reachable and nothing else is |
| `tier_parser_accepts_explicit_tier_one`, `tier_parser_rejects_missing_proof_by_omission`, `tier_parser_rejects_out_of_range_and_malformed_tiers`, `unavailable_destructive_tiers_fail_closed` | SAFE-007 fail-closed unavailable tiers; no tier runs by default |
| `docs/adr/0000-template.md` | WP-000 ADR template |
| `.github/CODEOWNERS` | WP-000 path-ownership review requirement |
| `LICENSE-MIT`, `LICENSE-APACHE`, `license` keys in every manifest, `[licenses] private = { ignore = false }` | SEC-005 license inventory has a subject; the workspace's own crates are gated, not exempted (ADR-0006) |
| `--locked` in the `xtask` alias, `the_xtask_alias_enforces_the_lockfile_at_the_gate_boundary` | SEC-010 committed lockfile enforced at the boundary that builds the gate, not only inside it (2026-07-29 audit: a deleted lock entry was silently regenerated and 160 tests passed against it) |
| `the_three_bypasses_the_text_scanners_missed_are_refused`, `discovery_does_not_depend_on_how_the_key_is_spelled`, `a_workflow_that_cannot_be_parsed_is_a_violation_not_a_skip` | SEC-010 discovery is a structural YAML parse; the quoted-key, anchored-key, YAML-escape, container-tag and out-of-tree local-action bypasses are permanent regressions. **Three earlier rows citing the text-scanner tests were removed here: those tests no longer exist, and traceability naming absent evidence is worse than naming none** |
| `container_images_are_executable_dependencies_too`, `a_dockerfile_action_is_followed_to_its_base_images` | SEC-010 covers containers, not only actions: job containers, the `container:` shorthand, service containers, a Docker action's `runs.image`, and the `FROM` bases of a Dockerfile it builds |
| `a_release_comment_cannot_be_borrowed_from_another_step`, `a_pinned_digest_still_needs_a_readable_release_tag` | The release-tag comment is bound to every occurrence, so two steps sharing a SHA cannot pass on one comment |
| `a_local_reference_must_actually_be_runnable_and_inside_the_repository`, `recursion_through_local_references_terminates` | A `./` exemption is only sound if the target exists, carries action metadata, resolves inside the repository — including its metadata file, not just its directory — and cannot loop |
| Committed `fuzz/Cargo.lock`, `--locked` preflight in `cargo xtask fuzz`, fuzz graph in `cargo xtask supply-chain`, `/fuzz` Dependabot entry | SEC-010 committed lockfiles and policy gates cover the out-of-workspace fuzz graph |
| `cargo xtask verify-licenses`, `a_manifest_that_loses_its_licence_key_is_a_violation`, `a_nested_json_licence_property_does_not_satisfy_the_gate` | SEC-005: every manifest declares the project licence, checked semantically — Cargo through `cargo metadata`, npm through JSON parsing at the document root |
| `cargo xtask verify-ownership`, `every_tracked_path_belongs_to_a_work_package`, `ownership_claim_patterns_are_understood_or_refused`, `claim_matching_is_prefix_exact_not_substring` | Section 1.10 inventory: every tracked path belongs to a declared assignment. Fails closed on an unclaimed file, a stale claim, and an uninterpretable pattern |
| `cargo xtask verify-change-ownership`, `a_change_must_declare_which_work_package_it_belongs_to`, `a_change_cannot_reach_outside_its_assignment_or_widen_it_to_fit`, `a_governance_change_may_move_assignments_but_carry_nothing_else` | Section 1.10 enforcement: a change belongs to the package its commits declare, judged against the **base** revision so self-widening fails. Reproduces the PR #47 shape and refuses it. Governance changes may touch only assignment documents |

## Verification

The local evidence command is `cargo xtask ci`. The supply-chain command is
`cargo xtask supply-chain` after installing the pinned tools. Neither command
contains privileged or destructive storage behavior. No WP-000 command
enumerates, opens, or writes a block device. Its filesystem reads are all of
repository-controlled text: workflow and composite-action YAML for the
action-pin check, `Cargo.toml`/`package.json` and `cargo metadata` output for
the licence check, the `owned-paths` blocks and `git ls-files` for the ownership
check, both lockfiles, and `.cargo/config.toml`. An earlier version of this
document said `.github/workflows/` was the only read, which stopped being true
as each gate was added.

## Known gaps carried into later work packages

- Section 11.7 requires `docs/traceability/` to be generated by CI. Every file
  under it is hand-maintained instead, and **generation is not yet assigned to
  any work package**. It needs requirement-ID annotations in code and tests,
  which no package currently delivers; Section 14 places the closest fit at
  WP-DOC100 (M4–M5), which is far later than Section 11.7 implies. This is an
  open scheduling gap, not a solved problem waiting its turn.
- Section 1.10 states that CI enforces path ownership via CODEOWNERS. GitHub
  CODEOWNERS requires review from an owner; it does not reject a diff that
  touches paths outside the work package's assignment. **Half closed
  2026-07-29:** the machine-readable data now exists — an `owned-paths` block in
  every `docs/work-packages/WP-*.md`, which is the same text a reviewer reads,
  so prose and data cannot drift — and `cargo xtask verify-ownership` runs
  inside `cargo xtask ci`, refusing a tracked file no package claims and a claim
  matching no file. All tracked files are claimed; `cargo xtask verify-ownership` prints the current count rather than this document restating a number that goes stale the next time a file is added.

  **The other half closed 2026-07-30.** `cargo xtask verify-change-ownership
  --base <rev>` requires every commit to carry a `Work-Package:` trailer and
  refuses any changed path outside that assignment. Two design choices carry the
  weight:

  - The declaration is a **commit trailer**, not a branch name or a label. This
    repository's branch names are inconsistent (`work/audit-round-four` sits
    beside `work/wp-020-held-root`), so keying on them would have been a guess
    dressed as a rule; trailers are already used here, need no API call, and stay
    in the log permanently.
  - The catalogue is read from the **base revision**, so widening your own
    `owned-paths` block in the same change buys nothing. That was the specific
    hole the audit named in the inventory-only check.

  A `Governance: <reason>` trailer permits editing the assignments themselves and
  then **only** `docs/work-packages/WP-*.md` may change, so the trailer cannot
  carry code past the check it sits beside.

  Still a review obligation, and not automatable: sub-file grants. WP-030 owns
  "its own status rows" in `README.md`; a path checker sees the whole file.

  **A worked example of that gap, recorded rather than hidden.** PR #47 was a
  nominal WP-000 change (the action scanner) that also edited
  `docs/work-packages/WP-020.md` and the WP-010, WP-020 and WP-030 traceability
  files, all owned by those packages. The inventory check passed, because every
  path was claimed by *someone*. The second follow-up audit caught it by reading.

  Those edits were audit-driven corrections to false claims, which is a
  legitimate need and a category the ownership model has no route for: a
  correction to WP-020's record is WP-020's territory, but leaving a known-false
  safety claim standing until that package is next active is worse. **The fix is
  not to widen WP-000's claims** — that is precisely the "widen your own block
  and pass against the widened tree" move issue #39 warns about. It is to give
  cross-package corrections an explicit governance route, which issue #39 now
  carries.
- Coverage floors are set at M1 per Section 11.8 and are therefore not
  configured here.
- ~~The project carries no license.~~ **Closed 2026-07-29.** ADR-0006 adopts
  `MIT OR Apache-2.0`; `[licenses.private]` is now `ignore = false`, so
  `cargo deny check licenses` checks the workspace's own crates against the same
  allow-list as every dependency. SEC-005's inventory is satisfiable.

  ~~Two declarations remain outside that gate.~~ **Closed 2026-07-29.**
  `cargo xtask verify-licenses` walks every `Cargo.toml` and `package.json` and
  fails unless each declares `MIT OR Apache-2.0`; it runs inside
  `cargo xtask ci`, and its own mutation test removes a key and watches it
  refuse.
- SEC-010 requires builder images pinned by digest. GitHub-hosted runner labels
  cannot satisfy this, and the deviation is now **documented with its residual
  risk** in `docs/quality/dependency-policy.md` rather than implied to be
  compliance. It re-opens as a hard requirement when release builds exist
  (ADR-S1, Section 19).
- The release-tag comment on action pins is format-checked, but nothing
  verifies the tag resolves to the pinned SHA; recorded in
  `docs/quality/dependency-policy.md` as a review obligation on every bump.
