<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-010 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| INV-003 | `every_classification_row_holds_against_its_fixture` | Every row of the accepted SI-35 classification table holds against its named catalogue fixture: healthy GPTs at both sector sizes are Present, one-valid-authority shapes are Present with their condition, the unreadable shape is Indeterminate, hybrid is Present with its condition, standalone MBR and APM are Present, and every signature-only medium is Absent |
| INV-003 | `the_decisive_fixture_classifies_indeterminate_never_present` | The decisive SI-35 fixture — two independently valid GPTs describing different partitions — classifies as ADR-C3 Indeterminate on the ambiguous arm, never Present and never Absent: the refusal demonstration's classification half, on the exact fixture the register's evidence clause names |
| MODEL-001 | `golden_vectors_encode_exactly` | Golden vectors pin exact canonical bytes, including unsigned values beyond JavaScript's safe integer range |
| MODEL-001 | `schemas/canonical-encoding-vectors.json`, `crates/domain/tests/shared_vectors.rs`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | Rust and TypeScript consume one shared fixture that pins exact bytes and hashes, schema-domain separation, and integer boundaries through u64. |
| MODEL-003 | `schema_fields_separate_domains` | Schema identifiers and versions participate in canonical content so otherwise identical artifact kinds do not collide |
| MODEL-003 | `schemas/canonical-encoding-vectors.json`, `crates/domain/tests/shared_vectors.rs`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | Rust and TypeScript consume one shared fixture that pins exact bytes and hashes, schema-domain separation, and integer boundaries through u64. |
| MODEL-005 | `a_javascript_number_encoded_as_float_is_rejected` | JavaScript's float encoding of an unsafe Number is outside the integer-only canonical profile and is refused |
| MODEL-005 | `appended_bytes_preserve_canonicality` | Appended bytes cannot smuggle trailing content past exact canonical decoding |
| MODEL-005 | `byte_substitutions_preserve_canonicality` | Boundary byte substitutions preserve the decode-reencode identity or are refused |
| MODEL-005 | `docs/work-packages/WP-010.md`, `crates/domain/tests/canonical_sets.rs`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | One normative rule and one deliberately unsorted fixture hold both implementations to bytewise full-element ordering, strict uniqueness, unchanged semantic arrays, and inherited depth in both the producer and validator. |
| MODEL-005 | `duplicate_and_misordered_map_keys_are_rejected` | Duplicate and misordered map keys are rejected rather than repaired or overwritten |
| MODEL-005 | `encoding_is_independent_of_insertion_order` | Canonical map bytes do not depend on insertion or container iteration order |
| MODEL-005 | `every_shared_vector_encodes_to_exactly_the_recorded_bytes` | Rust consumes the repository's shared cross-language fixture and reproduces every exact canonical byte string |
| MODEL-005 | `every_shared_vector_hashes_to_exactly_the_recorded_digest` | Rust reproduces every SHA-256 digest recorded in the fixture TypeScript also consumes |
| MODEL-005 | `every_single_bit_flip_preserves_canonicality` | Every single-bit mutation either remains a canonical value or is rejected, never accepted under a different re-encoding |
| MODEL-005 | `every_truncation_preserves_canonicality` | Every truncation either remains a canonical value or fails closed |
| MODEL-005 | `golden_vectors_encode_exactly` | Golden vectors pin exact canonical bytes, including unsigned values beyond JavaScript's safe integer range |
| MODEL-005 | `golden_vectors_round_trip` | Every committed golden value round-trips through the strict decoder without changing its canonical bytes |
| MODEL-005 | `hashing_bytes_requires_proving_they_are_canonical` | Generic raw-byte hashing proves pce/1 canonicality while leaving artifact-schema validation to the future typed boundary |
| MODEL-005 | `hashing_is_sha256_over_canonical_bytes` | Artifact hashing is exactly SHA-256 over the canonical bytes, with no unrecorded framing |
| MODEL-005 | `ill_formed_utf8_is_rejected` | Ill-formed UTF-8 is rejected so distinct hostile inputs cannot be repaired into one hashed value |
| MODEL-005 | `key_ordering_is_length_first_not_bytewise` | Text map keys use the pce/1 length-first ordering rather than a host language's default comparison |
| MODEL-005 | `nesting_beyond_the_depth_limit_is_rejected_not_crashed` | Decoder nesting is bounded with an explicit refusal instead of recursive stack exhaustion |
| MODEL-005 | `non_canonical_and_excluded_input_is_rejected` | Non-shortest, excluded, truncated, and trailing encodings fail closed instead of being normalized into an authorized value |
| MODEL-005 | `packages/canonical/src/canonical.ts`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | The TypeScript boundary refuses number-for-bigint and ill-formed surrogate substitutions, preserves non-normalized text, rejects byte-equal repaired keys, and snapshots generic arrays, schema sets, and hash input without letting caller-controlled species or prototype mutation substitute, drop, reorder, widen, or split the bytes it validates and emits. |
| MODEL-005 | `schema_fields_separate_domains` | Schema identifiers and versions participate in canonical content so otherwise identical artifact kinds do not collide |
| MODEL-005 | `schemas/canonical-encoding-vectors.json`, `crates/domain/tests/shared_vectors.rs`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | Rust and TypeScript consume one shared fixture that pins exact bytes and hashes, schema-domain separation, and integer boundaries through u64. |
| MODEL-005 | `schemas/canonical-encoding.md` | The normative pce/1 profile defines one canonical byte encoding and remains unchanged by the schema-level set rule. |
| MODEL-005 | `seeds_are_canonical` | Every seed is a canonical fixed point before mutations are credited as fail-closed evidence |
| MODEL-005 | `semantic_arrays_keep_order_and_set_duplicates_are_not_removed` | Ordinary arrays preserve semantic order and remain generically hashable, while schema-declared sets sort and reject duplicates at their typed boundary |
| MODEL-005 | `set_element_encoding_inherits_the_enclosing_depth_budget` | Both set producers and validators consume the enclosing artifact's remaining depth budget; a standalone-valid element is refused exactly when the combined depth exceeds the shared limit |
| MODEL-005 | `shared_set_vectors_exercise_sorting_and_match_recorded_bytes_and_hashes` | Both languages actively sort one shared set fixture and reproduce the exact same canonical bytes and SHA-256 digests |
| MODEL-005 | `short_exhaustive_inputs_preserve_canonicality` | Exhaustive one- and two-byte inputs satisfy the strict canonicality property |
| MODEL-005 | `the_checksum_is_copy_invariant` | The Present checksum is computed over copy-invariant content, so the two agreeing GPT copies produce one value from either copy — the recipe stays stable under re-probe of unchanged bytes and never hashes the per-copy header fields that differ by design |
| MODEL-005 | `the_encoder_enforces_the_same_depth_limit_as_the_decoder` | The encoder enforces the decoder's same depth ceiling and never emits an artifact no conforming decoder can revalidate |
| MODEL-006 | `docs/work-packages/WP-010.md`, `crates/domain/tests/canonical_sets.rs`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | One normative rule and one deliberately unsorted fixture hold both implementations to bytewise full-element ordering, strict uniqueness, unchanged semantic arrays, and inherited depth in both the producer and validator. |
| MODEL-006 | `packages/canonical/src/canonical.ts`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | The TypeScript boundary refuses number-for-bigint and ill-formed surrogate substitutions, preserves non-normalized text, rejects byte-equal repaired keys, and snapshots generic arrays, schema sets, and hash input without letting caller-controlled species or prototype mutation substitute, drop, reorder, widen, or split the bytes it validates and emits. |
| MODEL-006 | `schema_set_errors_are_distinct_and_depth_is_not_optional` | The schema layer has its own fail-closed error boundary and treats the set array's actual depth as mandatory data |
| MODEL-006 | `semantic_arrays_keep_order_and_set_duplicates_are_not_removed` | Ordinary arrays preserve semantic order and remain generically hashable, while schema-declared sets sort and reject duplicates at their typed boundary |
| MODEL-006 | `set_element_encoding_inherits_the_enclosing_depth_budget` | Both set producers and validators consume the enclosing artifact's remaining depth budget; a standalone-valid element is refused exactly when the combined depth exceeds the shared limit |
| MODEL-006 | `shared_set_validation_vectors_enforce_strict_order_without_repair` | Schema-set validation accepts only strict byte order and fails closed on descending or duplicate elements without repairing them |
| MODEL-006 | `shared_set_vectors_exercise_sorting_and_match_recorded_bytes_and_hashes` | Both languages actively sort one shared set fixture and reproduce the exact same canonical bytes and SHA-256 digests |
| MODEL-006 | `the_extent_vector_distinguishes_bytewise_from_length_first_order` | The committed extent case makes bytewise and length-first comparators disagree, so choosing the wrong convention fails a test |
| SAFE-005 | `an_unverifiable_entry_array_invalidates_the_copy` | A GPT header that verifies its own CRC but demands an entry array the window cannot contain — or more entries than the declared bound — is an invalid copy, never a trusted one: the parser walks nothing on a header's say-so |
| SAFE-005 | `appended_bytes_preserve_canonicality` | Appended bytes cannot smuggle trailing content past exact canonical decoding |
| SAFE-005 | `byte_substitutions_preserve_canonicality` | Boundary byte substitutions preserve the decode-reencode identity or are refused |
| SAFE-005 | `caller_contract_violations_refuse_with_typed_values` | The parser refuses the caller's contract violations with typed refusals — unsupported sector sizes, non-sector-multiple windows, oversize windows, windows too small to hold the structures — while hostile media never refuse, they classify |
| SAFE-005 | `declared_length_beyond_input_is_rejected_before_allocating` | A hostile declared length is bounded by available input before it can drive an allocation |
| SAFE-005 | `duplicate_and_misordered_map_keys_are_rejected` | Duplicate and misordered map keys are rejected rather than repaired or overwritten |
| SAFE-005 | `every_single_bit_flip_preserves_canonicality` | Every single-bit mutation either remains a canonical value or is rejected, never accepted under a different re-encoding |
| SAFE-005 | `every_truncation_preserves_canonicality` | Every truncation either remains a canonical value or fails closed |
| SAFE-005 | `ill_formed_utf8_is_rejected` | Ill-formed UTF-8 is rejected so distinct hostile inputs cannot be repaired into one hashed value |
| SAFE-005 | `nesting_beyond_the_depth_limit_is_rejected_not_crashed` | Decoder nesting is bounded with an explicit refusal instead of recursive stack exhaustion |
| SAFE-005 | `non_canonical_and_excluded_input_is_rejected` | Non-shortest, excluded, truncated, and trailing encodings fail closed instead of being normalized into an authorized value |
| SAFE-005 | `packages/canonical/src/canonical.ts`, `packages/canonical/src/canonical.test.ts`, `cargo xtask cross-language` | The TypeScript boundary refuses number-for-bigint and ill-formed surrogate substitutions, preserves non-normalized text, rejects byte-equal repaired keys, and snapshots generic arrays, schema sets, and hash input without letting caller-controlled species or prototype mutation substitute, drop, reorder, widen, or split the bytes it validates and emits. |
| SAFE-005 | `schema_set_errors_are_distinct_and_depth_is_not_optional` | The schema layer has its own fail-closed error boundary and treats the set array's actual depth as mandatory data |
| SAFE-005 | `set_element_encoding_inherits_the_enclosing_depth_budget` | Both set producers and validators consume the enclosing artifact's remaining depth budget; a standalone-valid element is refused exactly when the combined depth exceeds the shared limit |
| SAFE-005 | `shared_set_validation_vectors_enforce_strict_order_without_repair` | Schema-set validation accepts only strict byte order and fails closed on descending or duplicate elements without repairing them |
| SAFE-005 | `short_exhaustive_inputs_preserve_canonicality` | Exhaustive one- and two-byte inputs satisfy the strict canonicality property |
| SAFE-005 | `the_decisive_fixture_classifies_indeterminate_never_present` | The decisive SI-35 fixture — two independently valid GPTs describing different partitions — classifies as ADR-C3 Indeterminate on the ambiguous arm, never Present and never Absent: the refusal demonstration's classification half, on the exact fixture the register's evidence clause names |
| SAFE-005 | `the_encoder_enforces_the_same_depth_limit_as_the_decoder` | The encoder enforces the decoder's same depth ceiling and never emits an artifact no conforming decoder can revalidate |
| SAFE-005 | `the_state_type_offers_no_proceed_enabling_reading` | The classification type carries no proceed-enabling reading: no Default, no is_-style predicate, no ordering — a consumer must match all three ADR-C3 arms explicitly, and what a writer may do with each stays the specification's to say |
| SAFE-005 | `wrong_geometry_answers_indeterminate_not_absent` | Probing a 4Kn medium under a 512-byte contract answers Indeterminate-unreadable rather than fabricating a table or reporting blank: the parser's answers are relative to the stated geometry, reproducing the measured libblkid PMBR trap honestly instead of guessing across sector sizes |
| SAFE-009 | `crates/domain/Cargo.toml`, `cargo xtask ci` | The domain crate inherits the workspace lint policy, including denied unsafe code, and the Tier-1 gate compiles and lints that boundary. |
| SEC-010 | `packages/canonical/package.json`, `packages/canonical/package-lock.json`, `cargo xtask cross-language`, `cargo xtask supply-chain` | The TypeScript authorization path has no runtime dependencies, its development graph is locked, and repository gates install and audit the committed graph rather than resolving an unrecorded one. |
| Section 11.4 | `fuzz/fuzz_targets/decode_is_canonical.rs`, `fuzz/fuzz_targets/roundtrip_value.rs`, `docs/quality/fuzzing.md`, `cargo xtask fuzz` | The two pinned fuzz targets cover strict pce decoding, value round trips, and schema-set producer/validator fixed points without adding binary corpus artifacts to the repository. |
| Section 11.4 | `the_crc_agrees_with_the_independent_spellings` | The in-crate CRC-32 agrees with the fixtures crate's two independent spellings on the published IEEE check value and on real fixture bytes, so three implementations by three methods anchor the same function |
| Section 11.7 | `docs/work-packages/WP-010.md`, `cargo xtask traceability` | WP-010 traceability is generated from validated sources, with an explicit zero-loss ledger for every claim and narrative section in the former hand-maintained document. |

10 requirement(s), 36 annotation(s), 8 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
