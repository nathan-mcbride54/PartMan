<!-- Generated by `cargo xtask traceability`. Edit evidence sources, not this file. -->

# WP-040 traceability

Every row below is generated from either a requirement annotation beside a test or a
typed evidence declaration in the package's work-package document. The generator checks
each specification reference and checks that every test, tracked path, and xtask command
exists and belongs here. Narrative — what a package has *not* established, and why —
remains in its work-package document.

| Requirement | Evidence | What it establishes |
| --- | --- | --- |
| MODEL-003 | `the_envelope_round_trips_strictly` | The envelope round-trips through one strict validator: exact schema, no unknown fields, every declared field well-typed, and the body re-proved canonical — an envelope cannot launder bytes the codec would refuse. |
| MODEL-003 | `the_handshake_refuses_and_never_degrades` | The handshake round-trips strictly, and the compatibility rule is total: equal versions are compatible; unequal versions refuse with a remediation naming the older side — there is no downgrade arm. |
| RPC-002 | `the_handshake_refuses_and_never_degrades` | The handshake round-trips strictly, and the compatibility rule is total: equal versions are compatible; unequal versions refuse with a remediation naming the older side — there is no downgrade arm. |
| RPC-003 | `the_envelope_round_trips_strictly` | The envelope round-trips through one strict validator: exact schema, no unknown fields, every declared field well-typed, and the body re-proved canonical — an envelope cannot launder bytes the codec would refuse. |
| RPC-003 | `unknown_and_mistyped_fields_refuse_by_name` | The strict arm rejects, both directions: an unknown field, a wrong schema, and a mistyped channel each refuse by name — never a silent skip, never a default. |
| RPC-004 | `the_size_bound_binds_the_wire` | The size bound binds the wire before anything parses: an oversized input refuses with both numbers named, at construction, at encode, and at decode. |
| RPC-005 | `the_envelope_round_trips_strictly` | The envelope round-trips through one strict validator: exact schema, no unknown fields, every declared field well-typed, and the body re-proved canonical — an envelope cannot launder bytes the codec would refuse. |

5 requirement(s), 4 annotation(s), 0 structured evidence row(s).

**Not established here:** that the evidence logically proves the requirement it claims.
The generator checks stable references, existence, command syntax, and ownership; the
semantic relationship remains a review obligation.
