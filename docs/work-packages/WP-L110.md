# WP-L110: Linux helper, GPT/MBR, file systems, polkit

- Spec version: 19.0.0
- Stage: Trustworthy write path (M3). Created 2026-08-19 on the arc plan
  `docs/reviews/WP-L110_ASSIGNMENT_PLAN_2026-08-19.md` (taken by the
  decision owner: `services/helper-linux/` as the path; LIN-003…007 and
  LIN-010 as later increments behind their own rounds, claimed nowhere
  yet).
- Objective: Deliver the Section 14 charter — the Linux privileged
  helper — as the process that is reached over the Linux transport
  (WP-040 increment 5, ADR-0055), accepts exactly HLP-001's six
  operations and nothing else, **independently re-discovers** the
  topology before the first write (HLP-002: the adapter's client contract
  run as root plus ADR-0018's byte layer over read-only device handles)
  and authors the two values nothing below it may — the table state
  (ADR-0014) and the protection verdict (ADR-0016) — validates plans
  over that authoritative snapshot with WP-050's engine and WP-060's
  planner, enforces ADR-0021's two-tier ladder (a floor act per apply
  consumed through WP-070's journal; polkit `auth_admin` without retained
  grants at ≥ Disruptive or any step flag), and executes admitted plans
  through WP-070's state machine and journal: GPT/MBR table writes and
  file-system operations per installed capability, through a SAFE-004
  launcher, under CONC-001 device locking, with cancel and resume. The
  helper integrates; it re-implements nothing delivered below it, and it
  never accepts a path, a command, or a client-asserted fact as an input
  to authorization.
- Requirement IDs: HLP-001, HLP-002, HLP-003, HLP-004, HLP-005, HLP-006,
  HLP-007; LIN-002 (GPT/MBR and the listed file systems "according to
  installed capabilities" — capability-gated, never assumed); LIN-009
  (polkit rules scoped to validated plan execution); CONC-001, CONC-002,
  CONC-003, CONC-004, CONC-005 (the helper is the serialization point);
  SEC-002 (replayed, expired, altered, cross-user, cross-device plans
  rejected); SEC-009 (the local audit log); PLAN-006 and PLAN-007
  (enforced, not defined); RPC-006's reattach *behavior* (the protocol
  half is WP-040's; the journal WP-070's; both exist, so the behavior
  binds here); SAFE-002 (context 1 — this is the one privileged process),
  SAFE-004 (every tool through the launcher), SAFE-005, SAFE-006, SAFE-009
  (a helper crate may carry reviewed `unsafe`; none is planned — HLP-002's
  device reads are `std::fs::File`). **Claimed increment-gated, three
  route decisions, none decided by drift:** (a) the launch and endpoint
  ownership route (who creates the `0711` directory and the per-user
  `0600` node, and how the helper comes to exist — ADR-0055 T5/T6 and
  LIN-009 in the room), before increment 1; (b) LIN-001's
  authorization/mutation half (ADR-0054: UDisks2, libblockdev or
  authoritative native tools; the UDisks2 ≥ 2.9 tool floor enters the
  CAP-006 store with the first invoker), before increment 4; (c) the
  launcher's home (WP-035's `SystemLauncher` is in `apps/cli`; a helper
  cannot depend on an app), before increment 4, with WP-035 in the room.
  Consumed, not claimed: every type and function below the helper
  (WP-010's domain and codec, WP-050's engine, WP-060's planner, WP-070's
  journal and state machine, WP-040's protocol and transport, WP-L100's
  adapter, the table parser); CAP-006 qualification rows (WP-050's
  reviewed act — this package *produces* the first tool invocations those
  rows qualify); LIN-008 (signed packages — `packaging/`); LIN-003…007
  and LIN-010 (LUKS, LVM, mdraid, dm/multipath operations, GRUB,
  fstab/crypttab verification — later increments or packages behind their
  own rounds, SI-13 among the gates); UI-011's display of the tier
  (WP-090's; this package reports it).
- Prerequisites: WP-000; WP-040 (increments 1–5: the protocol and the
  Linux transport); WP-060 (the planner and its refusals); WP-070
  (increments 1–5: the state machine, the journal, the apply lifecycle);
  WP-L100 (increments 1–5b: the client draft, the held standing, the
  runtime facts and the floor); WP-010's table parser.
- Test tier: T1, unprivileged, on every platform for the pure seams (the
  closed operation set, the refusal arms, the lifecycle over in-process
  pairs and authored journals) — no Tier-1 test opens a block device,
  launches a process, needs elevation, or needs Linux; the Linux-only
  legs are gated in the body and assert the typed unsupported-platform
  refusal elsewhere (the `ffi-linux-loop` and `transport-linux`
  precedent). **T2** for everything representational and everything that
  writes: disposable guests in the r-series shape on WP-020's fixture
  images, root as the setup actor, unprivileged users as the clients,
  never a workstation disk (SAFE-001, SAFE-007), each sitting recorded
  with its transcript digest.
- Register gates: **three, all inherited, one biting by name.**
  - **SI-13 (Later, before WP-L110's validate-plan surface)** — identity
    binding for pool and array write targets. The conservative answer is
    structural: validate-plan refuses any plan whose target is an
    `Aggregate` (no binding constructor for aggregates) until a round this
    package files decides; increment 2 ships the refusal, not the
    decision.
  - **SI-28 (Mitigated-open, floor in force)** — the adapter's client
    records stay `Weak`; this package's re-discovery is the first place a
    positively determined partition-table state exists, so `Strong` is
    reachable here **by SAFE-003's own terms** and nowhere else. That meets
    the floor; it relaxes nothing, and ADR-0017's revisit condition
    stands.
  - **SI-37 (Open, Later)** — multipath detection-only holds; a mutating
    operation on a multipath device or recognized member is `unsupported`
    (ADR-0011), and this package infers no cross-path sameness.
- Milestone sequencing, recorded so the start is honest: milestones gate
  integration exit, not package start (the standing precedent). M3's
  exit needs a live helper on ≥ 1 platform behind WP-080's apply
  surface; this package is that platform's helper and asserts no
  milestone exit by starting.

## Imported verification obligations — the founding duty

Each obligation below was recorded against this package's creation, with
the instruction that the creation cannot omit it. Each is assigned to the
increment that owns it; none is discharged by this document.

**From HLP-001, RPC-005 and CLI-004 (the closed surface):**

1. The helper accepts status/enumeration queries, validate-plan,
   apply-plan by plan hash, cancel, resume, and journal queries, and
   **nothing else exists**: no message carries a path to execute, a
   command string, or dynamic code, held by the protocol carrying only
   `schemas/`-defined types, and a closure test pins the operation set
   (increment 1).

**From HLP-007, RPC-001 and ADR-0055 (identity before work):**

2. Every connection is admitted by the kernel at the user-owned node and
   verified by the transport before any byte is read; a non-local or
   cross-session caller performs no work (SEC-002) (increment 1).

**From HLP-005, HLP-006, SEC-009, SAFE-006:**

3. Locked-down idle with exit permitted; structured, redacted logging
   appended to a local audit log with explicit retention — a raw serial,
   path, label, username or key in any log line fails the suite
   (increment 1).

**From HLP-002, ADR-0014, ADR-0016, ADR-0018 (re-discovery before the
first write):**

4. Before the first write the helper re-discovers independently — the
   adapter's contract as root plus `classify` over head and tail windows
   of each whole device opened read-only — and authors the table state
   and the protection verdict itself; client-provided discovery,
   capability or validation output is an untrusted hint, never an input
   to authorization; the authoritative snapshot is the one whose hash the
   authorized plan binds (increment 2).
5. The table node the helper authors is handed to WP-L100's increment 3b
   as the route ADR-0036's second branch waited on (increment 2; the 3b
   work stays WP-L100's).

**From HLP-004, PLAN-006, PLAN-007, SEC-002:**

6. Validity windows and snapshot-hash freshness enforced; replayed,
   expired, altered, cross-user and cross-device plans rejected with a
   typed, explained refusal (increment 2).

**From ADR-0013 and ADR-0054:**

7. The helper publishes its own reach declaration for its privileged
   contract, per state, from the contract, never omitted when "no";
   discovery is through the client-readable interfaces plus the byte
   layer — UDisks2 is not the discovery interface (increment 2).

**From SI-13:**

8. Aggregate write targets refused structurally until decided
   (increment 2).

**From ADR-0021, HLP-003, LIN-009 (the ladder):**

9. Every apply at every severity requires a fresh floor act by the
   RPC-001-authenticated user naming the exact plan hash, single-use,
   journaled, never cached — consumed through `admit_apply` (ADR-0028's
   one act, one apply); the interactive ceremony at severity ≥ Disruptive
   or any step flag through polkit `auth_admin` without retained grants;
   the required tier computed by the helper from its recomputed severity
   and flags and reported in the validate-plan response; no message
   carries an authorization requirement (CAP-007) (increment 3).

**From ADR-0028, JRN-001…006, Section 8, RPC-006, CONC-001…005:**

10. Apply drives the state machine over the journal; the journal is
    append-only with torn-tail recovery; an executing plan locks every
    device it binds; queued plans are revalidated; external changes
    invalidate drafts; discovery during execution is transitional; two
    racing applies — one wins, one explained rejection; cancel and
    resume reconstruct from journal plus event replay (increment 4).

**From SAFE-004, SAFE-005, CAP-004, CAP-006, LIN-002:**

11. Every tool through the launcher's fixed absolute allow-list with
    verified identity and version, structured argv, bounded output,
    timeout, sanitized environment; tool floors entered in the CAP-006
    store with the first invocation; absence fails closed; file-system
    operations per installed capability, never assumed (increment 4,
    behind the toolset and launcher-home routes).

**From ADR-0011 and SI-37:**

12. A mutating operation on a multipath device or recognized member is
    `unsupported` with the multipath reason (increment 2's validation,
    increment 4's apply).

**From ADR-0053 and WP-L100 increment 4b:**

13. The consumed-versus-released question for a held member is decided
    in this package's capture, reading (b) (increment 2).

## Evidence sourcing — the rule this package works under

The WP-L100 rule, extended to the privileged layer: structural properties
(closure of the operation set, refusal arms, one-act-one-apply, bounds,
locking) may be tested over authored inputs, in-process pairs and
authored journals; every **representational** claim about a real Linux
host — what polkit answers, what a device's bytes classify to, what a
tool prints, what a kernel does on a write — rests on a recorded capture:
a `docs/quality/observability.md` row (WP-035's, filed in the two-act
bracket) or a Tier-2 transcript from a disposable guest archived with its
digest. Where none exists the increment delivers the fail-closed answer
and says so. Nothing in this package writes to a device outside SAFE-001's
disposable environments.

## Obligations on other packages, recorded so they are not discovered late

- **WP-035 — DR20, the polkit row:** whether `polkitd`/`pkexec` are
  present by default on the three pinned tier images, their versions,
  which agent answers, whether `auth_admin` can be exercised headless, and
  the `/run` tmpfiles conventions each tier ships — filed before the
  launch round closes; increment 3 does not merge before it is recorded.
- **WP-000 — the SI-13 round** (the register's), filed by this package
  before any aggregate operation is planned.
- **WP-035 — the launcher-home round** has that package in the room: the
  one SAFE-004 implementation is its today.
- **WP-050 — the first CAP-006 qualification rows** that increment 4's
  tool invocations would be judged by are WP-050's reviewed act, never
  authored here.

```traceability
mode: generated
```

Born `hand-maintained` per the standing precedent and converted to
`generated` by increment 1 in the same change that first ran
`cargo xtask traceability --write` (the standing conversion precedent).

## Boundary — surfaces this package must not build

Each prohibition names its governing authority.

- **No operation outside HLP-001's six**, no raw command, no path
  execution (RPC-005, CLI-004).
- **No authorization from connection standing, cache or session**
  (HLP-003, ADR-0021); no client-asserted capability or tier (CAP-007).
- **No discovery through UDisks2** (ADR-0054); whatever the toolset round
  decides is the *mutation* side.
- **No write outside an admitted plan's steps**, and none outside
  SAFE-001's disposable environments in any test (SAFE-001, SAFE-007).
- **No network socket** (SEC-007); no transport of its own (WP-040's).
- **No journal, planner, engine or adapter re-implementation** — the
  helper calls what is delivered.
- **No LUKS, LVM, mdraid, multipath-mutating, GRUB or fstab operation**
  until its own round and increment (LIN-003…007, LIN-010; SI-13).
- **No package, unit file or polkit policy shipped as product** until the
  launch round decides (LIN-008 is `packaging/`'s).

## Owned paths
```owned-paths
CHANGELOG.md
Cargo.toml
README.md
docs/work-packages/WP-L110.md
```
```owned-paths-reserved
services/helper-linux/**
schemas/helper/**
docs/traceability/WP-L110.md
```
Shared claims, and what this package's share of each is: `Cargo.toml` —
the workspace `members` entry for `services/helper-linux` only (WP-000's
file); `CHANGELOG.md` — entries for this package's own changes; `README.md`
— this package's own status row, added with increment 1. The reservations
are in the checker's defined shape for paths claimed before first
delivery; `services/` is Section 4.3's proposed layout, created by this
package's first increment. No `AGENT_BUILD_SPEC.md` share is claimed: the
Section 14 charter row exists and every claimed requirement is normative
text; a route decision that moves a spec text arrives as its own
`spec-change` ADR first.

## Increments

1. **The helper process and its closed surface** (after the launch
   round). `services/helper-linux` as a workspace member: a binary over
   `partman-transport-linux`'s endpoint, HLP-001's six operations as
   `schemas/helper/` message types in `partman-rpc` envelopes, every other
   request a typed refusal; HLP-007 by the transport; HLP-006's redacted
   audit log; HLP-005's idle posture; status and enumeration answered
   from the adapter's contract run as root, labelled the proposal it is.
   No device opened. Tier-1 over in-process pairs; Tier-2 the real
   endpoint in a disposable guest.
2. **Re-discovery and validate-plan.** The byte layer over read-only
   device handles, the helper-authored table state and protection
   verdict, the authoritative snapshot and its hash, `plan()` over it
   with the engine's facts, PLAN-006/007 and SEC-002 refusals, the
   helper's reach declaration, SI-13's structural refusal, the table node
   for 3b, the held report's consumer. Tier-2 over WP-020's fixture images.
3. **The ladder** (after DR20). The floor act through `admit_apply`; the
   computed tier in the validate-plan response; the polkit `auth_admin`
   ceremony; audit-logged; nothing cached.
4. **Apply** (after the toolset and launcher-home rounds). The state
   machine over the journal; CONC-001 locking; the product's first GPT/MBR
   table writer (reviewed; fuzzed where it parses; proven on fixture
   images in disposable environments only); file-system operations
   through the launcher per installed capability; cancel and resume;
   the first CAP-006 entries filed on WP-050.
5. **The record.** Reach, capabilities, README, CHANGELOG, generated
   traceability, the Tier-2 transcripts.

Every Rust increment owes its WP-020 sitting on the re-pinned condition;
every host claim a WP-035 row; every spec text that moves an ADR.

## Delivery status

This package is being delivered in increments. Each is independently
gated.

| Increment | Scope | Status |
| --- | --- | --- |
| 1 | The helper process and its closed surface: `services/helper-linux` (`partman-helper-linux`, `#![forbid(unsafe_code)]`, over `partman-transport-linux`, `partman-rpc`, `partman-adapter-linux`); HLP-001's six operations as `partman.helper.request`/`response` v1 (`schemas/helper/operations.md`), decoded strictly — an unknown field, a wrong schema, a path-shaped or command-shaped operation each a typed refusal, the set closed by an exhaustive match; `status` and `enumerate` served (the latter the adapter's client contract as root, labelled `proposal: true`, kinds/classes/counts and never identifier bytes), the other four accepted and answered `not-yet-served` naming their increment — fail-closed, never a stub; the launch rule (the launch round's L2, taken 2026-08-19: `allow_active`, unit files to `packaging/`) — `--serve <uid>` refused unless `PKEXEC_UID` equals it, the `0711` directory ensured, a pre-existing node `AlreadyServed` (the launcher connects to it), the `0600` node through the transport's `Endpoint::create`, one request per connection, idle exit removing its own node (HLP-005); HLP-006's audit log a closed event vocabulary with no field for an identifier; the polkit action `org.partman.helper.serve` (`services/helper-linux/polkit/`, exec-path annotated, `allow_active` yes) for `packaging/` to install | **Delivered 2026-08-19.** Five tests (the closed set and strict decode incl. `/bin/sh -c id`, `rm -rf /`, `../../etc/shadow` refused; serve/refuse/not-yet-served each audited; the audit vocabulary closed and identifier-free; the launch rule's four arms; the real loop over the transport at the test's own uid — status names exactly the served set, enumeration a proposal with one of the adapter's four arms, a second launch `AlreadyServed`). Eight mutations killed (unknown op accepted; strict decode dropped; unserved op as served; launch rule ignoring `PKEXEC_UID`; serving without `pkexec`; a pre-existing node not refused; enumeration not a proposal; status claiming every op). Rust: the WP-020 sitting is r54. **The Tier-2 acceptance taken 2026-08-19 on the r54 guest, recorded below**: the helper-side arms all as stated; the `pkexec` arm measured a session-less client refused under `allow_active` (an open item for the headless launch path, increment 3) |
| 2 | Re-discovery and validate-plan | Not started |
| 3 | The ladder | Not started (DR20 recorded; `pkttyagent` row to file) |
| 4 | Apply | Not started (the toolset and launcher-home rounds first) |
| 5 | The record | Not started |

**Increment 1's Tier-2 acceptance — taken 2026-08-19 UTC on the r54 guest
(VMID 9491, jammy 5.15.0-186, polkit 0.105-33ubuntu0.1 and setuid `pkexec`
by default, DR20), as root, after the WP-020 sitting, on the increment's
merge commit `d2c38b4` (checkout clean; the helper binary the sitting's own
build produced, digest `69511b13…`), installed at the action's exec path
`/usr/libexec/partman/partman-helper-linux` with the policy file under
`/usr/share/polkit-1/actions/`. Seven arms:**

- **H1 — polkit sees the action.** `pkaction` lists
  `org.partman.helper.serve`: implicit any `no`, inactive `no`, **active
  `yes`**, the `exec.path` annotation naming the helper's path.
- **H2a — `pkexec` from a session-less client: refused.** `muser1` through
  `runuser` (DR20: no logind session) running `pkexec … --serve 1001`:
  `Error executing command as another user: Not authorized` (rc 127) —
  **`allow_active` admits an *active local* session, and a session-less
  caller is not one**; a finding the launch round did not state in
  terms: a CLI user over `ssh` or from a non-seat session is not "active"
  to polkit either, so the headless launch path needs the action's
  `allow_any`/`allow_inactive` tier decided (a password through a text
  agent, `pkttyagent`, the row increment 3 files) or the launch from a
  seat. Recorded as the open item it is; the helper side is unaffected.
  **H2b** root's `pkcheck` of the action for its own process: rc 0.
- **H2c — the launch as `pkexec` delivers it.** The helper run as root with
  `PKEXEC_UID=1001` (exactly what `pkexec` sets after authorizing): it
  created `/run/partman` `mode=711 owner=root` and
  `helper-1001.sock` `mode=600 owner=muser1 type=socket`.
- **H3 — the wire, as `muser1`.** A minimal client (the frame and the
  codec's bytes generated on the workstation by the Rust codec) connected
  to the node, handshook (76-byte reply), and round-tripped **`status`**
  (188-byte reply decoding to outcome `status`, `served` = [`status`,
  `enumerate`], `authorizing_uid` 1001) and **`enumerate`** (826-byte
  reply: `proposal: true`, `enumeration: listed`, ten devices each
  `{kind: plain, transport: Unrecognized, properties: 18, selector}` — no
  identifier bytes on the wire).
- **H4 — a second launch for the same uid:** `a helper already serves this
  user; connect to its node`, rc 0, still exactly one node.
- **H5 — `--serve 0` with `PKEXEC_UID=1001`:** `refusing to serve uid 0:
  pkexec vouched for uid 1001`, rc 2. **H6 — `PKEXEC_UID` unset:** `not
  launched through pkexec`, rc 2.
- **H7 — idle exit** after 20 s: rc 0, the node removed, the audit log
  reading `started uid=1001` / `admitted uid=1001 pid=…` / `operation
  name=status outcome=served` / `admitted …` / `operation name=enumerate
  outcome=served`, and `idle-exit idle_seconds=20` — no identifier in any
  line.

Transcript `40-helper-acceptance.txt`, SHA-256
`77245af87da171167be9a8efaf7c5f805f779881cbfb1c752c2d067f1a4f45ea` (4472
bytes), computed in the guest and on the workstation, agreeing; archived
with the r54 bundle under `guest/helper/` at
`%USERPROFILE%\PartMan-evidence\partman-wp020-evidence-r54\`, custodian
Nate McBride; the guest destroyed 2026-08-19T21:05:25Z. **Not taken:**
the Debian 12 and Arch legs with the dependency installed (one guest this
time; the helper-side arms H2c–H7 are tier-independent, the `pkexec`
arm is what differs, and DR20 already measured what each tier ships) —
owed to increment 3 with the `pkttyagent` row; and a launch from a real
seat session, which this apparatus has no desktop to supply.

## Verification

- Increment 1: the operation set is closed by a test that matches every
  variant; an unknown operation, a path-shaped or command-shaped payload,
  and a request on an unverified connection are each a typed refusal; a
  raw identifier in any log position fails the suite; the endpoint's
  admission is the transport's (not re-implemented).
- Increment 2: over a fixture image, the helper's table state agrees with
  the parser's classification byte for byte and the client draft's
  absence of one is preserved as a proposal; a plan bound to a stale
  snapshot hash, an expired window, another user or another device is
  refused with its ground; an `Aggregate` target is refused structurally;
  the reach declaration names every state.
- Increment 3: no apply without an unconsumed act for exactly the
  offered plan hash; a second apply on the same act refused; the tier
  equals the helper's recomputation and never a client value; the
  ceremony invoked at ≥ Disruptive or any flag and not below; each with
  a mutation killed.
- Increment 4: a destructive Tier-2 suite in a disposable guest per
  operation class, the journal replayed to the same terminal state,
  cancel and resume proven, the device lock held for the full execution.
