# WP-030: Design tokens, dark UI shell, accessibility harness

- Spec version: 4.1.0
- Objective: Establish PartMan's visual language as data and an automated
  accessibility harness; preserve the immutable Tauri 2 comparison baseline;
  and conduct ADR-0009's bounded, off-main Slint 1.17.1 production-feasibility
  evaluation for the same read-only four-region shell. Tauri 2, React, and
  TypeScript remain the normative production stack under specification 4.1.0.
  Every candidate uses synthetic data and performs no storage discovery,
  planning, execution, helper contact, or elevation.
- Requirement IDs: SAFE-001, SAFE-002, SAFE-004, SAFE-009, MODEL-001, UI-001,
  UI-002, UI-003, UI-007, UI-008, UI-011, UI-013, PLAN-004, SEC-005, SEC-007,
  SEC-010, PKG-001, PKG-002, PKG-003, PKG-004, PKG-005, Section 4.1,
  Section 9, Section 11.7, Section 12
- Prerequisites: WP-000, ADR-0009
- Test tier: T1, unprivileged
- Acceptance scenarios: none directly assigned. ACC-011 (keyboard-only
  accessible apply) depends on this package. Increment 2 is scoped to supply the
  synthetic shell and feasibility evidence only; the scenario remains blocked
  on real plan, apply, authorization, and progress flows plus complete keyboard
  and assistive-technology evidence. No ADR-0009 probe enables T2 or T3.

Increment 2 was reviewed against the active 4.1.0 specification. The canonical
design-token file remains pinned to its 4.0.0 vocabulary because 4.1.0 added
MODEL-006 and ADR-C6 without changing this package's UI or safety contract; a
token-version bump with no token-contract change would manufacture evidence
rather than improve it.

## Owned paths

```owned-paths
schemas/design-tokens.json
crates/tokens/**
tools/xtask/**
Cargo.toml
deny.toml
.github/workflows/ci.yml
.github/dependabot.yml
docs/work-packages/WP-030.md
docs/traceability/WP-030.md
docs/quality/dependency-policy.md
docs/quality/test-tiers.md
README.md
CHANGELOG.md
```

Shared claims, and what this package's share of each is:

- `tools/xtask/**` — the `tokens`, candidate `desktop`, and
  `slint-controls` tasks; feasibility report/input validation; and, inside
  `supply-chain`, only root-workspace audit arguments plus fail-closed guards
  for the exact Tauri control exceptions and exact Slint release graph described
  below. The candidate `desktop` task owns the authoritative pre-Cargo full-prefix
  `SLINT_*` environment guard and locked Cargo-metadata feature/target evaluator;
  it may not parse human `cargo tree` text or reject an inactive package-list
  entry. The fuzz audit and every other supply-chain policy remain WP-000's.
  Shared with WP-000 and WP-020. Node stays confined to the Tauri control;
  Slint does not add Node to the Rust-only `ci` command.
- `Cargo.toml` — **one branch-specific member set and nothing else.** The
  preserved Tauri control uses `apps/desktop/src-tauri`; the Slint feasibility
  branch uses `apps/desktop` plus the isolated
  `tools/slint-feasibility` generator/replay crate so internal compiler tooling
  does not become an `xtask` dependency. No candidate branch may contain both
  desktop stacks. Shared with WP-000, which owns the rest of the workspace.
  `Cargo.lock` is not transferred: it may accompany those member manifests only
  as verified derived churn.
- `deny.toml` — **only** the exact, reason-bearing Tauri 2.11.5 advisory entries
  on its control branch, or exact-version `[licenses].exceptions` for every
  reachable Slint/i-Slint package that carries the elected royalty-free licence,
  together with a package/version, licence-text-hash, and feature-graph guard.
  The custom licence and GPL are never added to the global allow-list, and no
  category-wide warning, source, bans, or unrelated licence policy may change.
  Shared with WP-000.
- `.github/workflows/ci.yml` — **only** the platform-conditional prerequisites
  and existing Tier-1 steps needed by that branch's `cargo xtask desktop` gate.
  The Tauri control retains its pinned Node/Linux setup; the Slint branch may add
  only source-backed Winit/accessibility build packages. No new job, mutable
  action, required-check name, release upload, signing step, or destructive
  capability is authorized. Shared with WP-000.
- `.github/dependabot.yml` — **only** the npm `/apps/desktop` entry used by the
  Tauri control. The Slint member is already covered by the root Cargo entry and
  does not justify another ecosystem stanza. Shared with WP-000.
- `docs/quality/test-tiers.md` — **WP-030's token, desktop, Slint-control, and
  feasibility-report entries; the exact repository-controlled reads, ignored
  build writes, structured subprocesses, and non-destructive external
  qualification limits for those commands.** Shared with WP-000 and WP-020.
- `docs/quality/dependency-policy.md` — **only** the exact Tauri control
  exception subsection and a Slint 1.17.1 feasibility subsection naming the
  pin, elected licence, features, resolved risks, required badge ordering,
  offline-build evidence, review/removal triggers, and residual limitations.
  No project-wide policy rule may be weakened. Shared with WP-000.
- `docs/quality/accessibility.md` — **only** the synthetic shell's platform
  accessibility tree, keyboard, zoom/reflow, text-spacing, renderer, and manual
  assistive-technology procedure/results required by ADR-0009. It must preserve
  the boundary that ACC-011 and real apply-flow accessibility are not delivered.
- `docs/quality/slint-feasibility-data/**` and
  `docs/quality/slint-feasibility.md` — **only** normalized, non-secret evidence
  inputs and the byte-reproducible generated report for ADR-0009's exact gate
  registry. These paths are shared with WP-000's project-wide quality claim;
  WP-030 owns their feasibility content, not the thresholds or pass criteria.
- `README.md` — **only** the current-status sentence, the WP-030 bullet under
  “What does exist”, the M0 accessibility-harness row, and the WP-030 status
  row. While both desktop candidates remain off-main, those edits may describe
  evaluation progress but must preserve the truthful “no GUI on main”, no-CLI,
  no-planner, and no-helper claims. No other status, gate, milestone, or package
  text is assigned.
- `CHANGELOG.md` — **this package's own Unreleased entries only, not the
  surrounding document.** *Amended 2026-07-29: the project audit found
  increment 1 editing these files without owning them. Narrowed and clarified
  2026-07-31 before increment 2: its delivered shell would otherwise make the
  README's “no GUI” statements false while the package remained forbidden to
  correct them. The amendments are recorded rather than silent, because a work
  package that cannot update its own status guarantees the drift Section 1.10
  exists to prevent. ADR-0009 evaluation entries must say they are off-main and
  non-production; they cannot announce a delivered GUI before the promotion
  sequence completes.*

A path claim is file-granular; each sub-file grant above is narrower than any
checker can express, so it remains a review obligation. That gap is stated here
rather than implied to be automated.

### ADR-0009 bounded Slint feasibility assignment

This authority begins only when the governance-only commit carrying the exact
trailer below is on the implementation branch's base revision:

```text
Governance: authorize WP-030 Slint feasibility under ADR-0009
```

Specification 4.1.0 still requires Tauri 2, React, and TypeScript. This
assignment does not adopt Slint, weaken Section 4.1, or authorize a second
permanent desktop stack. It authorizes one review branch to determine whether a
Slint replacement can qualify under the immutable gate inventory in
`docs/adr/0009-bounded-slint-desktop-feasibility.md`. That implementation branch
uses `Work-Package: WP-030` and remains unmerged while evidence is collected.

The feasibility branch may:

1. Replace the branch-local Tauri application with one Rust workspace member at
   `apps/desktop/`, pinned to Slint 1.17.1 with the exact Winit/accessibility and
   renderer boundaries in ADR-0009. The default FemtoVG, software-only, and
   marked combined-control graphs are permitted; Qt, Skia, backend-default,
   interpreters, live preview, MCP, system testing, tray support, and unstable
   Slint features are not. `slint-build` is not permitted. The package owns a
   small AOT adapter that uses exact `i-slint-compiler` 1.17.1 with only the
   ADR-approved compiler feature closure and exact `spin_on` 0.1.1, writes a
   fixed file beneath `OUT_DIR`, and includes it directly without a `SLINT_*`
   Cargo environment handoff. The internal API remains exact-pinned,
   source-hash guarded, fixture-tested, and subject to a full source/API review
   and requalification on every Slint re-pin.
2. Extend `schemas/design-tokens.json` and `crates/tokens/**` only for canonical
   typography, cursor, layout, theme-signal, and generated `.slint` bindings
   demanded by the ADR. Independent policy and mutation tests must change with
   the schema. PartMan strings remain Rust-catalogue values; generated Slint
   code carries typed IDs, not English display text.
3. Add one lint-inheriting workspace tool for the content-addressed generator,
   replay, measurement, compiler-boundary audit, and normalized-data machinery
   under `tools/slint-feasibility/**`, plus generated
   `docs/quality/slint-feasibility.md` and its reviewed integer-unit inputs under
   `docs/quality/slint-feasibility-data/**`. The merged ADR is the gate and
   threshold authority; implementation-owned result fields cannot declare a
   pass. Raw diagnostics, secrets, user/device identifiers, signing material,
   and binary disk images remain uncommitted. Collection verifies each raw
   artifact before emitting a hash-bound reviewed attestation; replay verifies
   committed normalized inputs, manifests, and attestations. A discarded raw
   artifact is recorded as a digest observation, not misrepresented as a file
   that repository CI can still rehash. This tool owns the pinned compiler
   source/feature/environment inventory and AST/lowered-IR audit; it remains
   isolated from `xtask`, which invokes it only as a structured subprocess.
4. Add non-shipping package probes only beneath
   `apps/desktop/packaging-feasibility/**`. They may inspect named disposable VM
   state and generated synthetic disks read-only; they may not use future
   production packaging directories, sign/notarize/upload a release, discover a
   host device through the candidate, or enable a destructive test tier.
5. Make only the shared-file changes enumerated above: the branch-specific
   application and feasibility-tool member entries plus their derived root
   lockfile churn; exact policy/feature guards; the existing Tier-1 desktop step
   and source-backed build prerequisites; generated WP-030 traceability; the
   package's test/accessibility/dependency documentation; and its tightly scoped
   status/changelog rows.

The application is synthetic presentation code. It has no storage inventory,
external-command bridge, RPC/helper client, plan serialization, elevation,
telemetry, network path, or successful-operation simulation. Raw Linux byte
paths and Windows ill-formed UTF-16 identifiers remain authoritative Rust
values; only collision-safe escaped display strings cross the Slint boundary.
Exact storage values remain Rust `u64` and cross as preformatted IEC and exact
byte strings. A native dialog control performs no I/O before the held-handle and
object-identity containment checks in ADR-0009 pass.

The immutable Tauri core baseline is commit
`b0f11249903372d9b9cfba76128479ecfd3917f3` on draft PR #85. A later Tauri
dialog-control commit may add only behavior equivalent to the Slint dialog
control and remains off-main. The core baseline hash does not change. Primary
performance thresholds compare dialog-free cores; dialog dependencies and
footprint are reported as separate per-stack deltas and equivalent-feature
totals.

Success does not let this branch merge directly. It requires, in order, the
non-operative WP-000 adoption ADR, the separate 5.0.0 `spec-change` pull
request, and the governance-only promotion whose exact trailer is:

```text
Governance: promote WP-030 Slint implementation after Slint adoption
```

Failure closes the application branch. Only the normalized inputs, reproducible
non-product generator/replay harness, and generated failure report may land in a
separate evidence-only WP-030 pull request. A final governance-only change then
removes temporary application/manifest/policy/CI authority while retaining only
landed evidence paths, using:

```text
Governance: retire failed WP-030 Slint feasibility authority under ADR-0009
```

### Increment 2 comparison assignment (Tauri shell)

The decisions below preserve the Tauri implementation as ADR-0009's immutable
comparison/control branch. They no longer authorize that already-created branch
to merge ahead of the feasibility decision, and they grant no Tauri production
exception beyond specification 4.1.0.

Reserved now, ahead of the work, because both 2026-07-29 audits observed that
WP-030's assignment did not authorize creating a shell and that expanding scope
in a pull-request description after the fact is the pattern ownership exists to
prevent. The increment-2 application, package, workflow, and accessibility paths
do not exist yet; the checker treats a claim matching no file as a
**reservation** and reports it, so this list is visible rather than silently
vacuous. The exact review path has since activated.

*Corrected 2026-07-31, before increment 2 lands.* The formal requirement list
originally omitted UI-002 even though this package's scope, delivery table, and
reserved paths all assign the four-region shell here. A delivery-table mention
is not a substitute for the explicit assignment field Section 17 requires, and
behavior evidence cannot repair its own missing authority. UI-002 is therefore
added now, while its delivery status remains unchanged; the implementation pull
request must provide generated traceability before marking increment 2
delivered.

UI-013 is assigned here at the same boundary. Increment 2 creates the desktop
string catalogue and the inspector's display-size/exact-byte contract; leaving
that requirement unassigned would make both behaviors conventions rather than
reviewable obligations. This assignment covers the shell-owned boundary, not a
claim that user-facing strings introduced by future packages are already
externalized.

### Increment 2 Tauri Linux advisory boundary

The first full supply-chain dry run against the exact Tauri 2.11.5 graph found
a dependency constraint that the pre-shell assignment could not have named:
Tauri 2's required Linux `wry` runtime reaches the archived GTK3 Rust bindings
and `glib` 0.18.5. No maintained feature choice in the locked Tauri/Wry crates
removes that Linux backend. The graph therefore carries these informational
unmaintained advisories:

`RUSTSEC-2024-0370`, `RUSTSEC-2024-0411`, `RUSTSEC-2024-0412`,
`RUSTSEC-2024-0413`, `RUSTSEC-2024-0415`, `RUSTSEC-2024-0416`,
`RUSTSEC-2024-0418`, `RUSTSEC-2024-0419`, `RUSTSEC-2024-0420`,
`RUSTSEC-2025-0075`, `RUSTSEC-2025-0080`, `RUSTSEC-2025-0081`,
`RUSTSEC-2025-0098`, and `RUSTSEC-2025-0100`.

It also carries `RUSTSEC-2024-0429`, an unsound `glib::VariantStrIter`
implementation fixed only in `glib >=0.20.0`. GTK3 requires `glib ^0.18`, so
that published fix is not resolvable. Upstream is reviewing an exact backport
and 0.18.6 release; until one exists, the implementation may admit this ID
**only if** a fail-closed supply-chain guard proves the locked graph and scans
every non-`glib` Rust source in it for the affected `VariantStrIter` and
`array_iter_str` APIs. Any use, missing source, package/version drift, new
advisory, or newly resolvable patched release must fail and force review.

This amendment authorizes the implementation pull request to:

1. add only those fifteen exact, reason-bearing IDs to `deny.toml`;
2. pass the same exact IDs to the root-workspace `cargo audit` invocation,
   leaving the fuzz audit and `--deny warnings` unchanged;
3. add the locked-package and affected-API guard above, with automated tests;
4. document the evidence, residual risk, and removal triggers in the exact
   dependency-policy subsection assigned above.

It does **not** declare the exception acceptable in advance, classify
unsoundness as maintenance-only, authorize a category-wide warning mode, or
permit a production release while a reachable unsound path is known. The
implementation still has to demonstrate the boundary, keep every other
advisory fatal, and state that migration to a patched `glib` or Tauri 3/GTK4 is
the required long-term resolution.

```owned-paths-reserved
apps/desktop/**
packages/ui/**
packages/design-tokens/**
tools/slint-feasibility/**
docs/quality/accessibility.md
docs/quality/slint-feasibility-data/**
docs/quality/slint-feasibility.md
docs/reviews/WP_030_TRACEABILITY_MIGRATION_2026-07-31.md
```

The review path is deliberately exact: WP-030 owns only the completed zero-loss
ledger that records conversion of its former hand-maintained traceability
document. WP-000's broader `docs/reviews/**` claim still governs every other
project-wide review.

### Decision: the path is `apps/desktop/`, not `apps/shell/`

Decided 2026-07-29 by the decision owner, recorded because the second follow-up
audit rightly said the deviation should be decided rather than inherited. The
first reservation said `apps/shell/**`, which was a deviation from §4.3 nobody
had chosen.

Section 4.3's proposed layout is `apps/desktop/` beside `apps/cli/`, and §4.1
separately calls the component "Desktop shell: Tauri 2" — so *shell* is the
specification's own word for the thing, just not for the path. Following §4.3
costs nothing and settles two problems the deviation would have created:

- **`apps/cli/` is coming, and it is a different work package.** Section 14
  assigns the CLI to WP-080, and CLI-001…008 are normative. Two front ends
  named on one axis — `desktop` and `cli`, by how a user reaches them — stay
  parallel forever. `shell` beside `cli` mixes a role name with a form-factor
  name and invites the fair question of whether the CLI is a shell too.
- **"Shell" is overloaded in exactly this product.** SAFE-004 already governs
  how PartMan invokes external tools, and the product ships a real command-line
  interface. A directory called `shell` that is emphatically *not* the
  command-line thing is a misreading waiting to happen.

"Desktop shell" remains the component's name in prose, matching §4.1. The
directory follows §4.3. Nothing is deviating.

`packages/ui/**` was **missing** from the first reservation and is also §4.3's.
Without it the first shell pull request would have edited an unclaimed path and
failed `cargo xtask verify-ownership` — the reservation existed precisely to
stop that, and it had the gap it was written to prevent.

On the preserved Tauri comparison branch, `apps/desktop/` is the Tauri
application: the Rust `src-tauri` crate plus the React and TypeScript front end.
`packages/ui/` holds shared components and `packages/design-tokens/` the
generated typed accessor the audits asked for—the front end must **not** copy
the palette, so that package emits typed styles from
`schemas/design-tokens.json` and the desktop shell consumes only those. On the
Slint feasibility branch, `apps/desktop/` is instead the single native Rust
candidate described above; neither Tauri's web packages nor `src-tauri` exist
on that branch. No branch may contain both implementations.

### Integration decisions for increment 2

Decided 2026-07-30, because the audit was right that reserved paths alone do not
make the shell dependency-ready. Each is decided from evidence already in the
repository rather than from preference, and each says what would reopen it.

**1. Package manager: npm.** Not a preference — `packages/canonical` already
uses it, and `docs/quality/dependency-policy.md` builds a policy on that:
`package-lock.json` committed, `npm ci` in CI so a different tree fails rather
than resolving silently, `devDependencies` pinned to exact versions, and
`npm audit --audit-level=moderate` gating. A second package manager would mean a
second lockfile format, a second audit story, and a second Dependabot ecosystem
for one product. Reopen only if Tauri tooling requires something npm cannot do.

**2. Node configuration is app-local**, under `apps/desktop/`. Root-level files
would need their own WP-000 assignment and would couple the shell's tooling to
the repository root for no gain. The Node version stays pinned the way the
existing job pins it — exactly, in the workflow, with `engines` in
`package.json` as the human-readable floor.

**3. Root Cargo integration lands *with* the shell, not before it.**
*Corrected 2026-07-30. This decision originally said a WP-000 pull request would
add the workspace member and the lockfile entry first, and WP-030 would then
build the shell. That sequence cannot be executed. It was written from the shape
of the ownership rules rather than from an attempt, and attempting it is what
found the problem.*

Three measurements against `02ec952`:

- A minimal `apps/desktop/src-tauri` plus its member line, committed as
  `Work-Package: WP-030`, is **refused** for `Cargo.lock` and `Cargo.toml`.
- The identical tree committed as `Work-Package: WP-000` is **refused** for
  `apps/desktop/src-tauri/Cargo.toml` and its `src/main.rs`, because
  `apps/desktop/**` is this package's reservation. Neither package could take
  the first step.
- The member line cannot land alone either. Cargo refuses to load a workspace
  whose member has no manifest — `failed to read
  apps\desktop\src-tauri\Cargo.toml` — and a glob does not rescue it:
  `apps/*/src-tauri` matching nothing falls back to the literal path and fails
  identically. `cargo xtask ci` would have been red for everyone in between.

The deadlock is not this package's. `Cargo.lock` is WP-000's alone, so WP-010
adding a dependency to `crates/domain` hits the same wall; nobody had noticed
because no dependency-changing change had been attempted since the gate landed.

Three changes fix it, and all must land before increment 2:

1. **WP-000** teaches `verify-change-ownership` that a lockfile is *generated*.
   A `derived-paths` declaration lets any package carry `Cargo.lock` alongside a
   manifest that lockfile resolves; a lockfile moving on its own stays refused.
2. **Governance** shares root `Cargo.toml` with this package only for the
   branch-specific member set enumerated above: `apps/desktop/src-tauri` on the
   Tauri control, or `apps/desktop` and `tools/slint-feasibility` on the Slint
   candidate. This is a sub-file grant, so it remains a review obligation the
   checker cannot see, exactly like `tools/xtask/**` and this package's status
   rows in `README.md`.

3. **WP-000** also makes the inventory and the change gate agree about a
   reservation. *Added after this decision was first written, because an
   adversarial pass proved the sentence below false as originally stated: the
   scaffold commit passed `verify-change-ownership` and then failed `cargo xtask
   ci`, because `verify_path_ownership` did not count a matching
   `owned-paths-reserved` claim as coverage and reported `apps/desktop/**` as
   "claimed by no work package". No promotion route out was both green and legal.
   A reservation counts once it matches something.*

On the preserved increment-2T comparison branch, the crate, member line and
lockfile churn were added as one coherent change, and `cargo xtask ci` remained
green at every commit. That historical claim was checked by building the
scaffold against the composed branches rather than asserted — see the
increment-2T checklist below for the things the gate refuses that a generated
Tauri project would otherwise trip over. PR #85 remains off-main comparison
evidence; this paragraph does not authorize landing it.

### Increment 2T checklist, measured rather than guessed

Each of these was hit by actually scaffolding the crate against the composed
state. They are cheap to fix and expensive to discover one at a time:

- **`src/main.rs` must open with a `//!` crate doc.** `missing_docs = "warn"` in
  `[workspace.lints.rust]` plus `-D warnings` in the CI clippy line makes it an
  error, unconditionally. A Tauri template's generated `main.rs` does not satisfy
  it.
- **The crate manifest must carry `[lints] workspace = true`.** Without it a
  member silently escapes `unsafe_code = "deny"`, `missing_docs` and clippy
  pedantic — which matters most for exactly this crate, the one most likely to
  want FFI. `cargo xtask ci` now rejects every workspace member that omits this
  inheritance, so the former review-only gap is closed.
- **Any `package.json` needs a root-level `"license": "MIT OR Apache-2.0"`**, or
  `verify-licenses` refuses it. A generated Tauri or Vite manifest has no licence
  field.
- **The npm half of the supply-chain gate used to be hardcoded to
  `packages/canonical`.** WP-000 closed that gap before increment 2: it now
  discovers every `package.json`, refuses one without a committed lockfile, and
  audits every discovered package. The shell still needs its own exact
  Dependabot directory entry; this assignment authorizes only that entry.

**The Tauri dependency tree was reviewed as a second, separate question.** PR
#85 resolved and tested that graph under `deny.toml` and
`docs/quality/dependency-policy.md`; the baseline/advisory decision preserves
the result off-main for comparison rather than admitting it to the product. The
derived rule permits lockfile churn, but never decides by itself whether a new
dependency graph should enter this repository's supply chain.

**4. No new required CI job.** The shell's typecheck, lint and build are steps
in the existing Tier-1 job, because a new job means a new required-status-check
name, and adding one without updating branch protection in the same change
leaves every pull request waiting forever on a check that never reports. The
Slint candidate follows the same rule. The former
`.github/workflows/desktop.yml` reservation is removed by this governance
change; a later dedicated workflow needs its own assignment and must update the
required-check list in the same change.

*Corrected 2026-07-31.* The decision above originally named the existing
Tier-1 job without granting WP-030 any part of its workflow, and it did not
authorize adding the shell to Dependabot. That made the implementation
"dependency-ready" only on paper: it could create the application but could not
make its checks mandatory or its pins maintainable. The two exact sub-file
grants above close that governance gap without transferring ownership of CI or
repository-wide dependency policy away from WP-000.

The first correction still omitted one prerequisite that only became concrete
when the dependency tree was inspected: Tauri's `wry` runtime enables
WebKitGTK on Linux, while GitHub's `ubuntu-24.04` runner inventory does not
include the development package. Tauri's own prerequisite list requires
`libwebkit2gtk-4.1-dev`, `libxdo-dev`, `libssl-dev`,
`libayatana-appindicator3-dev`, and `librsvg2-dev` (the runner already supplies
the ordinary compiler and download tools). The workflow grant now includes
only an Ubuntu-conditional installation of those packages. This keeps the
existing Tier-1 check names and does not authorize unrelated runner mutation or
a new workflow.

**5. Increment 2T/Tauri-only: the front end may not contain a colour literal.**
The audits asked for a check that the shell uses no pairing the policy has not
declared, and the token harness cannot see it because it only reads the token
file. The enforceable form of that rule:

- `packages/design-tokens/` generates typed accessors from
  `schemas/design-tokens.json`. Generated output, never hand-edited, and a dirty
  regeneration fails CI — the same shape Section 11.7 wants for traceability.
- `apps/desktop/` and `packages/ui/` may reference colour **only** through those
  accessors. A check greps them for hex literals, `rgb(`/`hsl(`/`oklch(` and
  named CSS colours, and fails on any hit.

*Revised 2026-07-30, because the grep was to be the sole enforcement boundary and
this repository has now had a lexical boundary defeated four times — three in the
action scanner, once in the lockfile rule that was written the day before. A
deny-list over CSS and TypeScript would have to understand comments, test
fixtures, SVG attributes, CSS escapes, template strings, generated output and
whatever colour syntax CSS gains next. It will age exactly as the others did.*

The grep stays, but as the outermost and weakest layer, not the boundary:

1. **Generate** CSS custom properties and TypeScript role types from
   `schemas/design-tokens.json`; a dirty regeneration fails CI.
2. **Make the type the boundary.** Components consume a small typed style API
   keyed by role, not arbitrary colour strings — so the common case is enforced
   by the compiler rather than by a search for what the compiler allowed.
3. **Validate the pair where it is declared.** A component or story states its
   foreground/background *roles*; that tuple is checked against
   `contrastRules.pairings` at definition time. This is the check the audits
   actually asked for, and it catches an undeclared pairing before any renderer
   exists.
4. **Grep as defence in depth**, with an explicit allow-list for generated files.
5. **Increment 3 inspects computed styles** in rendered states, to prove the
   declared pair is the one a user sees.

Layers 1–3 are increment 2T. Increment 2S does not inherit this CSS/TypeScript
contract: it uses ADR-0009 G-CFG-05's generated typed `.slint` interface,
generated wrappers, and AST/lowered-IR enforcement boundary. What none of
these static layers establishes, and what must not be claimed before increment
3 exists: that the colour finally painted is the colour declared. A grep cannot
see a colour computed at runtime, and a role tuple checked at definition time is
a promise about intent, not evidence about pixels.

## Delivery status

| Increment | Scope | Status |
| --- | --- | --- |
| 1 | Design tokens as a single source of truth, and the accessibility harness that computes UI-001/007/008 from them | Delivered |
| 1a | Evidence remediation after the 2026-07-29 project audit: policy moved outside the audited file, required vocabulary made exact, strict parsing | Delivered |
| 1b | Follow-up audit repair: `tokenSetVersion` compared against `policy.rs` rather than merely required to be non-empty | Delivered |
| 1c | Generated traceability with source-local test annotations, typed non-test evidence, requirement-family-accurate findings, and a zero-loss migration ledger | Delivered |
| 2T | Tauri dark UI shell comparison: device rail, topology map, inspector, plan drawer (UI-002) | Preserved off-main in draft PR #85; 12 CI checks green; not delivered |
| 2S | ADR-0009 Slint production-feasibility candidate and generated evidence | In progress on dedicated draft PR #89. The native AOT shell, generated style wrappers, Rust-owned synthetic view model, exact byte/identifier presentation, Winit system-theme path, and exact FemtoVG/software runtime graphs compile and pass focused tests. Platform/rendered evidence remains uncollected. The latest stable 1.17.1 candidate currently fails the supply-chain hard gate on two unmaintained dependencies with no safe upgrade; this is rejection evidence unless upstream supplies a qualifying release |
| 3 | Keyboard navigation, focus management, and screen-reader semantics against an adopted real shell (UI-008, ACC-011) | Not started; blocked on the feasibility/adoption sequence |

**M0's "accessibility harness runs" criterion is partially met, not met.**
Increment 1 delivered a harness that runs and gates CI; increment 2S now adds
deterministic AOT generation, generated visual wrappers, a renderer-neutral
Rust view model, and an executable synthetic shell. This is implementation
evidence, not rendered cross-platform qualification. The parts the automated
boundary still cannot see are listed under "What this does not establish"
below, and they are the majority of WCAG 2.2 AA by volume.

## What increment 1 delivered and increment 2S adds

`schemas/design-tokens.json` is the single source of truth. It lives in
`schemas/` for the same reason `canonical-encoding-vectors.json` does: every
front end must consume *this* file. `AGENTS.md` already records the lesson for
the canonical codec — an implementation checked against a table it also owns
proves only self-consistency — and a second copy of the visual contract would
reintroduce exactly that.

`crates/tokens` reads it and audits these static contract families:

| Requirement | What is computed |
| --- | --- |
| UI-001 | Dark, high-contrast, and light themes exist, define the same exact 31-role roster, and carry their canonical label IDs. The default and unknown/dark/light system-scheme mappings are exact; high contrast remains a separate signal. This declares the mapping but does not detect an operating-system signal. |
| UI-003 | The eight storage-entity roles match UI-003 exactly, in both directions. A required entity cannot be deleted and a new entity role cannot be invented without editing `policy.rs`. This is static vocabulary evidence, not evidence that a shell distinguishes the entities visually and textually. |
| PLAN-004 | The five ordinal risk-severity roles match PLAN-004 exactly and findings for missing or invented severity roles name PLAN-004. |
| UI-011 | The eight progress-state roles match UI-011 exactly and findings for missing or invented progress roles name UI-011. This does not establish a rendered progress surface or live state transitions. |
| UI-007 | Each of the exact 21 entity, risk-severity, and progress-state roles declares an icon, label ID, and shape; no two share both icon and label ID. The static roster contains no health-state vocabulary and does not establish non-colour selection, so both remain shell-evidence obligations. The declarations are requirements on a later front end, not evidence that its channels were resolved and rendered together. |
| UI-008 | The complete colour roster, measurement-unit semantics, typography, text-flow, layout, cursor, caret, text-input selection, and oriented contrast-pairing contracts match independent policy. Every canonical foreground/background use meets its pinned WCAG 2.2 AA class — 4.5:1 for text (SC 1.4.3), 3:1 for user-interface components (SC 1.4.11) — in every theme. Token agreement is not rendered accessibility evidence. |
| UI-013 | Theme and semantic display text uses 25 stable label IDs rather than English embedded in the token contract. A dependency-free Rust test independently proves that the closed English catalogue resolves exactly those 25 IDs. Component wiring, localization infrastructure beyond the v1 catalogue, and exact-byte inspector presentation remain future application evidence. |

**The policy lives outside the file it judges.** `crates/tokens/src/policy.rs`
holds the WCAG and colour-separation floors plus the complete theme, colour,
label-ID, measurement-unit, typography, flow, layout, cursor, caret,
selection-pairing, and oriented contrast-pairing contract.
`schemas/design-tokens.json` restates those decisions so a front end can read
them, and the audit requires the restatement to *agree* — a lowered, removed,
invented, reclassified, duplicated, or renamed value is a finding, not a new setting. This
arrangement is a correction; see "What the audit found" below for what it
replaced and why.

Plus a colour-vision **smell test**: role pairs whose confusion would mislead a
user about risk are simulated under protanopia, deuteranopia and tritanopia and
required to stay apart by a CIE76 floor.

Run it with `cargo xtask tokens`. It is part of `cargo xtask ci`.

The second pre-Slint slice also generates
`packages/design-tokens/generated/partman-tokens.slint` byte-for-byte from the
audited source. The committed contract carries every current theme, colour
role, semantic role, mark, shape, label ID, typography/layout/cursor ID,
oriented contrast pair, and colour-separation pair. Text and UI contrast uses
have different enum, inventory, result, and resolver types, so a caller cannot
select the lower UI threshold with a runtime kind flag. Reversible encoded
identifiers avoid Slint normalization and Rust-codegen name collisions; a
dirty regeneration fails `cargo xtask tokens` and Tier 1. The generated source
contains typed IDs, not display labels.

`apps/desktop` now owns the native feasibility runtime. Its tests resolve the
exact 25-ID catalogue roster; independently round-trip every byte and every
WTF-16 code unit through domain-separated displays; cover invalid UTF-8,
surrogates, controls, bidi, combining text, emoji, CJK, backslashes, and escape
lookalikes; and prove bounded list displays cut only between whole graphemes or
escape tokens. Opaque selection IDs have one strict `sid:` plus sixteen-
uppercase-hex representation. The renderer-neutral view model preserves those
IDs across reorder/device changes, associates them with authoritative Rust
records, supplies preformatted IEC plus grouped exact-byte strings, and rejects
duplicates and malformed, forged, or stale callbacks. The synthetic fixture
matches the immutable Tauri core baseline and drives the AOT four-region shell;
it remains presentation-only and exposes no storage or helper API.

## The finding this package was worth doing for

The first palette written for this increment — chosen by eye, and looking
perfectly reasonable — failed its own harness on ten counts. The one that
matters:

```
severity.reversible against severity.destructive under deuteranopia
  is delta-E 10.1, below the 12 floor
```

`Reversible` is PLAN-004 severity 1, "fully undoable via an emitted reversal
plan". `Destructive` is severity 4, "data is intentionally destroyed". They were
mint green and salmon red — obviously different to a colour-sighted reviewer,
and nearly the same colour to a red-green colour-blind one. In the
high-contrast theme, the theme a low-vision user is most likely to select, the
same pair measured **delta-E 4.8**.

Three more pairs failed the same way, and three border colours sat under
WCAG's 3:1 floor for interface components.

The fix was not to lower the floor. The severity ramp now varies in **lightness
as well as hue**, because lightness survives every colour-vision deficiency
while the red-green axis does not, and the light theme uses a teal-leaning green
for `reversible` so it retains a blue component that deuteranopia preserves.
The current audit's tightest contrast is **3.57:1** for `border.default` on
`surface.base` in the light theme, against the 3.0 user-interface floor. Its
closest surviving colour-vision pair is delta-E 21.9 against a floor of 12.

## What the audit found

`docs/reviews/PROJECT_AUDIT_2026-07-29.md` demonstrated two fail-open paths in
increment 1. Both were reproduced before being fixed, and both are worth
recording because they are the same mistake in two places.

**The audited file supplied the standard it was audited against.** The contrast
floors were read from `contrastRules.thresholds` — in the file under audit.
Lowering the `text` floor to 3.0 and dimming `text.secondary` to match let
normal-size text pass the complete Tier-1 gate at **3.33:1**, well under
SC 1.4.3's 4.5:1. The colour-separation floor had the same shape.

This is precisely the failure `AGENTS.md` records for the canonical vectors —
"an implementation checked against a table it also owns proves only
self-consistency" — committed inside the harness written to enforce that rule on
colours. Increment 1's mutation table mutated *colours* thoroughly and never
mutated the *policy*, so it could not see it.

**The palette owned its own vocabulary.** The required roles were discovered by
reading the dark theme's keys and filtering for three prefixes. Deleting
`entity.container` from all three themes, the pairing list, and the channel
table left the gate green — 228 checks instead of 234 — even though UI-003
requires containers to be distinguished. A coordinated omission was
indistinguishable from a smaller product.

Both are fixed in increment 1a. `crates/tokens/src/policy.rs` holds the floors
and the vocabulary; the audit's own reproductions now produce findings.
Targeted mutations cover threshold lowering, threshold-class drift, threshold
removal, role deletion, pairing removal and duplication, risk-pair removal and
duplication, role invention, and a mismatched specification version.

The reader is also genuinely strict now: `deny_unknown_fields` throughout,
duplicate-rejecting readers for every map namespace, closed enums, bounded
integer units, and prose modelled as explicit `note` fields. Previously a
misspelled `nonColorChannels` key would have silently disabled the whole UI-007
check while leaving a file that looked complete; ordinary `BTreeMap`
deserialization would also have resolved a duplicate JSON member last-wins.

## What this does not establish

Recorded here rather than left to be assumed, because "the accessibility harness
runs" is exactly the sentence a reader will over-extend:

- **Compilation is not rendered or platform evidence.** The generated adapter
  reads Slint's system light/dark value and the shell binds canonical
  typography, flow, 44 logical-pixel targets, focus geometry, cursors, and caret
  width. No committed capture yet proves the resulting pixels, platform theme
  transition, high-contrast signal, text spacing, target geometry, pointer
  behavior, keyboard-only flow, screen-reader semantics, 200% zoom, or reduced
  motion. Those remain ADR-0009 qualification gates and later adopted-flow work.
- **Static vocabulary is not rendered distinction or live progress.** The
  harness proves that UI-003's eight entity-role names and UI-011's eight
  progress-state names exist. It does not prove that a shell distinguishes
  entities visually and textually, renders a persistent progress surface, or
  moves through the UI-011 states correctly. Those claims require increments 2
  and 3.
- **Only the exact canonical pairing roster is checked.** Its orientation and
  text-or-UI class are independently pinned, so the token file cannot silently
  lower a normal-text use to 3:1. A surface or pairing the front end invents
  outside that roster is still not covered. This becomes load-bearing the
  moment a shell exists. Increment 2S now compiles the typed `.slint` boundary,
  binds generated wrappers to visual/window/layout properties, and checks the
  pinned Palette/StyleMetrics inventory in source plus lowered IR. G-CFG-05's
  remaining evidence is that the declared pair is the pair actually painted in
  every required rendered state and renderer.
- **The colour-vision check is a model, not a proof.** Machado (2009) simulation
  and CIE76 difference, the crudest of the delta-E formulae. Passing is not
  evidence that two colours are distinguishable in practice. UI-007's redundant
  channels are the required design strategy; whether a shell resolves and
  renders them is separate evidence. The simulation exists to catch two roles
  collapsing onto one another, which is a design mistake worth failing a build
  over.
- **The non-colour channels are declared, not rendered.** The file says
  `severity.destructive` carries a `triangle-exclamation` icon and the label ID
  `meaning.severity.destructive`. The independent Rust catalogue resolves that
  ID and the other 24 canonical IDs, while the current shell renders textual
  entity and severity distinctions. It does not yet render the complete mark
  and shape gallery or every PLAN-004/UI-011 state, so renderer and platform
  accessibility-tree evidence remains required.
- **Identifier boundary tests are not rendered G-CFG-06 completion.** Full
  escaped byte/WTF-16 displays are collision-safe; bounded readable Unicode
  preserves original graphemes or whole escape tokens; stable Rust-owned IDs
  survive reorder and device changes; and the shell is wired to that model.
  The hostile corpus has not yet been rendered under each proposed renderer or
  inspected in each platform accessibility tree.

## Evidence discipline

Every independently pinned policy family has hostile mutations in
`crates/tokens/src/audit/tests.rs`. The aggregate test requires a finding owned
by the same requirement; overlapping checks can satisfy that assertion, so it
does not claim that every internal branch remains load-bearing in isolation.
The narrower historical deletion probes below were run after WP-020 found a
gate in `generate` that was load-bearing on nothing and kept 74 tests green
when removed.

| Check deleted | Mutation table |
| --- | --- |
| `check_color_vision` | FAILED — "'reversible' and 'destructive' become the same colour" was accepted |
| duplicate icon/label-ID detection | FAILED — "two roles share an icon and a label ID" was accepted |
| unknown-role detection | FAILED — "channels are declared for a role that does not exist" was accepted |

The colour maths is anchored outside this repository: black on white is WCAG's
published 21:1, a colour against itself is 1:1, and black against white is
delta-E 100 because CIELAB lightness runs 0..=100 by construction. The
colour-vision matrices are checked by their *defining qualitative property* —
red and green converge under protanopia and deuteranopia but not under
tritanopia, and greys are left untouched by all three — rather than by trusting
their digits, so a mistyped coefficient surfaces as a pair that failed to
converge instead of a plausible number with nothing to check it against.

## Traceability

How this package's `docs/traceability/` document is produced. Read by
`cargo xtask traceability`, which checks the declaration against the document it
describes in both directions: declaring `hand-maintained` while that file
carries the generated marker is refused, and declaring `generated` holds it to
byte equality with what the annotations produce.

```traceability
mode: generated
```

Evidence that does not belong beside one Rust test is declared here with typed
paths and commands. `cargo xtask traceability` validates every reference before
rendering `docs/traceability/WP-030.md`.

```traceability-evidence
- requirements:
  - UI-001
  - UI-003
  - UI-007
  - UI-008
  - UI-011
  - PLAN-004
  evidence:
  - path: schemas/design-tokens.json
  - path: crates/tokens/src/policy.rs
  - path: crates/tokens/src/audit.rs
  - path: crates/tokens/src/slint.rs
  - path: packages/design-tokens/generated/partman-tokens.slint
  - command: cargo xtask tokens
  claim: >-
    One token source is checked against independent theme-signal, complete
    colour-roster, semantic-vocabulary, measurement-unit, typography, layout,
    cursor, selection-pairing, exact oriented contrast-pairing,
    redundant-channel, and exact colour-separation-roster policy. The same
    audited source deterministically generates the committed typed Slint ABI,
    and dirty regeneration fails the command. This establishes the static
    design/generation contract only; Slint compiler acceptance, operating-system
    signals, and rendered or interactive accessibility remain outside the claim.

- requirements:
  - UI-013
  evidence:
  - path: schemas/design-tokens.json
  - path: crates/tokens/src/tokens.rs
  - path: crates/tokens/src/policy.rs
  - path: crates/tokens/src/audit.rs
  - path: crates/tokens/src/slint.rs
  - path: packages/design-tokens/generated/partman-tokens.slint
  - path: apps/desktop/src/catalogue.rs
  - path: apps/desktop/src/catalogue/tests.rs
  - command: cargo xtask tokens
  claim: >-
    Canonical theme and semantic entries carry stable label IDs rather than
    English display strings, the generated ABI carries typed IDs rather than
    labels, and the independent closed English catalogue resolves exactly all
    25 canonical IDs. This establishes the static externalized-string boundary;
    component wiring, exact-byte inspector presentation, and rendered labels
    remain future application evidence.

- requirements:
  - SAFE-009
  evidence:
  - path: crates/tokens/Cargo.toml
  - path: apps/desktop/Cargo.toml
  - command: cargo xtask ci
  claim: >-
    The token and desktop crates inherit the workspace lint policy, including
    denied unsafe code, and the Tier-1 gate compiles and lints both unprivileged
    boundaries.

- requirements:
  - Section 11.7
  evidence:
  - path: docs/work-packages/WP-030.md
  - command: cargo xtask traceability
  claim: >-
    WP-030 traceability is generated from validated sources, with an explicit
    zero-loss disposition for every row and narrative section in the former
    hand-maintained document.
```
