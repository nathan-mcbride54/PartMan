# WP-L100: Linux read-only inventory and capability adapter

- Spec version: 12.10.0
- Stage: Trustworthy read-only product (M1); the write-capable Linux
  helper that consumes this package is WP-L110's (M3)
- Objective: Deliver the Section 14 charter — the Linux read-only
  inventory and capability adapter — as a pure-read, unprivileged
  library that turns the ordinary Linux client contract into canonical
  domain values: INV-001/002/004/006/008 discovery normalized into
  WP-010's types with a MODEL-004 observation set behind every property,
  short of exactly the values the privileged helper authors,
  ADR-0018's transport classification supplied as the authenticated
  facts the protection closure consumes, LIN-006's detection-only
  representations, and the CAP-004-shaped runtime facts WP-050's engine
  already names this package as the future producer of. The adapter
  reads. It never opens a block device, mounts, unlocks, runs a repair
  tool, branches on privilege, or authors a value the privileged helper
  owns.
- Requirement IDs: INV-001, INV-002 (the reported properties and their
  observation sets; the SAFE-003 identity **record** and its strength
  are not this package's — they bind at validation, for the reason the
  register-gate note below gives), INV-004 (with
  ADR-0033's derivation rule as an imported obligation), INV-006,
  INV-008, LIN-006 (detection, with the multipath arm evidence-gated
  below), CAP-004 (the runtime facts produced, never judged), MODEL-002
  (the layering the topology expresses), MODEL-004 (provenance for every
  property this adapter reports), SAFE-002 (unprivileged, with no
  privilege-conditional branch anywhere), SAFE-004 (any tool the runtime
  facts require is launched through a structured argument array from a
  trusted absolute location — never a shell, never a user-controlled
  `PATH`), SAFE-005 (absence fails closed), SAFE-009 (workspace lint
  inheritance: this crate takes the `unsafe` denial rather than the
  adapter exception, because it opens nothing that would need one).
  **Claimed increment-gated: LIN-001** — UDisks2 as the discovery
  interface, behind its own recorded route decision (the WP-035
  increment-10 and WP-040 transport shape). The contract this package
  delivers reads sysfs and the udev database as ordinary files with an
  empty dependency closure; adopting UDisks2 buys LIN-001's named
  interface at the price of a D-Bus client dependency and an IPC
  surface, and that trade is a recorded choice, never drift.
  Consumed, not claimed: MODEL-005 and the `pce/1` codec (WP-010's), the
  snapshot body schema (WP-010's), CAP-001/003/005/006 (WP-050's engine
  judges the facts this adapter supplies; a qualification row in
  `docs/capabilities/` is that package's reviewed act), INV-003's
  *state* determination (helper-authored under ADR-0014 — this package
  publishes only its contract's reach, per ADR-0013), the protection
  verdict (ADR-0016, helper-authored), INV-005 and INV-009 event
  behavior (no consumer exists; they arrive with the surface that needs
  them), INV-007's diagnostic bundle (WP-035's; this package supplies
  the discovery material it admits), LIN-002's write stack (ADR-L1 and
  WP-L110's — only which file systems this contract distinguishes is
  detection), and every LIN-00x mutation requirement (WP-L110's).
- Prerequisites: WP-000; WP-010 (increments 1–3: the codec, the Section
  5 types, and the snapshot body this adapter fills); WP-020 (increment
  1: the deterministic fixture generator, for images a recorded capture
  is taken over); WP-050 (increments 1–4: the engine whose `RuntimeFacts`
  and `TopologySnapshot` inputs this package produces)
- Test tier: T1, unprivileged, on every platform. Every test drives the
  adapter's injected read seam over a fixture tree: no test opens a
  block device, launches a process, reads the environment, or requires
  Linux to run. Recording a capture from a real host is a separate,
  operator-run act whose output is a committed text fixture — never a
  test that needs hardware.
- Register gates: **two open, both conservative-refused here, neither
  resolvable by this package.**
  - **SI-28 (Mitigated-open, floor in force)** gates any identity-
    strength answer. The conservative answer is structural, and stronger
    than a refusal to classify: **this package emits no `DeviceIdentity`
    at all**, so it derives no strength for SI-28's population to be
    misclassified in. That record carries `table: TableState` as a
    required field whose every variant is a determination about the
    medium — `Present`, `Absent`, and an `Indeterminate` whose two
    causes are `Ambiguous` and `Unreadable` — and INV-003 forbids a
    client that read no table bytes asserting any of them, so an honest
    client record is not constructible in the first place. Identity
    binds at validation from the helper's own re-discovery: the pin
    SI-13's 2026-08-12 sweep verified, every delivered planner path
    emitting empty identity maps. What this package reports is identity
    *material* — each identifier an attributed observation naming the
    interface that produced it — and SI-28's floor inputs among it:
    transport class, removability, and identifier attribution, so the
    layer that owns the floor can compute it. Relaxation stays ADR-0017's
    named revisit condition and its apparatus-qualification evidence.
  - **SI-37 (Open, Later)** gates any relaxation of the unassembled
    unequal-identifier multipath population. ADR-0018's transport arm is
    the fail-closed design home and is delivered; this package supplies
    its inputs and infers no cross-path device sameness of its own
    (ADR-0011). Nothing here moves a closure-blocked population, and the
    per-platform dual-path matrix SI-37's evidence clause requires is
    not this package's to produce.
  - **SI-13** is pinned beyond this package — Later, WP-L110's
    validate-plan surface, verified accurate by the 2026-08-12 residue
    sweep. This package represents aggregates; it makes none of them a
    write target, because it makes nothing a write target.
- Milestone sequencing, recorded so the start is honest: milestones gate
  integration exit, not package start (the WP-035/WP-050/WP-060/WP-070
  precedents under the same sequential rule). M1's exit needs inventory
  correct against fixtures on **all** platforms; this package is one
  platform's half of that, delivers no user-facing surface, and asserts
  no milestone exit by starting.

## Imported verification obligations — the founding duty

Each obligation below was recorded against this package's creation, with
the instruction that the creation cannot omit it. Each is assigned to the
increment that owns it; none is discharged by this document.

**From ADR-0033 (SI-14, derived values are derivations):**

1. This package's INV-004 surface presents free extents and alignment as
   **derivations** — computed at use from the detected inputs they name,
   never stored, carrying no observation set and no confidence of their
   own — and does not present a derivation at all over an input whose
   observation set derives `unavailable` or `conflicting`, surfacing the
   input's own state instead, **with a fixture for each arm**
   (increment 3a). *Discharged in 3a. Free extents are not presented at
   all on this platform, on INV-004's own ground: the derivation "MUST NOT
   be presented at all where the host declares a table scheme the build
   cannot name", and this contract declares none. Alignment is presented,
   and its refusal arms carry fixtures.*

**From ADR-0011 (SI-12, multipath is detection-only):**

2. T1 tests over fixture-backed replay of **recorded** multipath
   enumeration data as regular files — the WP-035 replay pattern —
   asserting one dm node, its member path nodes, the kernel-reported
   membership relation, and `unsupported` with the multipath reason for
   every mutating capability on all of them; and two recorded devices
   presenting equal stable identifiers with no assembled dm node
   yielding SAFE-005 `blocked` on both (increment 4, evidence-gated
   below).
3. The fixture's shape is gated on the multipath observability rows that
   ADR's Consequences demand, because "a replay fixture authored from
   specification text alone would be the failure mode this ADR's own
   Context names." No such row exists today (increment 4's gate, and the
   obligation on WP-035 recorded below).

**From ADR-0013 (INV-003 scoped by privilege):**

4. The INV-003 reach declaration for **this package's own contract**:
   one answer per state, derived from the contract and the platform
   rather than from any device, never derived per-device, never omitted
   when the answer is "no" (increment 1).

**From ADR-0014 and ADR-0016 (the two authored fields):**

5. No partition-table state and no protection verdict is emitted by this
   package on any path — the helper authors both at validation, and the
   facts this adapter supplies leave exactly those positions absent, so
   the closure fails closed where an authored value belongs rather than
   reading a client's guess. The rule reaches the identity record too:
   `DeviceIdentity` carries a required table state, so this package
   emits no such record and the helper builds it from its own
   re-discovery (increments 1–3, structurally).

**From ADR-0018 (SI-11's protection closure, the transport arm):**

6. Transport classification into the closed positive-local list, with
   `Unrecognized` as the fail-closed default and the only answer for a
   class this build cannot positively name (increment 2). **Discharged in
   increment 2, and the discharge is `Unrecognized` for every device** —
   not a shortfall but the obligation's own terms. ADR-0018's evidence
   obligation (2), "fabric-versus-local transport discrimination rows per
   platform for each listed local transport", is outstanding on every
   platform. What is missing is not a value but the **discrimination
   protocol** that would let a value name a class: no row on any platform
   distinguishes fabric from local, and a mapping from interface strings
   to classes could come only from vendor documentation, which this
   package's evidence rule forbids. Two of the six positive-local classes
   now carry a real-hardware Linux measurement — USB mass storage, from
   the 2026-08-04 sitting and its two 2026-08-13 successors, and SD/MMC
   since the S5 sitting read a card through a native `sdhci-pci`
   controller — and neither classifies anything without the protocol.
   ADR-0018 prices exactly this availability cost under "Negative,
   accepted knowingly", and the answer resolves to `Indeterminate` at the
   closure, never `Permitted`. It improves when that protocol lands —
   whose home the obligations section below records as decided, and not
   this package's — and not before.

   *Restated 2026-08-14, on the rows issue #318 filed. This clause used to
   give two further reasons that have since gone stale: that "no value of
   any classifying key is recorded anywhere in this repository for any
   Linux host", which the 2026-08-13 readback falsified, and that five of
   the six positive-local classes had no Linux measurement of any kind,
   which the S5 sitting made four. Neither the discharge nor the answer
   moves; only the reason does, and it is worth stating precisely, because
   a reader who checks the old reason now finds it false and may conclude
   the answer is too.*

**From ADR-0019 (node naming):**

7. Node addresses are derived positional addresses, never stored; and
   same-kind nodes deriving equal addresses collapse, before encoding,
   into a counted, flagged, indeterminate collision group (increments 3a
   and 4). *The physical-device half is discharged in 3a through the
   domain's own `absorb`; re-implementing the grouping in this adapter
   would have been a second rule beside the normative one.*

**From ADR-C4 (MODEL-004's provenance shape):**

8. A positively observed **absence** is a value, not an unavailability —
   held by the observation outcome vocabulary's own shape, so the two
   cannot be conflated by a caller either (increment 1).

**From ADR-C5 (MODEL-002's aggregation model):**

9. An `Aggregate` carries the aggregate's **self-reported** member
   count, never a count of members currently observed (increment 4).

## Evidence sourcing — the rule this package works under

Structural properties — bounds, refusals, closure, unconstructibility,
the absence of a call — may be tested over authored fixture trees, the
shape WP-035's device-source tests already use. Every **representational**
claim about what a real Linux host exposes rests on a capture recorded
from a real host and committed as a text fixture.

Where no recording exists, the increment says so and delivers the
fail-closed answer rather than authoring a fixture from specification
text. ADR-0011 states this for multipath in terms; the rule is general
here because the failure mode is not multipath-specific — a fixture
written from the specification tests that the adapter agrees with the
author's reading of the specification, which is not evidence about any
platform.

## Obligations on other packages, recorded so they are not discovered late

- **WP-035** owns `docs/quality/observability.md`. The multipath
  observability rows ADR-0011's Consequences demand — which interfaces
  expose which path facts, at which privilege, and whether two paths to
  one device present bytewise-equal identifiers per layer and per form —
  must land there before this package's increment 4 can author its
  multipath fixture. Until they do, the multipath arm stays gated and
  the adapter answers that population with the fail-closed classification
  increment 2 produces.
- **WP-035** also owns the CLI's `inventory` and `topology` typed
  `not-implemented` refusals, which name this package by ID. Re-pointing
  those surfaces at this adapter is that package's edit under its own
  assignment, on its own schedule; nothing here changes them, and this
  package's delivery does not by itself make them stale.
- **WP-050** owns `docs/capabilities/`. A first Linux qualification row —
  the thing that would let any capability answer become `supported` — is
  a reviewed act under that assignment. This package supplies facts; it
  never qualifies a combination.

**Filed by increment 2 — the Linux rows this package's answers wait on.**
Each was recorded here rather than acted on: `docs/quality/observability.md`
is WP-035's path and `verify-change-ownership` refuses an edit to it. The
filing became issue #318, and WP-035 took all six as two records on
2026-08-13.

**Every one of the six is discharged.** The filed text of each is kept
below as the filing it was, with what answered it appended rather than
edited in — the six items were a claim about the record at the time of
filing, and rewriting them would erase what the sitting was for. The rows
that answered them live in `docs/quality/observability.md` under
*Readback rows, 2026-08-13* (**R1–R4**, transcribed from the archived
2026-08-04 transcript under re-verified custody; no new measurement) and
*The floor-rows sitting* (**FR1–FR5**, preregistered and taken the same
day on VM 9437, on the readback rows' own measured unit). The per-field
consequences are carried in `schemas/adapter-linux/fields.md`, swept to
match on 2026-08-14. **One residue survives, and it is not a
measurement:** item 1's discrimination *protocol*, whose home is now
decided and recorded at the end of this section.

1. **A transport-discriminating row per device class.** No value of any
   classifying key is recorded for any Linux host, so the transport
   answer is `Unrecognized` for every device and cannot improve without
   this. It is cheaper than it looks: the 2026-08-04 sitting *did* read
   the database entry — its interface column names the record path — and
   transcribed only the file-system and signature keys. This is a
   transcription gap in a completed capture, not an untaken measurement.
   — *Value half discharged by **R4**: `ID_BUS=usb`,
   `ID_USB_DRIVER=usb-storage`, `ID_USB_INTERFACES=:080650:` and
   `ID_TYPE=disk` on every database capture of the real unit, and
   `ID_PATH` observed with **two
   values in one sitting** when the passthrough moved between controllers
   — measured evidence that it names attachment topology, not the unit.
   The **protocol** half is the residue; see below. The transport answer
   stays `Unrecognized` for every device, now because no protocol maps
   these values rather than because no value exists.*
2. **A `removable` row.** None exists on any Linux host, and removability
   is one of SI-28's three named floor inputs. The field is read and
   nothing is derived from it; the gap is recorded per field in
   `schemas/adapter-linux/fields.md`.
   — *Discharged by **FR1**: `1`, rc 0, byte-stable across the double
   capture — the first qualifying `removable` value on any Linux host, and
   the value the SI-28 floor needs for this device class.*
3. **A real-hardware `queue/physical_block_size` row.** Measured on WSL2
   only, and absent from the one non-WSL projection.
   — *Discharged by **FR2**: physical `512` and logical `512` on real
   hardware, both rc 0 and byte-stable.*
4. **The whole-device discriminator.** No qualifying row establishes that
   a whole device positively lacks the `partition` attribute. The
   admission rule uses it fail-closed, so the unmeasured direction is the
   safe one, but the rule rests on a code precedent rather than a
   measurement and should not keep doing so.
   — *Discharged by **FR3**: on the whole device the read fails `ENOENT`
   with the attribute absent from the directory listing — a measured
   absence, ADR-C4's `ObservedAbsent` shape, not a failed read — and on
   the partition it reads `1`; both byte-stable. The admission rule this
   package and WP-035's CLI both use now rests on a qualifying
   measurement. It stays written fail-closed, which the measurement does
   not change.*
5. **Which sysfs attribute returned the serial the 2026-08-04 sitting
   observed.** The row bundles four attributes as one read set and
   attributes the value to sysfs generically. That is ordinarily
   academic; it is not academic here, because ADR-0019 makes the choice
   of a single named source per platform a normative, hash-visible act,
   and a naming designation cannot rest on a bundled row. The transcript
   is archived with its digest recorded and holds the instrument's exact
   paths, so this is a readback rather than a new sitting — and until it
   lands, **the Linux naming-source designation ADR-0019 owes cannot be
   made, and increment 3 cannot address a node.**
   — *Discharged by **R1**, and the answer was not the natural reading:
   the serial was read by parent traversal to the **USB device sysfs
   node**, `/sys/block/sdb/device/../../../../serial`, while
   `/sys/block/sdX/device/serial` — the SCSI-node attribute the bundled
   spelling suggested, and the one this package's roster reads — was never
   read in the sitting and is unobserved on every Linux host. **FR4** then
   resolved that traversal to `/sys/devices/…/usb10/10-1/serial`, identical
   value by both paths, discharging ADR-0034's evidence obligation 1. The
   designation ADR-0019 owed **has since been made**: ADR-0034, spec
   12.11.0, designating the traversal for USB-attached block devices and
   leaving WWN and every other attachment class undesignated. Increment 3's
   designation blocker is therefore clear; reconciling the delivered roster
   with the designated source is increment 3's own work, and
   `schemas/adapter-linux/fields.md` now records `device/serial`'s evidence
   as **none** so nothing relies on it meanwhile.*
6. **The `size` unit convention, which also blocks increment 3.** The
   512-byte-sector reading is confirmed only inside a record marked
   non-qualifying. `NamingFields::PhysicalDevice` carries a required
   `total_bytes`, so increment 3 cannot address a device without a byte
   figure, and ADR-0033 closes derivations at exactly two — no third may
   be minted here. This one is a prerequisite, not a nicety.
   — *Discharged twice, at two scopes. **R3** measured the 512-byte unit on
   the partition node against a declared byte extent, and stated the
   whole-device gap rather than bridging it by convention; **FR5** then
   measured the whole-device node itself — sysfs `size` `244457472` × 512 =
   `blockdev --getsize64`'s `125162225664` **exactly**. Increment 3's
   size-unit acceptance still lands in that increment's record, but it now
   cites a row instead of a convention.*

**The governance question is answered, and one residue outlives it.** The
question this section raised — WP-035's `observability.md` share is an
enumerated grant and did not visibly extend to a fabric-versus-local
transport-discrimination protocol, so these rows might have no
grant-covered home — went to the decision owner as a recommendation round
and was decided on 2026-08-13. Two routes were accepted and both are now in
WP-035's share:

- **The rows have a home.** The enumeration gains the class "rows another
  package's assignment explicitly files on this package as obligations",
  naming this package's issue #318 filing as its instance and bracketing
  every such row between two reviewed acts — the filing here and the
  recording there. That regularizes the two records that had already
  landed on the filing and merge review alone, and it means a future
  obligation filed from this section needs no further governance step.
- **The protocol does not.** The transport-discrimination protocol row is
  deliberately left with **no home in that grant**, "housed by whichever
  package first records a transport route decision, which files it here
  like any other obligation." That is the clause **as accepted on
  2026-08-13**, kept here as the text this section read; its addressee was
  re-pointed on 2026-08-17, below.

**That second clause needed a reading before anyone could act on it, and
this package was the one that noticed.** Everywhere else in this repository
a *transport route decision* is WP-040's — the per-OS **IPC** transport
(named pipe, Unix socket, XPC), route-decision-gated on the WP-035
increment-10 triangle. ADR-0018's evidence obligation (2) is about a
different thing under the same word: the **device-scope** transport arm's
closed positive-local list (NVMe over PCIe, SATA, directly attached SAS,
USB mass storage, SD/MMC, the paravirtualized local classes) against
fabric transports. Read literally against repository usage, the protocol
landed with a package that never consumes it, and the deferral became the
late discovery that filing obligations exists to prevent. Filed as
**issue #366**, and it stood as an open reading here until the decision
owner answered it.

**Answered 2026-08-17: the sponsor is WP-010**, and the clause in WP-035's
share now says so by name rather than by role. The reading this section
recorded is the one that was accepted — the phrase did denote the IPC
sense, and the protocol did have no scheduled author. Two facts measured
on the resolving round decided which replacement: the ADR-0034-pattern
designation extension that the 2026-08-13 round named beside WP-040 is
sponsored by **WP-010's** assignment, which reserved that ADR's path; and
**this package cannot be the sponsor**, because the protocol's only source
is vendor documentation and imported obligation 6 above records that this
package's evidence rule forbids it. The rows remain measurable here and
the protocol is not — that split is what the resolving round added to this
section's original reading, and it is why "whichever package first
discharges evidence obligation (2)" was rejected as the replacement.
**Nothing in this package's schedule changes.** The re-pointing gives the
protocol a sponsor; it does not make one owed here, and no increment below
gains or loses an item.

**Filed by increment 3a — two rows and one modelling gap.** Recorded
here on the same terms as increment 2's six, and for the same reason: none
is this package's to write. The filing became Gitea issues **#1002**,
**#1003** and **#1004** — the first issues minted in the post-migration
range, per `docs/quality/forges-and-the-push-gate.md`.

1. **A USB-device-node recognition row.** ADR-0034's designated serial
   source is "the nearest ancestor sysfs node that is a USB device node",
   and **FR4** establishes that the measured traversal *reaches* one. No
   row establishes what a client may read to *recognize* one, which is a
   different claim and the one the delivered predicate makes. The
   contrast is ADR-0035's mmc cell, whose structural rule **S5c**
   measured directly, which is why that designation needs no ancestor
   search at all. Delivered fail-closed on increment 2's
   `partition`-attribute precedent, so the predicate can only ever lose a
   name and never invent one; `schemas/adapter-linux/fields.md` records
   it per field as **none**. The row belongs in WP-035's
   `docs/quality/observability.md`.
2. **A real-hardware table-role row, or the finding that none exists.**
   The loop-attach record measures `ID_PART_TABLE_TYPE` misreporting on
   five fixtures; what no row covers is what the key does on real
   hardware, which is what a future designation would have to rest on.
3. **Mounts have no domain kind — resolved 2026-08-18.** Increment 3a's
   scope statement named mounts among the things assembled into a
   `Topology`, and `NamingFields` carries no mount variant. Filed as
   gitea#1004 and answered by decided text: MODEL-005's body-stability
   rule and ADR-0005 Rule 2 place the mount set in the envelope, so a
   mount is not a topology node and the sentence, not the model, was
   wrong. Increments 3b and 4 above now read accordingly; the Section 5
   `Mount` type is WP-010's, envelope-placed, arriving with its first
   consumer.

Nothing in this package blocks on it meanwhile. The transport answer is
`Unrecognized` for every device — the obligation's own terms, per
imported obligation 6 above — and it resolves to `Indeterminate` at the
closure, never `Permitted`, so the unmeasured direction stays the safe
one.

**Filed by increment 4, before its first line of code — ten detection
rows.** Recorded here on the same terms as the two filings above, and for
the same reason: `docs/quality/observability.md` is WP-035's path. The
plan that measured the gap is
`docs/reviews/WP-L100_INCREMENT_4_PLAN_2026-08-18.md`; the filing is
Gitea issue **#1005**. Increment 4 as scoped makes a representational
claim on every arm — what a mount-table line carries, what marks a block
node as device-mapper, mdraid, or loop, where a member's technology and
an array's self-reported count are read, what a Btrfs multi-device file
system exposes — and **no row supports any of them**: the Linux table
establishes the sysfs block-class attributes and the udev database and
nothing else, and this package's own `schemas/adapter-linux/fields.md`
§4 records "no mount table, no swap table … no row measures it on Linux
at all". Under the evidence rule above the increment cannot author its
fixtures from `proc(5)` and the sysfs ABI documents, so it waits on the
rows exactly as increment 2 waited on #318's. The ten cells, each a
client-baseline read on a disposable VM under WP-035 increment 6's
grant, double-captured, with root only as the setup actor:

1. **DR1** — `/proc/self/mountinfo`: readability, one line per mount,
   and the field shape (mount id, parent id, `major:minor`, root, mount
   point, options, optional fields, the `-` separator, file-system type,
   source, super options). The state-layer interface for INV-004/INV-005
   mounts, and its keying field.
2. **DR2** — `/proc/swaps`: readability and shape. ADR-0018's active-swap
   state-layer source (`docs/adr/0018-…:366`).
3. **DR3** — kind markers: which of `dm/`, `md/`, `loop/` exist under a
   device-mapper, an mdraid, a loop, and a plain whole device; the values
   of `dm/name` and `dm/uuid` (the `LVM-` and `CRYPT-` prefixes). What
   positively marks a host-assembled block node.
4. **DR4** — `slaves/` and `holders/` listings on members and on the
   assembled device, both directions. The kernel-reported membership
   relation, and whether it is symmetric as read.
5. **DR5** — `md/level`, `md/raid_disks`, `md/array_state`, and the udev
   record's `MD_LEVEL`, `MD_DEVICES`, `MD_UUID`. The self-reported member
   count imported obligation 9 requires, and its interface.
6. **DR6** — udev `ID_FS_TYPE`, `ID_FS_USAGE`, `ID_FS_UUID` on an LVM
   member, an mdraid member, the LUKS device, each Btrfs member, and the
   ext4 device. Whether the cached signature view names the member
   technology, and whether Btrfs members share one UUID.
7. **DR7** — `loop/backing_file`, `loop/offset`, `loop/autoclear` on a
   file-backed loop device and on a snapd loop device. The host-backing
   input (ADR-0019), read by name — the #94 standing recorded beside it,
   since the row's claim is what a client sees and not what the device is
   bound to.
8. **DR8** — `/sys/fs/btrfs/<uuid>/devices/`: the multi-device layout as
   one file system over n backings.
9. **DR9** — byte stability of every sysfs attribute and the udev record
   of the ext4 device across mount → unmount → mount. ADR-0005 Rule 2's
   mount arm, at the input layer.
10. **DR10** — per-unit distinctness and stability: two volume groups,
    two arrays, two LUKS containers — whether `dm/uuid`, `MD_UUID`, and
    `ID_FS_UUID` are distinct per unit and stable across a re-assembly.
    The inputs a naming-designation round for host-assembled kinds would
    rest on (ADR-0034's three criteria); no such designation exists on
    Linux today, so increment 4's topology half waits on that round as
    well as on these rows, and its state-layer half waits on the rows
    alone.

What the sitting deliberately does not measure: no multipath (imported
obligation 3 stays gated; two paths to one LUN are not this apparatus),
no partition-hosted arm (3b's block stands), no transport-discrimination
protocol.

*Discharged 2026-08-18, the same day: WP-035 preregistered the ten cells
and took them on two disposable guests (VMID 9467 void for DR2 only, on
a setup-actor flag; VMID 9468 the cited run), all ten established —
`docs/quality/observability.md`, "The detection-rows sitting". Three
findings changed what increment 4 could assume, and are carried into
4a: a Btrfs mount's `major:minor` is an anonymous device (DR1);
`slaves/`/`holders/` is a per-mapping relation, not aggregate membership
(DR4); a plain disk's cached `ID_FS_TYPE` is positively empty (DR6).
Increment 4a landed on the rows; 4b waits on them and on the designation
round DR10's inputs allow.*

```traceability
mode: generated
```

Born `hand-maintained` per the standing precedent — the generated file
cannot exist before the first increment — and increment 1 converted the
mode in the same change that first ran
`cargo xtask traceability --write`, following the standing conversion
precedent.

## Boundary — surfaces this package must not build

Each prohibition names its governing authority.

- **No mutation, no privilege, no device access.** This is a read-only
  adapter running at Tier 1 under SAFE-002, with no privilege-conditional
  branch: running it as root must produce the same answer as running it
  as anyone else, or the published INV-003 reach becomes a per-user
  statement, which INV-003 forbids. Every Linux mutation requirement
  (LIN-001's authorization half, LIN-002's write stack, LIN-003, LIN-004,
  LIN-005, LIN-007, LIN-009, LIN-010) is WP-L110's.
- **No authored helper values.** Partition-table state is authored by
  the privileged helper from its own raw-sector parser (ADR-0014), and
  the derived protection verdict is authored under ADR-0016 from the
  helper's evidence contract. This package emits neither, on any path.
  MODEL-005's authoring set is closed at exactly those two entries, and
  an adapter that filled either would be the creep that closure exists
  to prevent.
- **No planning basis.** What this package assembles is Section 6's
  client draft snapshot — a **proposal**. The snapshot an authorized plan
  binds is the one HLP-002's re-discovery produces at validate-plan, and
  no surface here may describe its output as a validated planning base
  or as evidence that an undeclared state is absent (INV-003's consumer
  rule).
- **No capability judgment.** CAP-001's conditioning, CAP-003's statuses
  and reasons, and CAP-005's one-engine rule are WP-050's. This package
  produces CAP-004's facts and hands them over; a client capability
  answer is advisory in any case (CAP-007), and nothing here may
  short-circuit the engine or upgrade an answer.
- **No sameness inference.** The product infers no cross-path device
  sameness of its own (ADR-0011, a standing rule since spec 4.3.0).
  Two interfaces reporting a serial produce two attributed observations,
  never one elected identifier and never a claim that two rows are one
  device.
- **No identity record, and therefore no strength.** `DeviceIdentity`
  carries a required table state, and every variant of that field is a
  determination INV-003 forbids this contract making. The record binds
  at validation from the helper's own re-discovery; this package reports
  the material it is built from, attributed per observation. That is
  SAFE-003's and INV-003's arithmetic rather than a posture this package
  chose, and SI-28's floor is honored by the absence rather than by a
  promise.
- **No user-facing surface.** The CLI is WP-035's and WP-080's, the UI
  is WP-030's and WP-090's, and the diagnostic bundle is WP-035's. This
  package is a library.

## Owned paths

```owned-paths
CHANGELOG.md
Cargo.toml
README.md
docs/work-packages/WP-L100.md
```

```owned-paths-reserved
crates/adapter-linux/**
schemas/adapter-linux/**
docs/traceability/WP-L100.md
```

Shared claims, and what this package's share of each is: `Cargo.toml` —
the workspace `members` entry for `crates/adapter-linux` only (WP-000's
file, the standing sub-file-grant pattern); `CHANGELOG.md` — entries for
this package's own changes; `README.md` — this package's own status row
in the work-package table, and nothing else. The reservations follow the
checker's defined shape for paths claimed before first delivery;
`schemas/adapter-linux/**` is a directory reservation in a
per-file-claimed directory, the `schemas/rpc/**` and `schemas/journal/**`
precedent, and it is where the published reach declaration and the
observation-record format live.

No `AGENT_BUILD_SPEC.md` share is claimed: the Section 14 charter row
exists, every claimed requirement is already normative text, and the
ADRs this package builds under are landed. A future need — LIN-001's
route decision demanding a dependency-policy amendment, or a reach
declaration that turns a `no` into a `yes` — arrives as its own
governance grant first.

## Increments

1. **The contract, its seam, and its published reach.**
   `crates/adapter-linux` workspace member (`[lints] workspace = true`,
   `unsafe_code` denied by inheritance, `//!` docs), the injected read
   seam over sysfs and the udev database with the bounded-read
   discipline WP-035 established — a device-count bound and a
   per-value byte bound, each refusing rather than truncating, so a
   partial list is never mistaken for a complete one. The MODEL-004
   observation type: source adapter, adapter version, method, and an
   outcome vocabulary that separates a value, an unavailability reason,
   and a read error — with ADR-C4's rule structural, so a positively
   observed absence cannot be encoded as an unavailability by anyone.
   The INV-003 reach declaration for this contract under ADR-0013, with
   a source-text test that the reach module cannot name the device seam
   at all. Converts the traceability mode to `generated`.
2. **Devices and their identity material (INV-001, INV-002, ADR-0018).**
   Whole-device enumeration reported as attributed observations: model,
   vendor, transport, removability, capacity, both sector sizes,
   read-only status, and every identifier the contract exposes, each
   carrying its own observation set naming the interface that produced
   it — two interfaces reporting a serial produce two observations, and
   nothing here elects one. Transport classification into ADR-0018's
   closed positive-local list, `Unrecognized` the fail-closed default and
   the only answer for a class this build cannot positively name. **No
   `DeviceIdentity` is constructed**, held by a test over the crate's
   public surface: that record carries a required table state this
   contract may not determine, so it binds at validation instead, and
   SI-28's gate is discharged by the absence. SI-28's floor inputs are
   reported as facts; no cross-device sameness is inferred anywhere.
3. **The topology, and INV-004's derivations (ADR-0019, ADR-0033).**
   Split on delivery, because the two halves turned out to rest on
   different evidence and only one of them has any.

   **3a, delivered.** Devices addressed under ADR-0019's derived
   positional addresses from ADR-0034's designated sources, equal-address
   same-kind nodes collapsing into the counted, flagged, indeterminate
   collision group; the bytes-preserving naming seam ADR-0034 names as
   this increment's first delivery obligation; and INV-004's alignment
   derivation under ADR-0033, recomputed at use, stored nowhere, carrying
   no observation set or confidence of its own, and withheld with the
   input's own state surfaced wherever an input cannot carry it.
   **ADR-0033's imported obligation lands here**, with its fixture for
   each arm.

   **3b, not started.** Partitions, volumes, file systems, and encryption
   layers assembled into a `Topology`; `BackingSignature`
   nodes for FS-004 signatures including the observed-signature-without-
   observed-aggregate case INV-008 names; an unrecognized variant carried
   for every closed enum over externally observed values; the `Captured`
   snapshot assembled as Section 6's client draft proposal, its facts
   carrying no table-state entry at all, so the closure fails closed at
   exactly the position the helper's authored value occupies rather than
   reading a client's guess. Every one of those kinds needs a
   partition-table node to hang from, and the table-role question below
   is what stands between this package and one.
4. **LIN-006's detection layer.** Device mapper, loop devices with
   their ADR-0019 host-backing edges, Btrfs multi-device as one
   `FileSystem` over n ≥ 1 backings, LVM/mdraid/LUKS members as
   signatures and aggregates carrying ADR-C5's self-reported member
   count, and active root/boot/swap dependencies — **mounts included,
   as state-layer facts and never topology nodes**: MODEL-005's
   body-stability rule and ADR-0005 Rule 2 place the mount set in the
   envelope, so mounts are detected here (INV-004, INV-005) and reported
   through the adapter's observation surface, keyed to the mounted node's
   ADR-0019 address, and consumed by WP-050 once the Section 5 `Mount`
   type exists (`docs/reviews/ISSUE-1004_MOUNT_VARIANT_ROUND_2026-08-18.md`;
   gitea#1004). **The multipath arm opens only when its evidence
   exists** — obligations 2 and 3 above.
   Until then this increment delivers everything else and the adapter
   answers the multipath population with increment 2's fail-closed
   classification. **Split on evidence, 2026-08-18**
   (`docs/reviews/WP-L100_INCREMENT_4_PLAN_2026-08-18.md`), because the
   two halves rest on different rows and one of them also on a naming
   designation that does not exist:

   **4a, delivered 2026-08-18.** The state layer and the withdrawal, on
   the DR rows alone. The procfs mount and swap tables enter the contract
   as its third interface (`linux-procfs`, `Method::Direct`, entered by
   DR1/DR2 exactly as the first two entered by rows) and are read through
   the bounded seam under a table bound of their own; every line becomes
   an attributed observation carrying the kernel's line verbatim, parsed
   into DR1's/DR2's documented fields with no transformation, and a line
   off the recorded shape refuses the whole table rather than a partial
   one. Mounts key to the admitted devices by `major:minor` and nothing
   else — never by source path — so the whole-disk, loop and dm mounts
   key and the anonymous Btrfs, pseudo and partition entries stay unkeyed
   (DR1's finding, carried); the "mounted node's ADR-0019 address" of the
   scope sentence is reached through the admitted device the mount keys
   to, since a partition or dm source has no address here until 3b/4b.
   And the withdrawal: DR3's `dm/`, `md/`, `loop/` markers classify every
   admitted whole device — a marker positively present makes the node
   host-assembled, **reported and named nothing**, not an operand; every
   marker positively absent admits a plain disk; a marker whose listing
   did not answer refuses, the `partition` discipline again. Increment 3a
   had admitted every such node as an operand-eligible `PhysicalDevice`;
   that is withdrawn in the fail-closed direction. Four tests over
   authored trees carrying DR1/DR2/DR3's recorded shapes; six mutations
   killed (the field-count refusal dropped, the undetermined marker
   admitting as plain, keying by source path, the procfs method set
   heuristic, the withdrawal removed, the swap header check dropped).
   The published reach declaration names the third interface in its
   contract statement; no cell moves. `schemas/adapter-linux/fields.md`
   §7 carries the roster on its rows.

   **4b, not started — waits on a naming-designation round.** The
   topology half: member `BackingSignature`s from DR6's cached view,
   `Aggregate` with DR5's self-reported count, `Volume`s it produces,
   `EncryptionLayer` over a whole-device LUKS signature, `BackingExtent`
   plus host-backing edge for a file-backed loop only where the host file
   system is a node this adapter has (none until 3b) — each hosted on
   whole devices only, each a body node only through a designated Linux
   naming source, which ADR-0034's table does not carry for any
   host-assembled kind. DR10's per-unit distinctness and stability
   (`dm/uuid`, `MD_UUID`, `ID_FS_UUID`) are the inputs that round rests
   on. Partition-hosted arms and the multipath arm stay gated, each
   answered fail-closed and said so.
5. **The capability seam and the record.** `RuntimeFacts` produced in
   CAP-004's shape for Linux — tool presence and version state under
   SAFE-004's structured-argv, trusted-absolute-path discipline, and the
   Section 9 floor determination for the Debian/Ubuntu and Arch tiers —
   handed to WP-050's engine, which judges them. INV-006 held
   structurally, with a test pinning the absence of every mount, unlock,
   and repair-tool call. INV-007's discovery material offered through the
   field allowlist WP-035's bundle applies. Package record sweep: README
   row, CHANGELOG, generated traceability current.

Beyond these five: LIN-001's UDisks2 route decision; the multipath
fixture when its observability rows land; INV-005 and INV-009 event
behavior when a consumer exists to drive invalidation; and the mutation
requirements, which are WP-L110's under its own assignment.

## Delivery status

This package is being delivered in increments. Each is independently
gated.

| Increment | Scope | Status |
| --- | --- | --- |
| 1 | The contract, its seam, and its published reach: `crates/adapter-linux` (workspace lints, `unsafe_code` denied by inheritance, depending on `partman-domain` so MODEL-004's observations are the domain's own vocabulary and not a second one beside it), the injected read seam returning **bytes** so every declared rule is decided above it and a Tier-1 fake drives each — the entry bound and the byte bound each refusing with the count seen rather than truncating, non-UTF-8 refusing rather than converting lossily, and exactly one trailing newline stripped so a padded value is not turned into an absence. ADR-C4's separation is structural rather than documented: an attribute read requires an `InterfaceAnswered` token that only a successful listing produces, so a missing attribute cannot be read as a positively determined absence by a caller who cannot show the interface answered (compile-fail-proven unassertable), and an interface that did not answer is `unavailable`, never an empty listing. The interface decides MODEL-004's method — `sysfs` direct and therefore `authoritative`, the `udev` database heuristic and therefore `inferred`, because a cached third-party computation is not this client's observation — and the INV-003 reach declaration publishes one cell per state in INV-003's order, fixed-size so a missing cell is a compile error, every cell negative on a deliberately not-measured basis with the decision recorded (`schemas/adapter-linux/reach.md`). No test is platform-gated: the crate is pure over the seam, so all fourteen run on every CI leg. Converts the traceability mode. Seven mutants killed by named tests before proposal — the fail-open one (an absent interface listed as empty) killed by two, because the evidence token catches it a second time | Delivered |
| 2 | Devices and their identity material: whole-device enumeration admitted **only** on a positively determined absence of the `partition` attribute, so an unreadable attribute admits nothing — the fail-open reading would promote a partition into the device list and report its sector count as a device capacity. Nine sysfs fields and six database keys, each an attributed observation under its own `interface:native-property` key, electing nothing: the attribute layer's serial and the database's serial-shaped key are two properties because they are two interfaces' different answers, and merging them would manufacture a `conflicting` confidence from values never in conflict. ADR-C4's separation extended across the database half by a second token producer — a record that does not exist makes every key `unavailable`, while a key missing from a record that does exist is a positively determined absence. **Imported obligation 6 discharged**: the transport answer is `Unrecognized` for every device, which is the obligation's own terms rather than a shortfall, since ADR-0018's discrimination rows are outstanding on every platform and no Linux row records a classifying value — a source-text guard holds that no positive class is constructible in the module. The published reach moves to `implemented-reaches-no-table-state` with every cell still negative on a re-decided not-measured basis, and increment 1's `ID_PART_TABLE_TYPE` sentence is corrected in both the module and `schemas/adapter-linux/reach.md`. Per-field evidence published in `schemas/adapter-linux/fields.md`, stating for each field the row that supports reading it or an explicit none. Twenty-two tests and one compile-fail proof, none platform-gated. Seven mutants killed by named tests before proposal, two of them by two tests each. **Two corrections landed afterwards**, recorded rather than edited away: this increment claimed the no-`DeviceIdentity` rule was "held by a test over the crate's public surface" and shipped without one — the guard now exists and is mutation-verified — and `fields.md` gave `device/serial` a real-hardware strength that its bundled observability row does not license. **A third correction landed 2026-08-14**, when the rows this increment filed as obligations came back: `device/serial`'s strength was not merely over-stated but attributed to the wrong node, and is now **none**; `device/wwid`'s real-hardware result was a failed `ENXIO` read rather than the observed absence recorded here; and this row's stated reason for the `Unrecognized` transport answer — "no Linux row records a classifying value" — is false since `ID_BUS=usb` was transcribed, though the answer itself is unchanged, because what is missing is the discrimination protocol rather than the values | Delivered |
| 3a | Devices addressed, and INV-004's presentable derivation. **ADR-0034's first delivery obligation discharged**: a bytes-preserving naming seam beside the delivered text path, which that ADR records as "not a lawful naming-input path" because it validates UTF-8, refuses non-text, and strips one trailing newline. The divergence is measured rather than asserted — the same fixture file holding a lone newline is a positively determined absence through the text path and a one-byte name through this one, and non-UTF-8 bytes are a legal name where the text path refuses. The designated serial resolves **structurally**, not by the measured depth: the walk climbs the ancestor chain and takes the first node answering as a USB device node, and two decoy `serial` attributes — one nearer, one farther — kill the fixed-depth and first-readable readings together. ADR-0034's two outcome rules land with it: a measured absence leaves an operand with a weaker name, a failed read leaves an indeterminate non-operand, and an undesignated cell is read **not at all**, held by a source that records its reads so the negative is observable. WWN is read on no path. `total_bytes` comes from FR5's measured 512-byte unit, with every non-decimal, signed, empty and overflowing sector count refusing the address rather than guessing one, and an unaddressable device reported rather than dropped. Collision grouping is the domain's `absorb`, so the S4-measured same-model pair ADR-0034 prices collapses into one counted, flagged, indeterminate entry. **ADR-0033's imported obligation 1 discharged, with a fixture for each arm**: alignment presented over authoritative and inferred inputs, withheld with the input's own state surfaced over `unavailable` and `conflicting`, and withheld on an arm ADR-0033 does not name — an input fit by confidence that carries no usable value, which a positively determined absence is. Free extents are **not presented at all**, on INV-004's own ground. The `conflicting` fixture is hand-built and said to be: this adapter keys each property by the interface that answered, so production cannot currently produce a plural set, and the arm would otherwise go untested rather than unreachable. Fourteen mutants killed by named tests before proposal | Delivered |
| 3b | The layered topology: partitions, volumes, file systems, encryption layers, `BackingSignature` nodes, and the `Captured` snapshot as Section 6's client draft proposal | **Blocked on a table-role route, and the block is recorded rather than worked around.** Every kind here hangs off a partition-table node, and `NamingFields::PartitionTable` carries a `TableRole` — a scheme — which this contract cannot author, because it reads no table bytes and ADR-0014 makes the state helper-authored. ADR-0036's forward obligation put the choice to this increment in terms, and **the second branch is taken**: the solver reserves nothing on Linux client drafts until HLP-002 re-discovery supplies a table node. The first branch was declined on measured grounds rather than for want of a value. `ID_PART_TABLE_TYPE` *is* carried in the client-readable udev database for loop-attached fixtures, and the record measures it wrong on exactly the cases the domain model exists to represent: `gpt` on the **Indeterminate** conflicting-tables fixture whose backup view "appears nowhere"; `gpt` on a damaged fixture from which the kernel materialized nothing; `gpt, untraced` on a hybrid whose aliasing `0x0c` entry "left no trace in the client projection"; `materialized, unmarked` with "nothing flagged" on a missing-backup fixture; and `PMBR` rather than `gpt` on a 4Kn disk. Designating it would make this adapter assert table states the record measures as false, and flatten precisely what INV-008 and `TableRole::HybridMbr` exist to keep | Not started |
| 4a | The state layer (procfs mount and swap tables as the third interface, keyed by `major:minor`) and the withdrawal of host-assembled block nodes from the physical-device set (DR3's markers) | **Delivered 2026-08-18** on the DR rows (gitea#1005; `docs/reviews/WP-L100_INCREMENT_4_PLAN_2026-08-18.md`). Four tests, six mutations killed. Rust — the WP-020 sitting is r42 |
| 4b | The topology half of LIN-006's detection layer, whole-device-hosted; the multipath arm evidence-gated | **Waits on a naming-designation round** over DR3/DR5/DR6/DR10 — no Linux source is designated for any host-assembled kind (ADR-0034's table) — and on 3b for partition hosts |
| 5 | The capability seam and the record | Not started |

## Verification

Every increment: `cargo xtask ci`, `cargo xtask test --tier 1`,
`cargo xtask verify-change-ownership --base origin/main`, real exit
codes checked. No requirement is recorded as delivered without a
generated traceability row backed by a live test, and no imported
obligation is marked discharged anywhere but in the increment that owns
it.
