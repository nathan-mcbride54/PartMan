# WP-010: Canonical domain model and encoding

- Spec version: 4.1.0
- Stage: Foundations (M0); increment 3 unblocked 2026-08-09 — the register's
  gate is empty (SI-28 reclassified, Mitigated-open, floor in force)
- Objective: Define the canonical domain model, schema versioning, and the
  canonical encoding and hashing required by MODEL-005.
- Requirement IDs: MODEL-001, MODEL-002, MODEL-003, MODEL-004, MODEL-005,
  MODEL-006
- Prerequisites: WP-000, and ADR-C1 accepted (Section 15)
- Test tier: T1, unprivileged
- Acceptance scenarios: None directly assigned

## Owned paths

```owned-paths
.github/pull_request_template.md
AGENT_BUILD_SPEC.md
CHANGELOG.md
CONTRIBUTING.md
Cargo.toml
README.md
crates/domain/**
packages/canonical/**
schemas/canonical-encoding.md
schemas/canonical-encoding-vectors.json
fuzz/**
docs/adr/0000-template.md
docs/adr/0002-hashed-artifact-body-and-envelope.md
docs/adr/0005-aggregation-vocabulary-and-signature-nodes.md
docs/quality/fuzzing.md
docs/quality/dependency-policy.md
docs/quality/observability.md
docs/quality/test-tiers.md
docs/spec-issues/README.md
docs/traceability/WP-010.md
docs/work-packages/WP-010.md
```

```owned-paths-reserved
crates/table-parser/**
schemas/table-checksum.md
schemas/domain/canonical-collections.md
schemas/domain/canonical-set-vectors.json
schemas/domain/topology-snapshot-body.md
schemas/domain/plan-body.md
schemas/domain/node-entry-format.md
schemas/domain/body-vectors.json
docs/adr/0008-canonical-set-order-and-depth.md
docs/adr/0011-multipath-is-detection-only-in-v1.md
docs/adr/0012-non-goal-protection-is-unrepresentable.md
docs/adr/0013-inv-003-detection-is-scoped-by-privilege.md
docs/adr/0014-si35-table-state-axis.md
docs/adr/0015-safe-003-blank-strength-scope.md
docs/adr/0016-si34-verdict-placement.md
docs/adr/0017-si33-continuity-witness.md
docs/adr/0018-si11-protection-closure.md
docs/adr/0019-si27-node-naming.md
docs/adr/0020-si40-technology-limit-status.md
docs/adr/0021-si18-authorization-ladder.md
docs/adr/0022-si19-reversal-linkage.md
docs/adr/0023-si15-authored-alignment.md
docs/reviews/WP_010_TRACEABILITY_MIGRATION_2026-07-30.md
```

`schemas/` is claimed per-file rather than wholesale: `design-tokens.json` in the
same directory belongs to WP-030, and a directory claim would have hidden that.
`packages/canonical/` and `fuzz/` are listed because increments 2 and 4 delivered
them, which the prose list omitted.

The added shared and reserved claims are deliberately narrower than WP-000's
repository-wide foundation ownership:

- `AGENT_BUILD_SPEC.md` covers only a reviewed domain/canonical-semantics
  specification change, plus its document-control version and changelog row.
  It does not grant this package unrelated safety, platform, or product edits.
- `.github/pull_request_template.md`, `CONTRIBUTING.md`,
  `docs/adr/0000-template.md`, `docs/quality/dependency-policy.md`,
  `docs/quality/observability.md`, and `docs/quality/test-tiers.md` cover only
  their current-spec-version pointer when that normative addition bumps the
  specification. Their other policy and template content remains outside this
  package's assignment.
- `README.md` covers its current-spec-version pointer and WP-010's own status
  rows; `CHANGELOG.md` covers WP-010 implementation entries and the bounded
  post-acceptance integrity pass described below. A path claim is
  file-granular, so those sub-file boundaries remain review obligations.
- `docs/adr/0008-canonical-set-order-and-depth.md`,
  `docs/spec-issues/README.md`,
  `schemas/domain/canonical-collections.md`, and
  `schemas/domain/canonical-set-vectors.json` are the exact decision, issue
  disposition, schema rule, and cross-language fixture needed to land SI-31 at
  the per-schema layer where its accepted answer belongs. Governance PR #69
  reserved them before this implementation created them; the ownership gate
  treats each matching reservation as live coverage.
- `schemas/domain/topology-snapshot-body.md`, `schemas/domain/plan-body.md`,
  `schemas/domain/node-entry-format.md`, and
  `schemas/domain/body-vectors.json` are reserved, before first delivery per
  the shape #69 set, for increment 3's body-format completion slice: the
  three normative format documents for the hashed bodies the landed slices
  built — the captured/simulated topology-snapshot body (3c), the plan body
  (3i), and the node-entry map both bodies build on (3a/3f) — plus the
  shared cross-language fixture extending the MODEL-005 parity proof to
  those schemas. The documents record decided formats: they may restate the
  landed slice behavior and the ADR-0014/0016/0018/0019 texts it
  implements, and decide nothing — a field they describe exists because a
  slice delivered it, never because the document says so. The fixture is
  consumed by `crates/domain` and `packages/canonical` tests (both paths
  already owned) through the existing required `cargo xtask cross-language`
  job, which runs the whole TypeScript suite, so the slice needs no
  `tools/xtask` edit and is granted none.
- `docs/adr/0002-hashed-artifact-body-and-envelope.md` and
  `docs/adr/0005-aggregation-vocabulary-and-signature-nodes.md` are shared
  only for the bounded post-acceptance integrity pass below. Their accepted
  decisions remain outside that pass; the grant reaches stale current-status
  summaries and no further.
- `docs/adr/0011-multipath-is-detection-only-in-v1.md` and
  `docs/adr/0012-non-goal-protection-is-unrepresentable.md` are reserved for
  the SI-12 resolution and the SI-11 axis decision — register dispositions,
  which are this package's own blockers to clear, following the shape #69 set
  for SI-31. For those two ADRs, this package's `AGENT_BUILD_SPEC.md` share
  extends to the specification changes they land — their normative sentences
  and the §0.3 changelog and document-control version rows — and no further:
  the exclusion of unrelated safety, platform, and product edits above stands
  for everything the two ADRs do not decide. Granted here, before first use,
  per the ordering the ownership gate enforces.
- A bounded post-acceptance integrity pass may edit those two ADRs, SI-35's
  existing entry and the observability-status sentence under Part 6's
  "Preconditions on round four, in either issue" in
  `docs/spec-issues/README.md`, and the matching WP-010 `CHANGELOG.md` entry only
  to (1) record WP-035's already-landed ADR-0011 follow-up as complete,
  (2) correct ADR-0012's remaining attribution of all three rejected rounds to
  closure rules and qualify its guarantee to the client-visible facts its own
  safety analysis identifies, and (3) make SI-35's prose agree with the
  retained WP-035 record about the damaged-primary mechanism, the finite
  projections actually queried, and the incomplete/non-qualifying Windows and
  loop runs. This grants no authority to change either accepted decision, any
  normative requirement, any register status, option, class, or dependency,
  or the evidence record.
- A second, separately reviewed evidence-currency pass may edit **SI-35's
  authoritative direct-blocker row, its `Evidence status` block and the
  matching prose in its detail entry, the observability-status sentence under
  Part 6's "Preconditions on round four, in either issue" in
  `docs/spec-issues/README.md`, and the matching WP-010 `CHANGELOG.md` entry**,
  for one purpose only: to stop the register **understating evidence that
  WP-035 has already established and recorded**. The preceding grant runs the
  other way — it was written to record the Windows and loop runs as
  incomplete and non-qualifying, which they then were — and is spent; this
  one exists because those runs have since been completed and validated, and
  the package that produced the evidence is the package forbidden to move the
  row it affects. That separation is working as designed, so the correction
  comes here rather than there.

  Bounded to what the WP-035 record already states: the hardened non-WSL
  descriptor-bound loop protocol taken 2026-08-03 and valid on its third
  sitting, and the Windows completion rerun taken 2026-08-04 and valid. It
  may report those two acceptance-evidence categories as discharged and must
  keep the third — the chosen-option refusal demonstration — outstanding.

  This grants no authority to: resolve SI-35 or change its state from
  **Open**; change its class, dependency, hash-visible value, or requirement
  metadata; decide or rank any of its options; assert that any existential
  H-separation hypothesis is refuted; extend a finding beyond the enumerated
  projection each run actually covered; or edit the evidence record in
  `docs/quality/observability.md`, which stays WP-035's. A correction under
  this grant that would make SI-35 look closer to resolved than its own
  record supports is outside it.
- **`docs/adr/0013-inv-003-detection-is-scoped-by-privilege.md` is reserved to
  land SI-38's resolution** — reserved rather than claimed, because the
  ownership gate treats a claim matching no tracked file as a stale claim that
  reads as coverage, and the ADR does not exist until the change it authorizes
  lands. This is the shape #69 set for SI-31 and ADR-0011/0012 followed, decided by Nate McBride on 2026-08-05 after an
  adversarial round. SI-38 is a requirement-versus-requirement conflict, so it
  cannot be resolved by an ADR alone; this grant therefore extends this
  package's `AGENT_BUILD_SPEC.md` share to the specification change ADR-0013
  lands — **INV-003's normative sentence, the §0.3 changelog row, and the
  document-control version row** — following the shape used for ADR-0011 and
  ADR-0012. It also covers the current-spec-version pointers in `README.md`,
  `CONTRIBUTING.md`, `docs/quality/test-tiers.md`, and
  `.github/pull_request_template.md`, which the bump makes stale in the same
  change; the matching WP-010 `CHANGELOG.md` entry; and **SI-38's register
  disposition** in `docs/spec-issues/README.md` — its entry, its move out of
  the transitive-blocker class, the authoritative count, and the removal of
  the SI-38 gate from SI-35's row.

  Recorded because it is unusual: the decision Nate accepted **narrows an
  existing MUST**, which is why it is a major bump under 0.1 rather than an
  addition, and why an ADR alone could not carry it. He also decided, on the
  same day and as a separate question, that an ADR *may* record a decision
  whose implementation weakens a MUST, with the spec amendment as the
  instrument. Both decisions belong in ADR-0013.

  This grants no authority to: amend SAFE-002, SAFE-005, HLP-001, HLP-002, or
  any requirement other than INV-003; decide SI-35 or any part of it beyond
  removing the SI-38 gate from its row; change any other register entry's
  state, class, dependency, or metadata; or edit
  `docs/quality/observability.md`, which stays WP-035's. SI-35 must remain a
  direct blocker and must remain undecided.
- **A fourth, separately reviewed pass may record SI-33's liveness
  precondition as discharged**, decided by Nate McBride on 2026-08-05, and may
  clear three stale statements the same week's work left behind. It covers
  SI-33's entry and its authoritative row, Part 6 precondition 1's
  observability-status sentence, SI-38's now-contradictory Dependencies
  paragraph, and the matching WP-010 `CHANGELOG.md` entry.

  **What the decision is, stated so the grant cannot be read wider.** SI-33's
  filing sets liveness as "a precondition on any design, not a detail" and says
  that "until that passes on real hardware, this is a hypothesis". The
  2026-08-04 sittings passed it. So the pass may stop the register calling
  SI-33 a hypothesis, and no more.

  **What it MUST also record, because the evidence declared it before any data
  existed.** That prompt movement "cannot be attributed to exchange-synchronous
  detection, because a background poll could equally have produced it"; that
  the strongest recordable positive is "no staleness observed under these
  conditions", bounded to the slot-exchange family on one reader, one bridge
  and one build; and that the exposed reading is **not globally monotone**, a
  measured decrease across a PnP-arrival boundary having made an equality-only
  witness unsafe. An entry that records the pass without those three is outside
  this grant.

  This grants no authority to: resolve SI-33 or change its state from **Open**;
  decide its axis, its design, or its body-versus-apply placement; relax or
  narrow SI-28's interim conservative floor, which needs the design and not the
  liveness pass; change any issue's class, count, dependency or hash-visible
  value; or edit `docs/quality/observability.md`, which stays WP-035's.
- **`docs/adr/0014-si35-table-state-axis.md` is reserved for SI-35's axis
  decision**, following the shape ADR-0012 set for SI-11: fix the axis, leave
  the issue open, and let the design round start from a settled foundation
  rather than re-litigating it.

  **The filename is subject-descriptive, not decision-descriptive, and that is
  a deliberate deviation from the house style.** ADR-0011, 0012 and 0013 are
  each named for the decision they took, which is better. It is not available
  here: the ownership matcher accepts an exact path or a `/**` suffix and no
  mid-segment glob, so a reservation must name the file before the decision
  exists, and naming it for an answer nobody has taken would prejudge it. A
  later rename to a decision-descriptive slug is welcome and needs its own
  reservation.

  The grant reaches the ADR, SI-35's authoritative row and entry insofar as
  the axis decision changes them, and a matching WP-010 `CHANGELOG.md` entry.
- **`docs/adr/0015-safe-003-blank-strength-scope.md` is reserved for SI-39's
  resolution**, in the ADR-0013 shape: one register conflict, one decision,
  one specification change recording what was rejected. The filename is
  subject-descriptive for the same mid-segment-glob reason 0014's is: it
  names the sentence in dispute — SAFE-003's blank-can-be-Strong derivation
  and its scope — not the answer, which belongs to Nate's acceptance and not
  to a path reservation.

  The grant reaches the ADR; the SAFE-003 amendment with its document-control
  version row and §0.3 changelog row, under this package's existing
  `AGENT_BUILD_SPEC.md` share; SI-39's authoritative rows and entry; the
  version references the 6.0.0 resolution's footprint already establishes
  (`README.md`, `CONTRIBUTING.md`, `.github/pull_request_template.md`,
  `docs/quality/test-tiers.md`); and a matching `CHANGELOG.md` entry. It
  grants no authority to decide SI-35's `Present` face, which SI-39's own
  filing deliberately left to SI-35.
- **`docs/adr/0016-si34-verdict-placement.md` is reserved for SI-34's
  resolution** (round accepted 2026-08-09,
  `docs/reviews/SI-34_ROUND_2026-08-09.md`): the protection verdict is
  hashed-body content, helper-authored at validation — ADR-0014's
  architecture applied to the second and last field only the helper
  derives. The filename is subject-descriptive per the 0014/0015 pattern.
  For this ADR, the `AGENT_BUILD_SPEC.md` share extends to the
  specification change it lands — MODEL-005's authoring set moving to its
  two named entries, the document-control version row, and the §0.3
  changelog row — and no further. The grant reaches SI-34's authoritative
  rows and entry (including the stale M10 currency sentences its own note
  says are correctable once M10 exists), and a matching `CHANGELOG.md`
  entry. It grants no authority over the verdict's internal shape,
  PART-014 classification, or reason vocabulary, which stay SI-11's, nor
  over node naming, which stays SI-27's.
- **`docs/adr/0017-si33-continuity-witness.md` is reserved for SI-33's
  resolution** (round accepted 2026-08-09,
  `docs/reviews/SI-33_ROUND_2026-08-09.md`): the continuity witness as a
  refusal input, never an assurance — an epoch-token/counter field of
  SAFE-003's identity record, helper-verified like every other record
  field, scoped to exchange-capable targets on qualified apparatus. For
  this ADR, the `AGENT_BUILD_SPEC.md` share extends to the specification
  change it lands — SAFE-003's witness field and consumer rule, the
  document-control version row, and the §0.3 changelog row — and no
  further. The grant reaches SI-33's authoritative rows and entry,
  SI-28's row for the one sentence recording that its relaxation route
  became a named revisit condition (its floor and Mitigated-open state
  untouched), and a matching `CHANGELOG.md` entry. It grants no authority
  to relax SI-28's floor, decide SI-28, or qualify any second platform's
  apparatus, which is operator measurement work under the sitting
  discipline.
- **`docs/adr/0018-si11-protection-closure.md` is reserved for SI-11's
  resolution** (round accepted 2026-08-09,
  `docs/reviews/SI-11_ROUND_2026-08-09.md`): the protection closure —
  total verdicts with an `Indeterminate` residual, the named two-layer
  helper evidence contract, the normative effect table with release
  counted as destruction, the affected-set fixpoint with downward
  production over destroyed substrate, node-local device-scope
  inheritance, the regime and operation-class capability mapping, the
  exhaustive PART-014 class table, and the acknowledgment vocabulary.
  The filename is subject-descriptive per the 0014/0015 pattern. For
  this ADR, the `AGENT_BUILD_SPEC.md` share extends to the specification
  change it lands — Section 2.1's enforcement paragraph gaining the
  closure commitment, the Storage Spaces entry's SI-29 boundary with
  WIN-003's matching sentence, the sealed-volume entry's SI-30 routing
  with MAC-009's matching sentence, the document-control version row,
  and the §0.3 changelog row — and no further. The grant reaches
  SI-11's authoritative rows and entry; SI-29's and SI-30's entries,
  resolved within this decision; SI-37's row and entry for the
  reclassification off the increment-3 gate, its issue staying open;
  SI-27's row and entry for the handed-over theorem-premise and
  bind-set-semantics requirements; the version references the 6.0.0
  resolution's footprint already establishes; and a matching
  `CHANGELOG.md` entry. It grants no authority to resolve SI-37, whose
  own evidence clause remains unmet; to decide SI-27's naming or edge
  kinds; to relax SI-28's floor or decide SI-28; to move any population
  the closure blocks to `Permitted`; or to edit
  `docs/quality/observability.md`, which stays WP-035's — the evidence
  obligations the resolution names are taken under the standing sitting
  discipline, not by this grant.
- **`docs/adr/0019-si27-node-naming.md` is reserved for SI-27's
  resolution** (round accepted 2026-08-09,
  `docs/reviews/SI-27_ROUND_2026-08-09.md`): the node-naming rule —
  derived, kind-discriminated positional addresses from contract-read
  fields, canonicalization by the contract's named single source, the
  collision-group construction rule whose artifact always encodes, the
  file/byte-range node with the host-backing edge, the
  platform-membership edge typed without preempting ADR-0011's deferred
  path-set encoding, table views with verbatim conflicting-entry
  evidence, and the preserved-unknown budgets. The filename is
  subject-descriptive per the 0014/0015 pattern. For this ADR, the
  `AGENT_BUILD_SPEC.md` share extends to the specification change it
  lands — Section 5's type-list additions, MODEL-002's edge-kind
  additions with their semantics classes, LIN-006's deferred-edge-kind
  clause gaining its ADR pointer, the document-control version row, and
  the §0.3 changelog row — and no further. The grant reaches SI-27's
  authoritative rows and entry, the version references the 6.0.0
  resolution's footprint already establishes, and a matching
  `CHANGELOG.md` entry. It grants no authority to decide the multipath
  path-set encoding (deferred by ADR-0011 to the spec change that first
  makes multipath writable), SI-19's created-node residue, or SI-28; to
  infer cross-path or cross-device sameness anywhere; or to edit
  `docs/quality/observability.md`, which stays WP-035's — the
  designator and backing-designator rows the resolution names are
  operator sitting work under the standing discipline.
- **`docs/adr/0020-si40-technology-limit-status.md` is reserved for
  SI-40's resolution** (decided by the decision owner 2026-08-10:
  reading (a) of the filing's options): FS-007's "blocked reasons" is
  the generic noun phrase for the capability reason vocabulary — the
  reading the repository's prose already used — and an immutable
  technology limit's status follows CAP-003's definitions:
  `unsupported`, carrying the limit as its explicit reason and a
  remediation stating no remedy exists. The filename is
  subject-descriptive per the 0014/0015 pattern. **No
  `AGENT_BUILD_SPEC.md` share attaches to this ADR**: reading (a)
  amends no normative text — that classification fact is in the filing
  and is the reason this reservation carries no spec-change grant; if
  a later revisit needs one, it arrives as its own grant. The
  resolution pass may edit SI-40's entry (the resolution banner), the
  authoritative table's Later and Resolved rows for SI-40 alone, and a
  matching `CHANGELOG.md` entry. It grants no authority to change any
  other issue's state, class, or dependencies, to amend FS-007 or
  CAP-003, or to implement — WP-050 increment 2 consumes the decision
  under its own assignment.
- **`docs/adr/0021-si18-authorization-ladder.md` is reserved for SI-18's
  resolution** (decided by the decision owner 2026-08-11, by delegation
  in the session that ran the adversarially reviewed recommendation
  round, `docs/reviews/SI-18_RECOMMENDATION_ROUND_2026-08-11.md`, an
  untracked session artifact restated in the ADR): authorization as a
  two-tier ladder — a floor act for every apply at every severity, the
  interactive ceremony at ≥ Disruptive standing verbatim and extended to
  any flagged plan — with SAFE-002 untouched, and the register's named
  question answered as no plan-carried authorization-requirement field.
  The filename names the decided mechanism, the 0017/0018 post-decision
  pattern.

  The grant reaches the ADR; the HLP-003 amendment with its
  document-control version row and §0.3 changelog row, under this
  package's existing `AGENT_BUILD_SPEC.md` share; SI-18's authoritative
  rows and entry; the version references the 7.0.0 resolution's
  footprint already establishes (`README.md`, `CONTRIBUTING.md`,
  `.github/pull_request_template.md`, `docs/quality/test-tiers.md`); and
  a matching `CHANGELOG.md` entry. It grants no authority to amend
  SAFE-002, PLAN-004, or any requirement other than HLP-003, to add any
  schema or plan-body field, or to implement enforcement — the helper's
  HLP-003 behavior is WP-070's, and WP-040's authorization vocabulary
  consumes the decision under its own assignment.
- **`docs/adr/0022-si19-reversal-linkage.md` is reserved for SI-19's
  resolution** (decided by the decision owner 2026-08-11, by delegation
  in the session that ran the adversarially reviewed recommendation
  round, `docs/reviews/SI-19_RECOMMENDATION_ROUND_2026-08-11.md`, an
  untracked session artifact restated in the ADR): the reversal is an
  ordinary `OperationPlan` draft — emitted at planning time, bound at
  its own validation after the forward apply — carried in the forward
  body by reference, not by value, so `OperationPlan` is not recursive;
  created-node targets are step-output references resolved at the
  reversal's validation; truthfulness is a two-time property re-checked
  as draft preconditions. The filename names the decided mechanism, the
  0017/0018/0021 post-decision pattern.

  The grant reaches the ADR; the PLAN-008 amendment and Section 6's
  reversal body item, with the document-control version row and §0.3
  changelog row, under this package's existing `AGENT_BUILD_SPEC.md`
  share; SI-19's authoritative rows and entry; the version references
  the 7.0.0 resolution's footprint already establishes (`README.md`,
  `CONTRIBUTING.md`, `.github/pull_request_template.md`,
  `docs/quality/test-tiers.md`); and a matching `CHANGELOG.md` entry.
  It grants no authority to decide SI-15, SI-16, SI-17, SI-20, SI-24,
  or any REC-* behavior, to land the linkage field's byte encoding (the
  jointly-sequenced schema change arrives when WP-060 implements, under
  its own grant), or to implement the reversal increment, which stays
  WP-060's behind its own assignment.
- **`docs/adr/0023-si15-authored-alignment.md` is reserved for SI-15's
  resolution** (decided by the decision owner 2026-08-11, by delegation
  in the session that ran the adversarially reviewed recommendation
  round, `docs/reviews/SI-15_RECOMMENDATION_ROUND_2026-08-11.md`, an
  untracked session artifact restated in the ADR): a PART-009 deviation
  is authored, not inherited — a boundary whose byte offset the plan
  sets follows policy or carries one of the two existing causes, a
  boundary byte-identical before and after is an inherited fact
  recorded in consequence text with no override demanded, and a
  boundary placed coincident with a pre-existing structural edge is
  policy-conformant and recorded as such. The filename names the
  decided mechanism, the post-decision naming pattern.

  The grant reaches the ADR; the PART-009 amendment with its
  document-control version row and §0.3 changelog row, under this
  package's existing `AGENT_BUILD_SPEC.md` share; SI-15's authoritative
  rows and entry; the version references the 7.0.0 resolution's
  footprint already establishes (`README.md`, `CONTRIBUTING.md`,
  `.github/pull_request_template.md`, `docs/quality/test-tiers.md`);
  and a matching `CHANGELOG.md` entry. It grants no authority to decide
  SI-16, SI-17, or SI-24, to build the deviation-override vocabulary
  (deliberately still inexpressible), to add any typed alignment-fact
  field (the round's recorded revisit condition), or to change the
  solver — the refused case's unlock is WP-060's, riding its next Rust
  increment under its own assignment.
- **`crates/table-parser/**` and `schemas/table-checksum.md` are reserved
  for SI-35's resolution package** (accepted 2026-08-09 from
  `docs/reviews/SI-35_RESOLUTION_ROUND_2026-08-09.md`): the pure,
  bounded, `unsafe`-free raw-sector table parser ADR-0014 names as the
  contract — classification of caller-supplied byte windows only, no
  I/O, no process, no Section 5 domain type, no snapshot, no plan, no
  device access — and the copy-invariant `Present {checksum}` byte
  recipe. The parser's fuzz target travels under the existing `fuzz/**`
  claim; its registration stays WP-000's xtask row.

  The `Cargo.toml` claim added above is the WP-020 sub-file-grant
  pattern: the workspace `members` entry for `crates/table-parser` only,
  nothing else in that file. Shared with WP-000, whose file it is, and
  with the existing per-package member grants.
  If the accepted axis turns out to require a specification change — which the
  MODEL-005 placement question may force, since its envelope rule triggers on
  the very condition an axis decision settles — this package's
  `AGENT_BUILD_SPEC.md` share extends to that change's normative sentences and
  the §0.3 changelog and document-control version rows, in the shape ADR-0011,
  0012 and 0013 each used, and no further.

  This grants no authority to: resolve SI-35 or change its state from
  **Open**, which is the whole point of deciding an axis separately; change
  its class, dependency, or hash-visible value; decide SI-34, whose placement
  question this one abuts; assert that any existential H-separation hypothesis
  is refuted, which no measurement has done; or edit
  `docs/quality/observability.md`, which stays WP-035's. SI-35 must remain a
  direct blocker, and any text implying otherwise is an error against this
  grant.
- **A separately reviewed pass may record SI-28's reclassification off the
  increment-3 gate**, decided by Nate McBride on 2026-08-09, the SI-37
  pattern applied to the register's last direct blocker: SI-28 stays
  **Mitigated-open** — not Resolved — its interim conservative floor in
  force and unchanged, its relaxation route staying ADR-0017's named
  revisit condition; what moves is only its class, off the type's critical
  path, because the floor refuses the affected population from decided,
  contract-readable facts (transport class, removability, identifier
  presence — no undecided hashed field is an input to it) and the refused
  population can hold no issued authorization for a later mechanism to
  invalidate. The pass covers: the register's authoritative counts and
  class table, the direct-blocker state table's retirement, SI-28's rows
  and entry gaining the reclassification banner with the priced
  schema-major cost stated, this file's stage line, and a matching
  `CHANGELOG.md` entry. It grants no authority to resolve SI-28, relax or
  restate the floor, alter ADR-0017's revisit condition, weaken Part 7's
  warning against false closure, or change any other issue's state, class,
  or dependencies.
- A third, separately reviewed evidence-currency pass may edit **the
  observability-status sentence under Part 6's "Preconditions on round four,
  in either issue"** and **append a bounded status annotation to SI-34's
  "Evidence required before any option is accepted" paragraph**, plus a
  matching WP-010 `CHANGELOG.md` entry, for one purpose: to record that the
  macOS observability record now exists (issue #155). The preceding pass named
  the same precondition-1 sentence but is spent, and it reached SI-35 only;
  SI-34's paragraph was never within it.

  Bounded to what the WP-035 record states: the increment 6 macOS matrix taken
  2026-08-05, valid on its second sitting, M1–M8 executed, M9 `not
  established`, M10 `not yet taken`.

  **The annotation MUST state, not omit,** that M10 is untaken, that the macOS
  record therefore has no privileged comparison leg, and that SI-34's separate
  both-views freshness-projection requirement is unmeasured on macOS. It must
  also record the reading it relies on — that precondition 1 defines the
  observability record as "established empirically and **non-elevated**", so
  the privileged leg falls outside what "observability established" requires —
  together with the narrower reading it rejects, rather than silently taking
  the generous one.

  This grants no authority to: record SI-34 as resolved or as closer to
  resolution; declare its evidence clause discharged; change its state, class,
  dependency, hash-visible value, or requirement metadata; decide or rank any
  SI-34 option; assert that any element of its evidence clause other than the
  observability one is satisfied; touch SI-35, SI-38, or any other entry; or
  edit `docs/quality/observability.md`, which stays WP-035's. A correction
  under this grant that would make SI-34 look closer to resolved than its own
  evidence supports is outside it.
- That integrity pass may also (1) mark the copied blocker lists in ADR-0002
  and ADR-0005 as historical acceptance-time snapshots and direct current
  status to the register, (2) correct ADR-0005's stale claim that SI-11 and
  SI-27 both necessarily add hash-visible fields, (3) align SI-11's detailed
  scope with its authoritative axis-decided row and Part 6's surviving
  obligations without absorbing SI-27, (4) align SI-27's detailed scope and
  historical SI-12 prerequisite with its authoritative row and ADR-0011, and
  (5) place already-withdrawn SI-36 in a distinct Withdrawn class rather than
  inside the direct-blocker detail table. These are explicit exceptions to the
  structural register-class prohibition in the preceding sentence only as
  needed to represent already-current states; they may not decide an open
  issue or change an accepted decision.
- The same subsequent WP-010 change may file SI-37 for ADR-0011's
  already-recorded unassembled-multipath/unequal-identifier residual, add that
  filing to the register's authoritative count, class, state, and dependency
  rows, and correct ADR-0011's factual coverage claims by distinguishing the
  absolute v1 non-goal from the recognized or detected population for which
  enforcement is currently specified, with the uncovered population pointed
  to SI-37. This is issue-filing authority only: it does not decide SI-37,
  alter ADR-0011's accepted policy, change Section 2.1, or authorize
  implementation.
- A separately reviewed WP-010 change may **file SI-39** for the SAFE-003 /
  INV-003 conflict, and add that filing to the register's authoritative count,
  class, state, and dependency rows, plus a matching WP-010 `CHANGELOG.md`
  entry. Section 0.2 requires this rather than permitting it: two requirements
  in this spec conflict, so agents MUST stop and file rather than silently pick
  a side.

  **The conflict, both sides quotable.** SAFE-003 requires every plan that
  writes storage to bind an identity record containing "all available
  identifiers", including partition-table state, and says of it: "A blank
  device can therefore be Strong." INV-003, as amended by ADR-0013 in spec
  6.0.0, says unprivileged discovery "MUST NOT report a state its contract
  cannot reach", naming as an instance "a medium as positively without a
  table … where the contract does not separate that case". The macOS increment
  6 matrix measured exactly that non-separation: a blank disk and media
  carrying ext4, LUKS2, LVM2 or mdraid project byte-identically to the
  unprivileged client. So on macOS a client may not report `Absent`, cannot
  positively determine the state, and the device cannot be Strong — which is
  what SAFE-003 says it can be.

  **Recorded because it is uncomfortable rather than despite it: this
  repository created the conflict today.** INV-003's amendment is ADR-0013's,
  landed hours earlier, and its adversarial round did not reach SAFE-003. The
  filing must say so, because a register that hides where a conflict came from
  is worth less than one that does not.

  **Issue-filing authority only.** It does not decide SI-39, choose between
  SAFE-003 and INV-003, amend either, revisit ADR-0013 or ADR-C3, change the
  specification version, or authorize implementation. It may not decide
  whether reporting `Present` on a medium whose consistency the contract
  cannot establish is itself such a report — that question overlaps SI-35 and
  must be recorded as adjacent and undecided, never resolved here. Every
  resolution named in the filing is recorded as an option, none as a
  recommendation carrying authority.
- A separately reviewed WP-010 change may **file SI-40** for the FS-007 /
  CAP-003 conflict, and add that filing to the register's authoritative
  count, class, state, and dependency rows, plus a matching WP-010
  `CHANGELOG.md` entry. Section 0.2 requires this rather than permitting
  it: two requirements in this spec conflict, so agents MUST stop and file
  rather than silently pick a side.

  **The conflict, both sides quotable.** FS-007: "Surface immutable
  technical limits, such as XFS not shrinking, as explicit blocked
  reasons." CAP-003's definitions: "`blocked` — implemented, but a runtime
  precondition fails (missing tool, version, state)" and "`unsupported` —
  the product does not implement the operation for this target." An
  immutable technology limit is not an implemented operation with a
  failing runtime precondition, so giving it CAP-003 `blocked` contradicts
  `blocked`'s definition; giving it `unsupported` contradicts FS-007's
  word "blocked". The case is product-visible on every capability surface.
  WP-050 increment 1 deliberately left the `TechnologyLimit` reason's
  status coupling unasserted rather than bury the choice in a constructor
  (`crates/capability/src/lib.rs`), and WP-050 increment 2's
  technology-limit composition waits on the decision.

  **Readings the filing records as options, none as decided.** (a)
  FS-007's "blocked reasons" is the generic noun phrase for the reason
  vocabulary — the reading the repository's own prose already uses
  ("the blocked-reason capability surface is WP-050's") — and the status
  follows CAP-003's definitions: `unsupported`, carrying the limit as its
  explicit reason. (b) FS-007 mandates the literal `blocked` status, and
  CAP-003's definition list is the text to amend. (c) any distinct shape a
  round proposes. The filing may record, as classification rather than
  recommendation, that (a) amends no normative text while (b) retexts
  CAP-003's definitions.

  **Issue-filing authority only.** It does not decide SI-40, choose
  between FS-007 and CAP-003, amend either, change the specification
  version, or authorize implementation. Every resolution named in the
  filing is recorded as an option, none as a recommendation carrying
  authority.
- A separately reviewed WP-010 change may **file SI-38** for the INV-003 /
  SAFE-002 conflict that the 2026-08-03 and 2026-08-04 measurements made
  concrete, and add that filing to the register's authoritative count, class,
  state, and dependency rows, plus a matching WP-010 `CHANGELOG.md` entry.
  Section 0.2 requires this rather than permitting it: "If two requirements in
  this spec conflict, agents MUST stop, file a spec issue describing the
  conflict, and not silently pick a side." Both sides are quotable spec
  requirements, so this is the requirement-versus-requirement case that belongs
  in the register rather than on a repository issue.

  **Issue-filing authority only.** It does not decide SI-38, choose between
  INV-003 and SAFE-002, amend either requirement, scope INV-003 to a privilege
  layer, add a fail-closed clause to it, change the specification version,
  decide or pre-empt SI-35, or authorize implementation. Recording the conflict
  is the whole of the grant; every resolution named in the filing must be
  recorded as an option, none as a recommendation carrying authority.
- One later, separately reviewed WP-010 integrity change may edit only the
  authoritative SI-11 metadata, the existing SI-12 and SI-34 explanatory prose
  in `docs/spec-issues/README.md`, the 4.3.0 historical changelog row in
  `AGENT_BUILD_SPEC.md`, and a matching WP-010 entry in `CHANGELOG.md` to:
  (1) correct SI-11's `Hash-visible` value from `no` to `yes` and add MODEL-005
  and Section 6 to its requirement metadata, because Part 6's retained
  affected-set, bind-set, table-extent, and host-qualified-extent obligations
  and unresolved choices necessarily affect hashed plan or snapshot bodies;
  (2) replace SI-12 and the 4.3.0
  changelog row's attribution of unequal simultaneous multipath identifiers to
  the bridge-synthesis and two-layer-serial measurements with the narrower
  retained fact: those measurements establish that equality across identity
  layers or bridges cannot be assumed, but no retained run measured one LUN on
  two simultaneous paths with unequal identifiers, and the unmeasured
  enforcement residual remains filed as SI-37; and (3) state in SI-34 that the
  stale-signature result refutes the universal roster-identity premise for one
  named finite projection, but does not by itself establish complete graph or
  final-verdict divergence. This authorization is metadata and historical-
  fidelity repair only. It may not select an option, change an issue's state,
  class, or dependencies, alter an ADR decision or normative requirement,
  promote evidence, or change the specification version. SI-12 remains
  Resolved and SI-11, SI-34, and SI-37 remain Open.
- One later, separately reviewed WP-010 integrity change — the Part 1
  framing-prose currency pass, repository issue #198 — may edit
  `docs/spec-issues/README.md` only by **prefacing the Part 1 prose block
  that begins "Half the approach is settled; the other half is reopened."
  and ends with the "A second instance has since been measured, in a
  different layer." paragraph with a dated-history banner**, plus a
  matching WP-010 `CHANGELOG.md` entry, for one purpose: to mark that
  block as recording the state as of the SI-34 filing — naming SI-35's
  resolution in spec 8.0.0 by ADR-0014 and SI-34's in spec 9.0.0 by
  ADR-0016, the placement question closed by the architecture the SI-35
  resolution built — and to direct current status to the authoritative
  table and the issues' own entries. The retained paragraphs stay
  verbatim as history. This grants no authority to change any issue's
  state, class, dependencies, options, or evidence record, any
  authoritative row, any ADR decision, any normative requirement, or the
  specification version; to touch any issue entry; or to edit any prose
  outside the banner it adds. The issue's requested sweep of Part 1's
  remaining framing prose is a check, not an edit: any further superseded
  statement it surfaces needs its own recorded grant rather than this one
  stretched.
- `docs/quality/fuzzing.md` covers only WP-010's parser-target descriptions and
  resource/coverage consequences.
- `docs/reviews/WP_010_TRACEABILITY_MIGRATION_2026-07-30.md` was reserved for,
  and now contains, the zero-loss ledger required when this package converts
  its existing hand-written evidence to generated traceability.

These paths are shared with WP-000 where its broader claims already match. The
overlap is intentional and does not erase the narrower obligations above.

## Delivery status

This package is being delivered in increments. Each is independently gated.

| Increment | Scope | Status |
| --- | --- | --- |
| 1 | `pce/1` canonical encoding profile, Rust codec, golden vectors, strict decoder | Delivered |
| 2 | TypeScript codec and cross-language golden-test harness (MODEL-005) | Delivered |
| 2a | Schema-level canonical set order, inherited-depth enforcement, and shared Rust/TypeScript set vectors (MODEL-006, SI-31) | Delivered |
| 3 | Section 5 domain types with MODEL-003 schema versioning and MODEL-004 provenance | Started 2026-08-09, delivered in slices below |
| 3a | Node naming per ADR-0019: `NodeId` as a derived positional address over a domain-separated canonical preimage, per-kind naming maps, collision-group absorption, and the committed regressions (ancestor-only address, L9 byte-identical group, duplicate-designator flag, stale-pair two addresses, hybrid two views, distinct backing files) | Delivered |
| 3b | Edges and topology construction per ADR-0019: the five MODEL-002 edge kinds with ADR-0018's semantics classes (platform-membership platform-asserted and bind-inert in v1), `Topology::build` as the fail-closed construction boundary — unknown referents, self-edges, duplicates, and every endpoint pair outside the kind's table refused as typed values — with the theorem premise (no backing, production, or host-backing pair targets a physical device) enforced by the pair table and proved by exhaustive enumeration, not review | Delivered |
| 3j | The authoring-set architecture: ADR-C3's table state becomes a body-carried snapshot fact stamped when the helper produces the snapshot at validation (ADR-0014); Section 6's bound identities enter the plan body keyed by target address, strength still derived; and the boundary enforces the client-authored-value-never-validates rule — a plan identity whose table state disagrees with the snapshot's stamp refuses as `AuthoredFieldMismatch`, while the derived protection verdict stays committed through its body-carried inputs, unforgeable because there is nothing stored to forge | Delivered |
| 3i | The plan body and its validation boundary: PLAN-004's risk model (five severities, the five orthogonal flags, plan severity as the step maximum), the Section 6 skeleton whose vocabularies are decided (schema identity, plan id, creation timestamp, the snapshot hash as bound at validation, PLAN-007's window as enforced-not-re-derived body content, steps as a semantic dependency array), and `from_canonical_body(bytes, &snapshot)` re-running every step through the sole constructor — the hand-forged artifact refused by recomputation (ADR-0012's second verification row), the wrong-snapshot presentation refused (the ACC-007 shape at the type layer) | Delivered |
| 3h | The mutating step's sole constructor and ADR-0012's proof: `PlanStep` with private fields, constructible only through the closure over the snapshot's authenticated facts; the compile-fail doctest is the construction-refusal proof in the chassis's pattern; ADR-0018's acknowledgment vocabulary lands closed at three, with `Release` converting exactly the orphan-signature arm on exactly the named node, refused nodes coverable by no acknowledgment, and the unmodelled kinds refusing at construction | Delivered |
| 3g | The protection gate on capability, per ADR-0018's canonical-step rule: CAP-002's fourteen operations modelled with their source/mutating classes, canonical effect-table entries derivable with no plan in scope, and `protection_gate` computed by the same closure the constructor runs — CAP-005 agreement enumerated over every target/operation pair, source operations never suppressed, refusals mapping to `unsupported` and indeterminacies to `blocked` | Delivered |
| 3f | Body carriage of the protection facts: extents, transports, and member counts enter the snapshot body per node — verdict inputs authenticated per ADR-0016's logic — with kind-compatibility refusals for misplaced facts, round-trip through the typed boundary, and the full-stack regression: a decoded body's own facts drive the closure and refuse the pool end to end | Delivered |
| 3e | ADR-0018's protection layer as pure functions: the three-valued verdict with the residual arm `Indeterminate` (never `Permitted`), the enumerated arms (non-goal technologies, Fusion by self-reported member count, the transport positive-list at device scope, orphan signatures, collision groups), node-local inheritance (producer and own-root-device only, never a sibling), the effect-table range sets with release-as-destruction, and the affected-set fixpoint whose two destruction classes keep containment reach range-bounded — the committed regressions hold: the root-on-ZFS pair, sibling non-capture, and the LUKS descent to the pool below | Delivered |
| 3d | SAFE-003's identity record: the three-valued table state with ADR-C4's three-distinct-body-values guard held in bytes, strength derived and never stored (a forged `strength` key refuses as an undeclared field), ADR-0017's witness with the measured comparison semantics (comparable only within an unchanged epoch, never on a decrease, absence as unavailability), and the strict body round-trip | Delivered |
| 3c | Snapshot body/envelope and the typed boundary: two schema identifiers for captured and simulated topologies (MODEL-003, §5 domain separation), the CONC-004 transitional marking in the body, nodes and edges as MODEL-006 sorted sets, MODEL-004 provenance in the envelope with ADR-C4's derived-never-stored confidence, and `from_canonical_body` — the strict schema-validation pass with its own error type, whose decode-recompute equality rebuilds the topology and requires it to reproduce the input bytes exactly | Delivered |
| 3k | The body-format record and its cross-language proof: `schemas/domain/` documents for the three delivered body schemas (topology-snapshot body, plan body, node-entry format), each recording a decided format and deciding nothing, plus `body-vectors.json` — snapshot, plan, and node-entry vectors that are the Rust constructors' own pinned output, the plan vectors binding their snapshot's recorded digest, reproduced byte-for-byte and digest-for-digest by the TypeScript codec through the existing required cross-language job | Delivered |
| 4 | `cargo-fuzz` targets for the decoder (Section 11.4) | Delivered |

Increment 1 is the part ADR-C1 gates, and everything else in the package
encodes through it, which is why it is first.

## Assumptions

- The encoding profile is fixed by ADR-C1 and specified in
  `schemas/canonical-encoding.md`. Changing it is a profile version bump, not an
  edit, because it would change every hash previously issued.
- No field in the Section 5 domain model is inherently floating-point. The
  profile makes floats unrepresentable, so a genuine need for one is a spec
  question before it is an encoding question.
- `usize` is at most 64 bits on every supported target.

## Out of scope

- RPC transport and schemas (WP-040).
- Capability engine interfaces (WP-050).
- Any discovery, planning, or storage mutation.

## Increment 3 status

Increment 3 started 2026-08-09. It was blocked from 2026-07-28 to
2026-08-09: extracting the Section 5 requirements surfaced specification
conflicts filed in `docs/spec-issues/README.md` as Section 1.11 requires,
and **that register's status table is the sole authoritative blocker list;
this document no longer copies its names or counts.** The register's
direct-blocker row emptied on 2026-08-09 (every decision landed through
ADR-0011 to ADR-0019, with SI-37 and SI-28 reclassified open off the
gate), which is what permits types to be written. The increment lands in
slices; each slice implements only decided texts and cites them.

Under MODEL-005 and ADR-C1 a hashed artifact's shape cannot be changed later
without invalidating every hash the product has issued, and an authorization
token that no longer matches has no migration path. Choosing now and correcting
later is the only option with no cheap exit, which is why this stops rather than
proceeds on assumptions.

### Package-owned codec prerequisites

| Prerequisite | Owner | State |
| --- | --- | --- |
| SI-31's set sort-key encoding reset the depth budget, so a standalone-valid element could become undecodable after insertion into a deep artifact | This work package, before increment 3 | **Closed by ADR-C6 and MODEL-006.** Both languages require the set's actual depth, share accepted/rejected boundary vectors, and emit the same bytes they sort |
| Raw-byte hashing accepted bytes nothing validated — and generic `pce/1` validation cannot establish artifact-schema validity | This work package, before increment 3 | **Codec half closed; typed boundary remains mandatory.** `hash_encoded` / `hashEncoded` reject non-canonical `pce/1`, but increment 3 MUST fully validate the artifact schema (including MODEL-006 sets) before hashing and MUST NOT authorize through the generic value or raw hash APIs |
| The TypeScript encoder authenticated forged payloads as other values | This work package, before increment 3 | **Closed**, see below |

## Codec remediation, before any domain artifact uses this encoder

A progress review found two defects on the MODEL-005 authorization boundary and
reproduced both. Neither needed a specification decision, and both had to close
before increment 3 writes a type that hashes through this codec.

### A forged boolean was authenticated as the opposite value

The TypeScript `bool` arm used JavaScript truthiness:

```ts
out.push(value.value ? 0xf5 : 0xf4)
```

so `{ kind: 'bool', value: 'false' }` encoded as `f5` — canonical **`true`** —
and `hash` published a digest over the opposite logical value. Reproduced before
fixing: the forged payload emitted `f5`, and `{ kind: 'bool', value: 0 }` emitted
`f4`.

TypeScript types do not protect an object that arrived as JSON, over RPC, from a
plugin, or as `unknown`. That is exactly why the `text` and `bytes` arms already
validated at runtime — `TextEncoder` coerces a non-string and `Uint8Array.from`
truncates modulo 256 — and the same reasoning simply had not been carried to the
rest. Rust is unaffected: its `match` is exhaustive over a real enum, so the
value cannot be constructed.

Every variant now validates:

- `bool` requires an actual boolean, so neither a truthy nor a falsy non-boolean
  can become either byte;
- `array` requires an `Array` and `map` a `Map`, where previously `.length` and
  `.size` on something else gave `undefined`, `BigInt(undefined)` threw a native
  `TypeError`, and the module's contract that every refusal is a
  `CanonicalError` quietly stopped holding;
- **map keys must be primitive strings.** This was the quietest of them:
  `requireWellFormed` iterates `.length`, which is `undefined` on a number, so
  its loop never ran and the key passed — then `utf8` coerced `1` to `"1"`. A map
  keyed by a forged number encoded as one keyed by text;
- a map key present with no value is refused rather than dereferenced;
- and a `value` that is not an object, or carries no string `kind`, is refused
  before its payload is read at all.

The test that should have caught this covered `text` and `bytes` only. It is now
table-driven with at least one forged payload per variant, so a variant added
without validation fails it, plus a dedicated test asserting a forged boolean is
refused in *both* directions and a property test that everything the encoder
accepts decodes back to the same value.

### Raw-byte hashing proves `pce/1`, not an artifact schema

Both languages exported a function that hashed whatever bytes it was handed,
documented as "use this only for bytes produced by `encode` or accepted by
`decode`". That is an instruction, not a guarantee. HLP-001 applies plans by
hash, HLP-003 binds interactive authorization to an exact hash, and SEC-001
authorizes only exact hashes, so a public digest-over-anything is a bypass for
whoever forgets.

`hash_canonical_bytes` and `hashCanonicalBytes` are gone. `hash_encoded` /
`hashEncoded` take their place and **decode first** — `decode` accepts only the
unique canonical `pce/1` encoding, so bytes that survive it are byte-canonical
by construction rather than by the caller's say-so. The private digest helper
each language keeps is called only where `encode` produced the bytes a line
earlier.

That is deliberately narrower than artifact validity. Generic `pce/1` must
accept a descending Array because it may be a semantic sequence; the same bytes
are invalid when an artifact schema declares that field to be a set. Therefore
the future increment-3 API must expose a typed decode/validate/hash path that
checks every schema invariant, including MODEL-006, before the exact bytes
enter authorization. HLP-001/HLP-003/SEC-001 callers MUST use that typed path
and MUST NOT treat the generic `hash`/`hash_encoded` primitives in either
language as an authorization verdict.

No digest changed: both languages still reproduce every recorded `sha256` in the
shared vector file, and a test in each asserts the two routes to a hash agree.

### What an adversarial pass then found in the fix

The first version of this remediation was attacked before it was proposed, and it
was directionally right and incomplete in a specific way: **every guard checked a
field, and a payload can lie about a field between two reads.**

- **`kind` was read twice** — once by the shape check, once by the dispatch — so
  a getter could be validated as `bool` and written as `null`. Both arms happened
  to be safe, which is why it is worth pinning: the guarantee held by luck.
  `kind` and the payload are now each read exactly once, and a test counts the
  reads.
- **Containers declared a count and then wrote a body**, reading the source
  twice. A `Proxy` over an array reporting `length` 5 while holding 2 items, or a
  `Map` subclass whose `size` disagrees with `entries`, produced a head that did
  not describe the body. Both are now snapshotted into private containers
  through captured intrinsics before anything is measured.
- **`bytes` trusted `Symbol.iterator`.** `instanceof Uint8Array` passed while a
  lying iterator yielded values outside `0..=255`, which `Uint8Array.from` then
  truncated modulo 256. The copy now verifies the exact intrinsic Uint8Array
  brand and copies indexed bytes into a new plain byte array.
- **`instanceof` is not a brand.** `Object.create(Uint8Array.prototype)` passes it
  with no typed-array internals, and the copy above then threw a native
  `TypeError` — breaking the contract that every refusal is a `CanonicalError`.
  Those are now converted.
- **`hashEncoded` validated a prefix and hashed a buffer.** `decode` walks the
  array through its `length` property while `crypto.subtle.digest` reads the
  underlying buffer, so a view with a shadowed `length` got its prefix validated
  and its whole buffer hashed — a digest over bytes nothing proved canonical,
  which is the one thing that function exists to prevent. Both steps now see the
  same snapshot.
- **Species and mutable prototypes could substitute semantic content.**
  `Array.prototype.slice` consulted a caller-controlled constructor species,
  and indexed getters could replace `push`, `map`, or `sort` after the call
  began. A generic Array element was changed, set elements were dropped or
  reordered, and a wider typed-array species made `hashEncoded` validate one
  byte while hashing two. Array/set snapshots are now species-free, internal
  arrays use captured intrinsics, and typed bytes are copied only after an exact
  brand check. Dedicated regressions reproduce each route.

The tests were vacuous in the same way the code was. `every variant refuses a
forged runtime payload` asserted only the error *class*, and its `array` case
used the string `'not an array'` — which has a `.length`, so the encoder walked
it as a sequence and refused one of its characters. **The array guard never ran,
and deleting it left all 24 tests green.** Every case now names the phrase its
refusal must contain, exactly as WP-020's mutation table does.

### Each guard is proven by a deletion that fails the suite

| Guard | Deleting it |
| --- | --- |
| `bool` must be a boolean | caught |
| `array` must be an `Array` | caught |
| `map` must be a `Map` | caught |
| map keys must be strings | caught |
| `hashEncoded` validates the bytes it hashes | caught |
| Array and set snapshots ignore constructor species | caught |
| Set snapshots/sorts use captured intrinsics after caller getters | caught |
| Byte snapshots require an exact Uint8Array brand and ignore species | caught |
| `encode` decodes its own output | **not caught** |

The last row is recorded rather than hidden. `encode` re-decoding its own output
makes §6.1 a computation instead of a claim, but every attack the pass produced
is caught by a field guard first, so **no test currently requires it and it is
not counted as evidence.** It is kept as defence in depth against the variant
nobody has thought of yet — which is exactly the case that cannot be written as a
test — and the code says so where it sits.

Each of the two original fixes was also verified by reverting it and confirming
the new tests fail.

Accepting ADR-C5 and ADR-C6 does **not** unblock this increment. They fix the
aggregation vocabulary and set encoding, respectively; the authoritative issue
register still has unresolved hash-visible and protection-model gates. Writing
types against only some answers would freeze a shape the rest are still free to
change.

SI-28 is the exception to the ordering above: it is a data-destruction path in an
already-accepted decision (ADR-C3) rather than a question about types, and it
does not depend on anything else here.

Increment 4 (`cargo-fuzz`) did not depend on the domain types and is delivered.

## Verification and remaining limits

`cargo xtask ci` runs the Rust codec and stable canonicality suites at Tier 1.
The domain crate performs no I/O, launches no process, and never touches
storage, so this package cannot reach a host or user disk. It neither implements
nor exercises privileged behavior.

`cargo xtask cross-language` runs the Rust and TypeScript suites against the
same canonical value and canonical-set fixtures. The command remains a separate
required CI job so Rust-only local work does not silently waive parity.

The original encoder was manually checked against the `cborg` oracle named by
ADR-C1, including map-key order and the `2^53` and `2^64 - 1` boundaries. That
historical differential run is not a CI gate and `cborg` is not a runtime
dependency. The original dependency inspection counted unsafe occurrences in
`generic-array` (78), `sha2` (29), `cpufeatures` (9), and `block-buffer` (4);
`digest`, `crypto-common`, and `typenum` forbade unsafe code. Those exact
version-dependent counts are historical inspection, not a frozen policy
assertion. SAFE-009 applies to this project's crate, which inherits workspace
`unsafe_code = "deny"` and is checked by the Tier-1 gate.

Scheduled long fuzzing and corpus accumulation are now configured and have
completed successfully; `docs/quality/fuzzing.md` is the current source for
resource limits and remaining Section 11.4 gaps. Parsers that do not yet exist
have no target, and the future release pipeline still needs the zero-untriaged-
crash gate.

The Section 5 domain types, MODEL-002 layering, MODEL-003 public artifact
schemas, and MODEL-004 provenance remain unimplemented. This increment only
settles and implements the collection boundary they must use.

Two repository-summary edits were deferred when this increment was accepted so
that WP-010 would not cross its README sub-file grant: the WP-000 row still
grouped WP-010 with the hand-maintained trace documents, and the accepted-ADR
roster stopped at ADR-C5. A subsequent WP-000-owned follow-up removed the stale
phrase and added ADR-C6 to the roster. The WP-010 row and this document already
reported the generated conversion and accepted decision.

## Traceability

How this package's `docs/traceability/` document is produced. Read by
`cargo xtask traceability`, which checks the declaration against the document it
describes in both directions: declaring `hand-maintained` while that file
carries the generated marker is refused, and declaring `generated` holds it to
byte equality with what the annotations produce.

```traceability
mode: generated
```

Evidence that does not belong beside one Rust test is declared here with typed
paths and commands. `cargo xtask traceability` validates every reference before
rendering `docs/traceability/WP-010.md`.

```traceability-evidence
- requirements:
  - MODEL-005
  evidence:
  - path: schemas/canonical-encoding.md
  claim: >-
    The normative pce/1 profile defines one canonical byte encoding and remains
    unchanged by the schema-level set rule.

- requirements:
  - MODEL-001
  - MODEL-003
  - MODEL-005
  evidence:
  - path: schemas/canonical-encoding-vectors.json
  - path: crates/domain/tests/shared_vectors.rs
  - path: packages/canonical/src/canonical.test.ts
  - command: cargo xtask cross-language
  claim: >-
    Rust and TypeScript consume one shared fixture that pins exact bytes and
    hashes, schema-domain separation, and integer boundaries through u64.

- requirements:
  - MODEL-003
  - MODEL-005
  - MODEL-006
  evidence:
  - path: schemas/domain/body-vectors.json
  - path: schemas/domain/topology-snapshot-body.md
  - path: schemas/domain/plan-body.md
  - path: schemas/domain/node-entry-format.md
  - path: crates/domain/tests/body_vectors.rs
  - path: packages/canonical/src/body-vectors.test.ts
  - command: cargo xtask cross-language
  claim: >-
    The increment-3 body schemas are documented as delivered formats and held
    to one shared fixture: the snapshot, plan, and node-entry vectors are the
    Rust constructors' own pinned output, the plan vectors bind their
    snapshot's recorded digest, and both languages reproduce every byte and
    digest.

- requirements:
  - MODEL-005
  - MODEL-006
  evidence:
  - path: docs/work-packages/WP-010.md
  - path: crates/domain/tests/canonical_sets.rs
  - path: packages/canonical/src/canonical.test.ts
  - command: cargo xtask cross-language
  claim: >-
    One normative rule and one deliberately unsorted fixture hold both
    implementations to bytewise full-element ordering, strict uniqueness,
    unchanged semantic arrays, and inherited depth in both the producer and
    validator.

- requirements:
  - MODEL-005
  - MODEL-006
  - SAFE-005
  evidence:
  - path: packages/canonical/src/canonical.ts
  - path: packages/canonical/src/canonical.test.ts
  - command: cargo xtask cross-language
  claim: >-
    The TypeScript boundary refuses number-for-bigint and ill-formed surrogate
    substitutions, preserves non-normalized text, rejects byte-equal repaired
    keys, and snapshots generic arrays, schema sets, and hash input without
    letting caller-controlled species or prototype mutation substitute, drop,
    reorder, widen, or split the bytes it validates and emits.

- requirements:
  - SAFE-009
  evidence:
  - path: crates/domain/Cargo.toml
  - command: cargo xtask ci
  claim: >-
    The domain crate inherits the workspace lint policy, including denied
    unsafe code, and the Tier-1 gate compiles and lints that boundary.

- requirements:
  - Section 11.4
  evidence:
  - path: fuzz/fuzz_targets/decode_is_canonical.rs
  - path: fuzz/fuzz_targets/roundtrip_value.rs
  - path: docs/quality/fuzzing.md
  - command: cargo xtask fuzz
  claim: >-
    The two pinned fuzz targets cover strict pce decoding, value round trips,
    and schema-set producer/validator fixed points without adding binary corpus
    artifacts to the repository.

- requirements:
  - SEC-010
  evidence:
  - path: packages/canonical/package.json
  - path: packages/canonical/package-lock.json
  - command: cargo xtask cross-language
  - command: cargo xtask supply-chain
  claim: >-
    The TypeScript authorization path has no runtime dependencies, its
    development graph is locked, and repository gates install and audit the
    committed graph rather than resolving an unrecorded one.

- requirements:
  - Section 11.7
  evidence:
  - path: docs/work-packages/WP-010.md
  - command: cargo xtask traceability
  claim: >-
    WP-010 traceability is generated from validated sources, with an explicit
    zero-loss ledger for every claim and narrative section in the former
    hand-maintained document.
```
