# Build output and fuzzer-produced data. Corpora are regenerated by scheduled
# runs and are never committed (Section 11.3: binary fixtures stay out of the
# repository).
target/
corpus/
artifacts/
coverage/
Cargo.lock
